# HANDOFF — King Maker website lane → **Website Engineer 17 (WE17)**

**From:** WE16 · **To:** WE17 · **Date:** 2026-06-27 · **Lineage:** WE4→…→WE14→WE15→WE16→**you**
**Blackboard handle:** `website-engineer` · **You own:** the **King Maker firm site** (`king-maker-site/` — THE active project, blue/white, home + buyer's guide SHIPPED) + **Summit & Oak** (the live $10k reference roofing build, PAUSED) + the **launch-builder / WO workflow** + the **custom skill/gate spine** (§6) + the **KM value-prop / GTM research spine + the 1,017-roofer audit + the content/buyer's-guide engine** + the **pricing/offer model** + coordination with **n8n / meta-ads / cos** + the contractor **template/demo-engine** + the **roofing + AM flagships** (both OUT unless named) + the **verify-gate system**.
Supersedes `AGENT-WEBSITE-ENGINEER-16-2026-06-26.md` (kept on disk in AMW-Redaux; its §4/§5/§8 carried forward below). Carried items attributed (WE7)…(WE16), VERBATIM — do the same for WE18.

> ⭐ **THE ARC THIS SESSION (WE16):** (1) Drove **WO_08** (propagate the maximalist home vocabulary → the rest of the site, readable-first / heading-level motion on inner pages) + **WO_09** (the buyer's guide — 11 categories / 31-32 sub-sections, **REWRITTEN to structure not reused**) — both **SHIPPED + live + Joseph-approved** (buyer's guide verified 32/32 subs 200). (2) **WO_22 on Summit & Oak** — removed the **9 incoherent service×city combo pages** (the matrix is **ALL-OR-NOTHING**); 155→146 URLs, combos 301→city hubs, re-verified LIVE. (3) **The n8n quota crisis** — a 1-min Meta poller blew the 2,500/mo cap; killed it, audited zero-polling template-wide; confirmed `/api/lead` is **WIRED** (the WE15 "{ok:true} no-op" claim had DRIFTED — route.ts is ground truth). (4) **Wired kingmakerseo.com → the firm site** on Vercel (apex + www aliased; email/MX untouched). (5) **Generated the blue/white KM logo PNGs** for Joseph's Instagram. (6) **Pricing model converged → $497/mo subscription** (REVERSES the WE15 one-time lean) → drafted **WO_10 (the pricing tab — ONE new page)**, corrected to Joseph's exact scope; **NOT built yet — WARM prompt emitted, awaiting his builder.** (7) **Deleted brigade-mode** (stale artifact; native Workflow/ultracode dominates) + hardened rapid/edit/launch-builder against the mode-vs-build collision. **OPEN #1: land a CLIENT (outreach) — but Joseph re-opened building for the money page + the link-in-bio finish, which directly serves that.**

---

## 0. ⭐ ON ARRIVAL — ORIENT, THEN ASK (never auto-start)
1. **Reread this file in an ultrathink loop until a pass yields no new info (min 3 passes)** — the READ loop. Name what each pass adds; a clean final pass = converged.
2. **RE-VERIFY these volatile claims (dated 2026-06-27) by RUNNING the commands — a handoff fact is a lead, not a truth:**
   - **Firm site:** `curl kingmakerseo.com` → 200 + "WE MAKE KINGS" (the firm site is live on the APEX now, WE16 aliased it); `/guides` 200; **`/pricing` should be 404** (WO_10 not built — if it's 200, the build shipped, re-scope). `king-maker-site/` is STILL uncommitted (`?? king-maker-site/` in cg-main; deploys via Vercel CLI — a `git log` shows nothing).
   - **Summit & Oak:** `git -C summit-oak-roofing log --oneline -3` → `d8bd051` (WO_22). Live combo `…/locations/cary-nc/roof-replacement` → **301** (confirms WO_22 deployed). ⚠️ Confirm whether `d8bd051` is on `main` or still on `wo22-remove-combos` (memory says merge-confirm was pending; prod serves the fix either way).
   - **Mailbox:** `bb.mjs read --agent website-engineer` → n8n-architect (poller-kill + July-1 dependency) + vault-agent (pricing brief), both actioned this arc but **left un-acked so you inherit the July-1 loop**. Ack ONLY what you handle.
3. **STOP — do NOT auto-start.** Present Joseph a one-line "where we left off" + a TLDR of candidate next tasks (§3), then ask which. ⚠️ The driving goal is a CLIENT; building is only sanctioned where it unblocks outreach (the money page + the link-in-bio finish). If he reaches for build-for-build's-sake, name the build-avoidance (§8 build-stop — you're the enforcer).

---

## 1. What this is
King Maker sells done-for-you authority contractor websites + organic SEO. **THE active project = the KM FIRM SITE** (`king-maker-site/`, live on **kingmakerseo.com** + `kingmaker-firm.vercel.app`) — a B2B blue/white site selling to **every contractor trade**; the maximalist home (WO_07) + the buyer's guide (WO_09) are SHIPPED + approved. **Summit & Oak** (`kingmaker-summit-oak-roofing.vercel.app`, 146 URLs) = the live roofing reference clients clone, PAUSED. **WE = architect/reviewer: spec the WO, QA the live result — never build inline.** Two build gears: **WE inline `edit-mode`** for small/iterative tweaks on a live local site · **launch-builder → fresh/warm builder** for big/net-new/batch. (WE16 note: Joseph also runs his OWN builders, handing them WO prompts directly + km-handoff briefs — e.g. the separate `/work`+`/system`+`/playbook` rework builder, see §3.)

## 2. ⭐⭐ CURRENT STATE (volatile — 2026-06-27, re-verify per §0.2)

### 🟢 THE FIRM SITE — home + buyer's guide SHIPPED; live on the apex domain
| | |
|---|---|
| **Codebase** | `C:/Users/josep/Claude Gravity/king-maker-site/` (Next 16 SSG + Tailwind 4 + framer-motion). **UNCOMMITTED — `?? king-maker-site/` in cg-main, NOT its own git repo.** On-disk = truth. Deploys via **Vercel CLI** (project `king-maker-site`, `prj_7D4fxuL7C2vvHoMr1Tn7jU3r8p6G`). |
| **PROD** | **`kingmakerseo.com` (+ www) = LIVE on the firm site** (WE16 aliased apex+www → the firm deployment; DNS was already at Vercel; **email/MX untouched**). Also `kingmaker-firm.vercel.app`. Byte-check 2026-06-27: 200 + "WE MAKE KINGS"×1 + `/guides` 200. ⚠️ **Vercel alias is per-deployment — re-alias after EACH deploy** (or convert to a permanent project domain). ⚠️ **`SITE_URL`/canonicals likely still point at `kingmaker-firm.vercel.app`** — the flip to `kingmakerseo.com` is a PENDING step folded into the WO_10 deploy (§3). |
| **Home (WO_07)** | The maximalist home — Joseph-approved, the SHIPPED standard. Captured in `king-maker-site/KING_MAKER_TEMPLATE_MANIFEST.md` (the lossless primitives/tokens/recipes manifest — propagate FROM it, never re-derive). |
| **Buyer's guide (WO_09)** | LIVE: `/guides` pillar + **11 categories / 31-32 sub-sections**, REWRITTEN to the structure (not reused), readable-first, NEUTRAL-education voice, MEASURED 1,017-site stats baked in. WE16 verified 32/32 subs return 200, build 65 pages, visual QA ship-grade. The pricing guide within = `lib/guide-content/what-a-website-should-cost.ts` (3-tier ladder; enterprise now $100/page ≈ $10–15k after WO_10 §3). |
| **Motion primitives** (`components/motion.tsx`) | `useReducedMotionSafe` · `CountUp` (`useInView amount:0.35` = the stuck-at-0 cure) · `Reveal` · `Parallax` · `TypeIn` (literal per-char typewriter, `.km-caret`) · `DrawLine` (blue/red) · `Eyebrow` · `Stagger`/`StaggerItem`. `ENTER_EASE [0.16,1,0.3,1]`. Depth recipes in `app/globals.css` (`.km-card*`/`.km-code`/`.km-caret`/`.km-arrow-blink`). **Home = maximal motion; inner pages = heading-level only** (the gradient, `feedback_overstimulation_threshold`). |
| **🔴 NOT link-in-bio-ready yet** | (a) **`/work`** — gappy, inconsistent hero, some weak cards. (b) **`/system`** — Joseph: "completely fucked." (c) **`/playbook`** — Joseph may CUT it (undecided). **These 3 are a SEPARATE WO** Joseph is running his own builder on (he drafted it via km-handoff → `HANDOFF_website-engineer_firm-site_2026-06-27.md`). NOT yours to draft unless he asks. (d) `/pricing` tab = WO_10 (drafted, not built). |

### ⭐ Summit & Oak — PAUSED (`d8bd051` = WO_22, 146 URLs, 9.5/9.5)
`d8bd051` (WO_22 / Phase G: 9 service×city combos REMOVED, matrix all-or-nothing) ← `3aa63ba` (WO_21). Live: combos 301→city hubs ✅, prod 200, sitemap 146. ⚠️ **Confirm `d8bd051` is merged to `main`** (memory: was on `wo22-remove-combos` awaiting Joseph's merge-confirm; prod already serves it — reversible). Desktop CWV passes; mobile 4× LCP 3.0-3.3s = the accepted persistent-hero tradeoff. The 9 combos return only as a COMPLETE per-metro matrix at clone time (never a partial slice).

### ⭐ LEAD PIPELINE — WIRED, blocked on the n8n July-1 reset (NOT a no-op)
- `app/api/lead/route.ts` IS wired (WO_04): validates + honeypot + forwards to `LEAD_WEBHOOK_URL` → n8n `/webhook/km-firm-lead` → **KM Leads Telegram** (Resend fallback). `LEAD_WEBHOOK_URL` is set on Vercel prod. Forms: BookAppointment, ApplyForm, SoftCapture (`type:"audit"`).
- **BLOCKER:** n8n Cloud quota (2,500/mo Starter) was **exhausted** by the now-killed 1-min Meta poller. End-to-end fire is **UNVERIFIED** until the **July-1 reset**. July-1 DoD: redeploy firm site → fire 1 test lead → confirm Telegram fires (with n8n-architect).
- n8n state (from n8n-architect, this arc): poller `JFihxX8qwhW9Xu95` **OFF + renamed "DO NOT REACTIVATE"**; webhook Meta intake (Funnel v2 `Z86sufKPCE219sKP`, `/webhook/fb-leadgen`, form-agnostic) **exists + active**; zero-polling audit clean across all 5 workflows. **NOT upgrading the plan** (Joseph: "it'll bill me on the first").

### 🔴 Working-tree three-way (the invisible-work trap)
- **COMMITTED + DEPLOYED:** `summit-oak-roofing/` (own git repo, `d8bd051`, live).
- **ON-DISK, UNCOMMITTED (cg-main `?? `):** ⭐ the ENTIRE `king-maker-site/` (incl. `KING_MAKER_TEMPLATE_MANIFEST.md`) + **this arc's** `KM_SITE_WORKORDER_08/09/10.md` + `KM_SITE_PRICING_BRIEF.md` + `KM_SITE_PRICING_BRIEF_REFINEMENTS.md` + `SUMMIT_OAK_WORKORDER_22.md` + `HANDOFF_website-engineer_firm-site_2026-06-27.md` (Joseph's builder brief) + the WE14/15 WO docs + audit `.png`/`.cjs` artifacts (noise — ignorable).
- **DEPLOYED-but-uncommitted:** the firm site (live on kingmakerseo.com, no git history at all).
- **THE SKILL/GATE SPINE (`~/.claude/`, NOT in cg-main git):** edited this arc — `skills/{rapid-mode,edit-mode,launch-builder}/SKILL.md` (mode-collision hardening) + **brigade-mode DELETED**. Plus WE15's `skills/{create-template,kmwe,km-handoff,verify-before-claim}/` + `hooks/{skills-gate,skill-invoked-logger,verify-gate}.mjs` + `~/.claude/.skills-invoked/`. **User-global; a cg-main `git log` won't show them.**
- **OTHER:** 4 blue/white KM crest logo PNGs in `~/Downloads` (this arc, for IG). Memory at `~/.claude/projects/C--Users-josep-Claude-Gravity/memory/` (auto-loads).

## 3. ⭐ OPEN LOOPS (by type)
- **⭐⭐ #1 (driving goal): land a CLIENT.** The build-stop (§8) is nominally active, BUT Joseph RE-OPENED building this arc for work that DIRECTLY unblocks outreach: the money page + the link-in-bio finish. Enforce against build-AVOIDANCE, not against build-that-lands-a-client.
- **IN-FLIGHT — WO_10 (the pricing tab):** drafted + corrected to scope; **WARM builder prompt emitted to Joseph's builder; NOT built.** Scope = ONE new page `/pricing` ($497/mo Tier 1 + off-page Tier 2 $1k/$2k/$3k, ICP qualify in/out, book-a-call, SALES voice, NO $10–15k anchor / NO à-la-carte / NO exposed price / NO buyout) + §3 minimal guide edit (enterprise $75→$100/page ≈$10–15k, guide stays pure education) + §4 matrix-language scan-and-correct (home edits surgical + flagged). **DoD:** build LOCAL → Joseph eyeball → Joseph confirm → THEN deploy + re-alias kingmakerseo.com+www + **flip `SITE_URL`/canonicals → kingmakerseo.com** + byte-check the canonical host. **Your job when it returns: QA live (offer-integrity grep + the standard + flagged matrix edits) BEFORE his eyeball.**
- **IN-FLIGHT (Joseph's own builder, NOT yours to draft):** `/work` + `/system` + `/playbook` rework → the link-in-bio finish. Brief = `HANDOFF_website-engineer_firm-site_2026-06-27.md`. DoD: the 3 pages reach the home's standard (or /playbook is cut). QA-assist if Joseph asks.
- **AWAITING USER (Joseph):** eyeball WO_10 once built · the offer numbers (pre-market/fluid) · the low-comp off-page floor ($500 vs the $1k floor — he settles at build) · whether `/playbook` stays or gets cut · the SO `main`-merge confirm for WO_22 (prod already serves it).
- **AWAITING OTHER AGENT:** **n8n July-1** — redeploy firm site + 1 test lead → confirm Telegram (n8n-architect + you) · meta-ads to confirm the old form `985131077477390` (Free-Until-Rank) isn't running on an UNsubscribed page (n8n-architect pinged them; the only way real leads drop).
- **TIME-GATED:** July-1 n8n quota reset (unblocks the lead-pipeline end-to-end test).
- **HOUSEKEEPING:** vault inbox pending ("ingest inbox" — `handoff-firm-site-we17` + this arc's Mode-D note) · cg-main current-state STALE (635 sessions, "refresh stale").

## 4. Locked decisions (do-not-relitigate) + WHY — CARRIED VERBATIM + attributed
- 🔴 **(WE9) Summit & Oak = COMPLETELY SEPARATE STANDALONE SITE** (from-scratch Next.js SSG; Vite SPA rejected). **(WE9) Conversion = "double the leads", engineer to per-page CEILING; traffic = cold ORGANIC (NOT paid). (WE10) Bar = 9.5/10 conversion + 9.5/10 on-page SEO. (WE10) NEVER PBN.**
- **(WE10) BUSINESS MODEL = "FREE-UNTIL-RANK"** (small deposit, free until ranks ~90d; one-time price on rank; numbers in flux). **(WE13) REDEFINE "rank" → ORGANIC money-term rankings, NOT map-pack top-3.** ⚠️ *(WE16: the firm's OWN offer has since converged to $497/mo subscription — see WE16 NEW below; "free-until-rank" survives only as offer HISTORY / a client-side option, `feedback_premarket_ideas_fluid`.)*
- **(WE9) SO palette = contractor RED on charcoal** (`#D8262C`/`#FF6066`/`#161719`); Newsreader + Plus Jakarta, Title Case. **(WE9) NC insurance compliance non-negotiable** (no deductible-waiver/public-adjuster/carrier-guarantee/unattributed-stat). **(WE7)** cost slug `/roofing-cost`. **(WE7)** "caveman" = simple plain bullets. **(WE8) AM-safety** = flagship/demo-engine shared-atoms only (SO standalone → N/A).
- **(WE11) THE LAUNCH-BUILDER WORKFLOW is the build mechanism** (architect → WO → builder executes+deploys → WE re-audits live; corpus COMPOUNDS; WARM=minimal reread / COLD=full reread+kmwe). **(WE11) DUAL-INTENT FORKING** (urgent=call-first/flipped sticky; retail=quiz-first). **(WE11)** cost tables ROUND to $5k. **(WE11) Experience-first E-E-A-T**, author **Marcus Bell** (demo, swappable). **(WE11) City pages QUIZ-FIRST + the anti-doorway gate** (`scripts/doorway-check.mjs` + `lib/doorway-gate.ts`).
- **(WE12) THE $10K AUTHORITY-BUILD ARCHITECTURE** (build the SYSTEM; page count = OUTPUT of real jobs; anti-doorway = a BLOCKING gate; off-page ~71% = per-client fulfillment NOT the build). **(WE12) ROUTE SCHEME = `/locations/[city]-nc/`** (middleware 301s legacy; combos under ONE `[sub]` segment).
- **(WE12) ⭐⭐ KM DOCTRINE — "THE WEBSITE WINS THE MAP PACK WHEN ALL ELSE IS EQUAL"** (equal GBP + equal reviews → the deeper site is the tiebreaker and WINS; "supports-not-wins" is DEAD). **(WE12) Built ≠ Reachable** (a built+200+sitemap page can be a nav ORPHAN; BFS ≤2-click — `npm run reachability-check`).
- ~~**(WE13) KM BRAND LOCKED — gold/black**~~ → 🔴 **STRUCK / SUPERSEDED (WE14, Joseph, 2026-06-26): the firm site is BLUE/WHITE** (below). Gold/black `KM_BRAND_KIT.md` kept for history; do not rebuild to gold.
- **(WE13) ORGANIC-OVER-MAPPACK OFFER DOCTRINE** (`feedback_organic_over_mappack`): organic regional dominance = the engine + offer HEADLINE; the map pack is a proximity-capped (~5-7mi), near-default BYPRODUCT. Honest map math: ~1-2 jobs/MONTH early, modeled.
- **(WE13) CONTENT-FIRST / PRACTITIONER-AUTHORITY GTM** (`project_content_first_gtm`): the buyer's guide = the lead-magnet/authority hub; the research IS the sales asset; the **RELEVANCY framing**; **"verify it yourself / ask any AI"**; adversary = CONSUMER-ADVOCATE (arm the buyer; concede-to-win; OWN THE STANDARD).
- **(WE13) GHL/COMPETITIVE THESIS:** GHL ships a competent BROCHURE; we ship a ranking SYSTEM. HONESTY RAILS (FTC-exposed): lead with MEASURED organic/technical/AI gaps; CONCEDE conversion; doorways RARE — do NOT say "doorway mills." **(WE13) AI-LEGIBILITY STANDARD** (`feedback_ai_legibility_qa`): word-level heading reveals only (per-letter inline-block FRAGMENTS rendered text); verify static+rendered+AI extraction N/N. ⚠️ **SCOPE-CORRECTED (WE15): a CLIENT/Summit-&-Oak standard — NOT a firm-site requirement. (WE13) v2 is NOT the first headless run.**
- **(WE14) — the firm-site direction:**
  - **(WE14) THE SITE IS THE PROOF** — the firm's own site is Exhibit A; technical-SEO-as-proof, own-the-standard.
  - **(WE14) 🔴 BRAND = BLUE/WHITE + MAX READABILITY** (supersedes gold/black). Audience = **50-60yo contractors who don't want to be on a computer**, on mobile. Palette: ink `#0f172a` · blue `#1d4ed8` + action `#2563eb` + deep `#172554` · **red `#dc2626` = DAMAGE** · white/slate · tint `#eff6ff`. Square corners + two-font (Archivo display + Plus Jakarta body + scoped JetBrains mono).
  - **(WE14) EDUCATION-FIRST, BROCHURE-TEARDOWN — NOT GHL.** Enemy = the generic 5-10-page brochure; GHL = one example. Practical-premium documentation.
  - **(WE14) ⭐ INDUSTRY-NEUTRAL, SITE-WIDE.** Every trade lands → NO roofing-specific terms in neutral sections; roofers = the audit *sample* only.
  - **(WE14) MAXIMUM READABILITY is the gate** — 17px/1.7, ~65ch, bullets + answer-first + tables, plain English, mobile-first.
  - **(WE14) The 1,017-roofer "BROKEN ROOFER WEBSITE REPORT" (cos-10) = the empirical proof spine** (median 1/10; ~55% no location pages; the 4-tier findability gap). The gap section uses it.
  - **(WE14) The "verify-with-any-AI" angle is DE-EMPHASIZED on-site** — Joseph delivers it in VIDEOS; the AI-OS = quiet moat.
  - **(WE14) The BOOK-AN-APPOINTMENT form = the link-in-bio conversion point.**
- **(WE15):**
  - **(WE15) 🔴 THE SKILLS-GATE IS v2 — real-invocation, not name-scan.** v1 scanned the transcript for skill NAMES = a proven NO-OP. v2: a **PostToolUse:Skill logger** (`~/.claude/hooks/skill-invoked-logger.mjs`) writes `~/.claude/.skills-invoked/<session>.jsonl`; the **gate reads THAT**. Wired in `settings.json`; **activates on Claude Code restart**. Memory `reference_skills_gate`.
  - **(WE15) 🔴 AI-LEGIBILITY IS NOT A FIRM-SITE REQUIREMENT** (Joseph, 2026-06-26) — it's a **Summit-&-Oak / client-ranking-site** standard. The firm-site typewriter is a LITERAL per-char effect; headings carry full text via `aria-label`. **Keep `feedback_ai_legibility_qa` for client/SO builds.**
  - **(WE15) ⭐ COMPONENT-FIRST / legible+polished > bespoke** (`feedback_component_first_builds`): scaffold from component/template libraries (**Magic MCP / 21st.dev is wired**) + edit-from-there; standard agency components/cards/pricing sections are FINE; RELAX the every-pixel-bespoke purism + strict-square; KEEP the functional floor (readable · some density · not-overwhelming · VERY fluid+animated · mobile). Design skills are EDITORS, not from-scratch generators. The clone-and-reskin law, applied to the firm site.
  - **(WE15) THE OVERSTIMULATION THRESHOLD** (`feedback_overstimulation_threshold`): every motion/density choice must HELP the reader, not COMPETE. **Bar: readable · some density · NOT overwhelming · VERY fluid · VERY animated** ("not overwhelming" is the only ceiling). One-shot + reduced-motion-safe; the ONLY continuous motion = the typewriter `.km-caret` + `.km-arrow-blink`. A dark "premium SaaS" overlay was REJECTED.
  - **(WE15) THE SPEED/PRECISION SKILL FRAMEWORK:** `rapid-mode` (PRECISION FLOOR + AUTO-ESCALATE + persistent marker) · `edit-mode` (fast local edits, NO deploy) · `create-template`. **Full-precision override:** km-handoff/create-template/launch-builder/verify-before-claim/kmwe SUSPEND rapid for their duration.
  - **(WE15) THE ANTI-SKIM HARDENING PATTERN:** claim ≠ done; every gate leaves a **tool-derived ledger/artifact**; any gap → HALT. A gate that checks ambient TEXT enforces nothing; a self-test must reflect REAL-session conditions.
  - **(WE15) WO_07 = the maximalist home is the SHIPPED standard**, captured in `KING_MAKER_TEMPLATE_MANIFEST.md`. Propagate from the manifest, don't re-derive.
- **⭐ NEW (WE16):**
  - **(WE16) 🔴 THE FIRM'S OFFER = `$497/mo` SUBSCRIPTION** (REVERSES the WE10/WE13 one-time/free-until-rank lean for the FIRM's own pricing; pre-market/fluid — `km-offer-497-subscription`, `KM_SITE_PRICING_BRIEF(_REFINEMENTS).md`). **Two SEPARATE subs:** Tier 1 = the SITE, **$497/mo flat, month-to-month, NO contract**, **kill-switch** mechanic (lives on KM's Vercel; stop paying → dark) → framed **"built + hosted + maintained by us," NOT "you own it"**; value = the site + hosting + uptime, **NOT monthly deliverables**. Tier 2 = OFF-PAGE SEO, separate, **$1k low / $2k med / $3k high-comp(metro)** (~$500 very-low-comp), ~3-6mo expectation, DECAYS/ongoing. **WHY $497 not $297:** the price differential IS the premium signal — un-comparison-shoppable; do NOT race to the bottom. **NO exposed one-time build price · NO "X payments of $497" · NO à-la-carte** (each reopens negotiation). ICP = competitive-area contractors sick of shared/lead-gen leads; **qualify IN and OUT** (NOT for low-traffic markets). CTA = book a call.
  - **(WE16) 🔴 BUYOUT (~$5k one-time) = OFF THE WEBSITE ENTIRELY** — never mentioned anywhere public; handle ad-hoc/on-request only.
  - **(WE16) 🔴 NO SERVICE×CITY COMBO PAGES — the matrix is ALL-OR-NOTHING.** A COMPLETE per-metro matrix or NONE; a partial/random slice is a SALES-CALL LIABILITY ("why these 9 and not the rest?"). Applies to BOTH the firm site AND SO (WO_22 removed SO's 9 incoherent combos). Dedicated SERVICE pages + dedicated LOCATION pages only. "by trade" stays ARCHIVED. The matrix returns only as a complete per-client add-on at clone time.
  - **(WE16) 🔴 TWO HOWARD-ROARK VOICES, NEVER CROSSED** (`km-content-voice-register`): **EDUCATION / buyer's-guide = NEUTRAL, detached fact-stating** (here are the facts, the limits, this may well be enough for you; zero angst/hype/attacking). **SALES / cold-copy + the pricing tab = the declarative reframe-the-norm-as-mediocrity Roark voice** (`feedback_copy_voice_howard_roark`). The buyer's guide's first adversarial draft was flagged → neutral rewrite.
  - **(WE16) 🔴 kingmakerseo.com IS THE FIRM-SITE PRODUCTION DOMAIN** — aliased apex+www → the firm deployment (DNS at Vercel; **email/MX untouched**). Vercel aliases are per-deployment → **re-alias after each deploy** (or convert to a permanent project domain). The `SITE_URL`/canonical flip → kingmakerseo.com is PENDING (folded into the WO_10 deploy).
  - **(WE16) 🔴 n8n RULE: WEBHOOKS ONLY — NEVER polling/schedule/cron/IMAP triggers.** A 1-min poll = ~1,440 execs/day = blows the 2,500/mo Starter cap in ~2 days (it did — `km-meta-poller-quota-burn`). `/api/lead` is WIRED (route.ts is ground truth; the WE15 "{ok:true} no-op" had DRIFTED). NOT upgrading the plan.
  - **(WE16) BRIGADE-MODE DELETED** — a stale artifact; native **Workflow / ultracode** dominates a hand-rolled multi-agent brigade across the board (cost + reliability). Use native Workflow for orchestration. rapid/edit/launch-builder were hardened against the mode-vs-build collision (mode-reset preamble + wrong-gear guard + auto-escalate).
  - **(WE16) 🛑 MONEY-PAGE / OFFER CADENCE (Joseph-confirmed):** build LOCAL → Joseph eyeball → Joseph confirm → THEN deploy. NO auto-deploy on the pricing/offer surface (it's pre-market/fluid). The HOME is the approved WO_07 masterpiece — matrix-copy fixes there are surgical copy/data only + FLAGGED for his eyeball, never a redesign.

## 5. Failures & dead-ends — CARRIED VERBATIM + attributed; **NEW** at end
- *(carried WE7-WE11):* edit-anchor trap · shared.ts drift · `.next` EINVAL→purge · "chrome ≠ identity" · didn't-look-at-pixels · global palette swap = 770 axe · Vercel CLI token deploy-only · **`whileInView` + full-page screenshot = false "void"** · DOM ground-truth beats a single vision pass · **DOM line-count can't detect inline-flex wrap (measure HEIGHT)** · "still dark as fuck" vision-baseline trap (eyeball overrides vision SHIP) · generative before→after roof video FAILED · external AI audits mix real catches with myths · `/tmp` can't resolve playwright (probe inside the repo).
- *(carried WE12):* **Built ≠ Reachable (orphan-nav miss)** · doorway-check combos-vs-parent blind spot · merge=union dupes imports · over-caveating a LOCKED doctrine frustrated Joseph · the research red-teamed our own numbers.
- *(carried WE13):* the **"site is only 19%" / off-page-pivot drift** (NEVER frame the site as "only 19%"; NEVER pivot a site-question to off-page — `feedback_site_wins_pack`) · doorway-inflation hypothesis KILLED by data · underselling via "component exists" ≠ "nothing to do" · crawler-mechanism overconfidence (aria-label NOT a ranking signal) · the WO_16 audit OVERCLAIMED Speakable · the 529-survived-via-resume pattern.
- *(carried WE14):*
  - **⭐⭐ "Builder reports clean" ≠ "is clean" — RE-VERIFY LIVE.** (WO_19/20 re-audit caught a missed WO fix + a regression the builder didn't flag.)
  - **⭐ The SESSION-RESUME REPLAY trap.** A replayed stale builder report ≠ current state — verify git SHA + live before acting.
  - **⭐🔴 The PROD-STALE trap.** A prod alias looked current but served the old build. NEVER trust a prod alias as "current" — verify palette/markers/bytes.
  - **⭐ The INDUSTRY-NEUTRAL catch.** A roofing-specific mockup; the firm site sells to all trades. Default neutral.
  - **Buggy guide-depth measurement** — a greedy `sed` tag-strip ate the HTML → false "0 words". Use byte-size + tag counts.
- *(carried WE15):*
  - **⭐⭐ THE SKILLS-GATE v1 NO-OP — a gate that checks AMBIENT TEXT enforces nothing.** v1 scanned the transcript for skill names (present every session) → could NEVER block; the synthetic 13/13 self-test gave FALSE confidence. **Lessons: (1) a gate must verify a tool-derived SIDE-EFFECT (a real invocation/receipt), not text; (2) a self-test must reflect real-session conditions.** Same bug was in `verify-before-claim` Gate #2 — killed it.
  - **⭐ THE 10-20% UNDER-DELIVERY (WO_05).** The maximalist overlay landed only ~10-20% — motion CODED but shallow, count-ups STUCK AT 0 (`useInView amount:0.6` too high → `0.35`). Root cause = **skill-invocation drift** (the builder never loaded the design/motion skills). Fix: a fresh builder, skills FORCED (gate + STEP-0 preamble), + Joseph's 8 concrete edits (concrete = checkable = no room to under-deliver).
  - **⭐ DISCIPLINE-ONLY GATES GET SKIMMED.** Fix is mechanical: a tool-derived ledger per gate + a hard DoD + HALT-on-gap.
  - **⭐ TOOLING-POLISH AS BUILD-AVOIDANCE.** Hardening the skill spine hit diminishing returns; the move is USE the skills + get to outreach, not theorize more.
- **NEW THIS ARC (WE16):**
  - **⭐⭐ READ THE ACTUAL ERROR BEFORE THEORIZING A CAUSE (the n8n quota mis-diagnosis).** Joseph's form failed; I first chased a CREDENTIAL issue + floated a "43k executions" hypothesis. Joseph corrected ("we only ran that shit for a little bit") and the execution log showed the truth: **"Execution limit reached"** — the 2,500/mo cap, burned by a **1-min Meta poller** (2,538 execs, all `status:error`). **Lessons: (1) read the execution/error log FIRST, don't hypothesize a root cause before you've looked; (2) when Joseph contradicts a quantitative claim of mine, believe him + re-derive from the data, don't defend the estimate; (3) a schedule/poll trigger on a metered plan is a silent quota bomb → webhooks only.**
  - **⭐⭐ A HANDOFF CODE-CLAIM CAN DRIFT — VERIFY AGAINST THE CODE.** The "`/api/lead` is a `{ok:true}` no-op" claim rode ~2 hops of handoffs while `route.ts` had actually been WIRED by WO_04. **Lesson: re-verify a handoff's CODE claims against the file, not the prior handoff's word** (the "a fact is a lead" law, applied to code). The builder handoff (`HANDOFF_website-engineer_firm-site`) already corrected this; it's fixed here too.
  - **⭐ MONEY-PAGE WO SCOPE-CREEP.** My first WO_10 draft over-scoped: 2 tasks + a $10–15k anchor ON the pricing tab + a subscription bridge IN the guide. Joseph corrected → ONE page (the tab), guide STAYS pure education (only $75→$100/page), matrix flag → active scan. **Lesson: on a money-page/offer WO, default to the MINIMUM surface Joseph named; don't bundle "while we're in there" — the offer surface is where over-exposure reopens negotiation.**
  - **⭐ A PARTIAL MATRIX IS A SALES LIABILITY.** SO shipped 9 random service×city combos earlier; an LLM cited them back to Joseph ("are we supposed to have these?"). A partial matrix invites "why these and not the rest?" on a sales call. **Lesson: the service×city matrix is ALL-OR-NOTHING — a complete per-metro set or none; never a convenient slice.**

## 6. Tooling gotchas
- **(WE16) BRIGADE-MODE IS GONE** — deleted this arc. For multi-agent orchestration use the **native `Workflow` tool** (deterministic fan-out/pipeline; the `Workflow`/ultracode path dominates a hand-rolled brigade). Only call it when Joseph opts into multi-agent scale.
- **(WE16) Vercel custom-domain alias is PER-DEPLOYMENT.** `npx vercel alias set <deployment-url> kingmakerseo.com` (+ `www.kingmakerseo.com`) after EACH `--prod` deploy, OR convert to a permanent project domain in the dashboard so it auto-follows prod. The firm site's `SITE_URL`/canonicals still need flipping → kingmakerseo.com (folded into the WO_10 deploy). Email/MX records are UNTOUCHED — don't change DNS beyond the A/CNAME for the apex+www.
- **(WE16) n8n:** WEBHOOKS ONLY (no polling). Firm-site intake = `/webhook/km-firm-lead` (workflow `CJnIFNbSCbJmt996`); Meta intake = Funnel v2 `/webhook/fb-leadgen` (`Z86sufKPCE219sKP`, form-agnostic). Quota 2,500/mo Starter, resets the 1st. The n8n MCP (`mcp__n8n__*`) is wired — read the SDK reference + best-practices before editing a workflow. `mcp__n8n__search_executions` shows the real error (that's how the poller burn was found).
- **(WE15) 🔴 THE SKILL/GATE SPINE (`~/.claude/`, user-global, NOT in cg-main git):**
  - **Hooks** (in `settings.json`, activate on restart): `PreToolUse`→`skills-gate.mjs` (v2: reads the invocation log, motion-aware, fail-open + `SKILLS_GATE_OFF` kill-switch) · `PostToolUse:Skill`→`skill-invoked-logger.mjs` · `Stop`→`verify-gate.mjs` (the flagship `web/` only) · `SessionStart`→`session-start.mjs`. Self-tests: `skills-gate.selftest.mjs` 13/13, `verify-gate.selftest.mjs`.
  - **The canary:** after invoking skills, `cat ~/.claude/.skills-invoked/<session>.jsonl` — if empty, the logger isn't firing → `SKILLS_GATE_OFF=1` + repair.
  - **`kmwe`** runs `preflight.mjs` (validates the above + invokes the arsenal) — fire it at website-engineer startup.
- **(WE15) Component libraries:** Magic MCP (21st.dev — `mcp__magic__*`) is WIRED — the only one. Use it to scaffold net-new atoms, then re-skin 100%.
- **(WE15) `king-maker-site/` is NOT its own git repo** (untracked `??` in cg-main; on-disk = truth). Deploys via `npx vercel@latest deploy --prod --yes` from the dir. **Playwright live-URL probe:** `NODE_PATH="<project with @playwright/test>/node_modules" node script.cjs` (king-maker-site has it).
- **(WE13 carried) The SO gate suite** (`npm run …` from `summit-oak-roofing/`, server `:3210`): typecheck · build · security-audit (→ `security-receipt.json`; `git checkout --` after) · doorway-check · reachability-check · Playwright desktop/mobile + heading-legibility. The flagship `verify.mjs` is for `web/`, NOT SO/firm.
- **(WE13 carried) The blackboard** — ack ONLY handled mail. **Higgsfield** (`mcp__f19ec897-*`): brand/logo gens; poll `show_generations`. *(WE16: the IG logo PNGs were rendered from `components/Crest.tsx` (the crest SVG) → PNG, not Higgsfield — the crest monogram is the brand mark.)*

## 7. Deploy + verify (fenced)
```bash
# FIRM SITE (the active project — UNCOMMITTED, deploys via Vercel CLI). Live on kingmakerseo.com.
cd "C:/Users/josep/Claude Gravity/king-maker-site"
npm run build && npx --yes vercel@latest deploy --prod --yes        # → a fresh deployment URL
npx --yes vercel@latest alias set <deployment-url> kingmakerseo.com  # RE-ALIAS apex …
npx --yes vercel@latest alias set <deployment-url> www.kingmakerseo.com  # … + www after EACH deploy
# Gates: tsc · next build · Playwright desktop + MOBILE-390px (axe 0 serious + AA contrast red/blue-on-white) \
#   · MOBILE readability · grep (no roofing terms in neutral sections) · LIVE-MOTION-FIRES (count-ups != 0) \
#   · 0 orphans · byte-check the canonical host (kingmakerseo.com once SITE_URL is flipped). \
#   · ⚠️ AI-legibility is NOT a firm-site gate (WE15). · /api/lead end-to-end blocked till the n8n July-1 reset.
# 🛑 MONEY-PAGE / OFFER surface (e.g. WO_10 /pricing): build LOCAL → Joseph eyeball → confirm → THEN deploy. No auto-deploy.

# SUMMIT & OAK (PAUSED — git-tracked, main = source of truth @ d8bd051, 146 URLs)
cd "C:/Users/josep/Claude Gravity/summit-oak-roofing"
npm run build && npx --yes vercel@latest deploy --prod --yes
# Gates (server :3210): typecheck · build · security-audit · doorway-check · reachability-check · playwright desktop+mobile \
#   · heading-legibility (AI-legibility IS a gate here) · the WE crawl probes (.so-seo-verify*.cjs) re-run against live.
git checkout -- security-receipt.json
```

## 8. ⭐ Taste & calibration ledger — CARRIED VERBATIM + attributed; **NEW** at end
- *(carried WE7-WE12):* editorial ≠ converting · the ONLY CTA pair (filled+arrow / outlined call pill) · "full, not overstuffed" · no walls of text · "triple it" → ~2× (state the call, his eyeball decides) · **NO inline screenshots** (disk + background vision verdicts; Joseph eyeballs URLs) · annotations are symptoms not specs (fix to the standard, extrapolate on 2×) · diagnose the ATOM, concede with numbers, NEVER defend · **moderate emoji, NO exclamation points, ✅/❌ status** · ultrathink keyword (routine) · **verify PIXELS + deployed CONTENT before "done"** · the Fulcrum/Balance Principle · Accent-Color Discipline · **the user's eyeball OVERRIDES a vision-agent SHIP** · he likes to DECIDE between options · "forget everything else" = converged on ONE concept · concede a real catch / refute a myth — both with evidence · **don't over-caveat / don't re-litigate a LOCKED doctrine** · he wants the HONEST expert take INCLUDING pushback · GTM claims defensible NOT optimistic (promise FLOOR, project CEILING) · relationship is strong — don't be gun-shy.
- *(carried WE13):* **NEVER "only 19%" / NEVER pivot a site-question to off-page** · **concede a real catch WITH GROUND TRUTH, don't defend** · **Own-the-X frame** (own the COLOR, the STANDARD) · the **"beater"/concede-to-win** · he runs **cross-model audit loops** · the **RELEVANCY framing** · the **dead-simple side-by-side demo** (label modeled "illustrative") · **brand iterates HARD then converges decisively**.
- *(carried WE14):* **⭐⭐ BLUE/WHITE + MAX READABILITY is the firm-site gate** ("this shit needs to be super fucking readable"; 50-60yo on mobile) · **⭐ HYPER-AGENCY + "give them flair"** (over-the-top polished agency cards; not cluttered) · **⭐ RED = the damage** (bad stats red, blue = brand) · **⭐ INDUSTRY-NEUTRAL** · **he iterates the home SECTION-BY-SECTION via inline mockups** · "verify with any AI" goes in his VIDEOS not the site · **driving goal = finish the firm site → content → link-in-bio** · **he hands the builder DROP-IN PROMPTS** for quick single edits.
- *(carried WE15):*
  - **⭐⭐ COMPONENT-FIRST taste shift.** "I don't care HOW we get there — legible + polished + serves the client + fast." Standard agency components are FINE; the site is a useful client product, not his manifesto. Stop handcrafting what exists. (Bespoke still where no component fits — the brand motion.)
  - **⭐⭐ THE BUILD-STOP / build-addiction (he named it, `feedback_build_stop_outreach`):** building stops · weekend = content + posting schedule · then PURE OUTREACH until he lands a CLIENT. **You are the ENFORCER** — name build-avoidance + redirect to the phone (only exception = a real blocker stopping outreach). Tooling-polish itself can be build-avoidance.
  - **⭐ VERY ANIMATED, not timid.** Lean INTO motion; "not overwhelming" is the only ceiling.
  - **⭐ HARDEN EVERYTHING WE MAKE** — BUT **don't invent unnecessary changes** if a skill is already good ("if it's good to go, it's good to go").
  - **⭐ RAPID MODE is his default fast gear** (triggers "caveman/TLDR/fast"); he WILL forget it's on → the persistent marker + self-suspend + auto-escalate cover it.
  - **⭐ "are you positive?" = he double-checks claims** — answer with EVIDENCE, not assertion.
- **NEW (WE16):**
  - **⭐⭐ PRICING/OFFER IS PRE-MARKET + FLUID** (`feedback_premarket_ideas_fluid`). Joseph: "we're probably gonna change this offer; don't over-engineer; it's a good starting point, might stick." Record latest-wins; **don't title it LOCKED, don't make him adjudicate idea-churn, don't over-flag** offer evolution. (But the offer's STRUCTURAL rails — no à-la-carte / no exposed price / no buyout / kill-switch framing — are firm until he says otherwise.)
  - **⭐⭐ THE TWO VOICES (calibration).** Education = **neutral, detached** ("here are the facts, the limits, this may well be enough for you" — zero angst/hype/attacking). Sales = the **Roark reframe-as-mediocrity**. He flagged the buyer's-guide's first adversarial draft ONCE → neutral rewrite. Never cross them. Pair with simplify-not-oversimplify (plain words, keep the substance).
  - **⭐ WIRE IT YOURSELF when it's safe + the info exists.** "Is there any way you could just wire the site to KingMaker SEO yourself on Vercel?" — he wants proactive infra EXECUTION (alias, deploy, config), not just advice, when it's reversible + low-risk. (I did the apex/www alias.) Still: the MONEY page deploys only on his confirm.
  - **⭐ BUILD-STOP, NUANCED (this arc).** He re-opened building ("it's time to build some more bullshit") — for the PRICING PAGE + the link-in-bio finish, which DIRECTLY serve landing a client. So the enforcement test is judgment, not a blanket no: **does this build move him toward a client (money page, link-in-bio) or away from the phone (polish/redesign for its own sake)?** Redirect only the latter.
  - **⭐ "LINK-IN-BIO-READY" IS MORE THAN THE HOME.** He realized mid-arc that `/work` (gappy), `/system` ("completely fucked"), `/playbook` (maybe-cut) also gate the link-in-bio. So the readiness bar = home + buyer's guide + work + system (+ playbook?). Don't call the site "ready" until those clear (or playbook is cut).
  - **⭐ HE RUNS HIS OWN BUILDERS NOW + uses km-handoff to brief them** ("I used King maker handoff to draft a new builder because I want the builder to be consistent"). When he's running a parallel builder, your job is to NOT step on it (don't draft its WO) + to QA-assist when asked.

## 9. Coordination
| Agent | Contract |
|---|---|
| **builder** (Joseph-run: ultracode / fresh-cold / warm) | Execute WOs. WE = architect/reviewer: QA live, never build inline. The launch-builder prompt BAKES the skill-invocation + mode-reset preamble (anti-drift). Joseph runs his own builders for some lanes (e.g. the /work+/system+/playbook rework) — don't draft their WOs. |
| **n8n-claude-architect-1** | Lead funnel. Poller killed + hardened; webhook intake live; **July-1 = redeploy firm site + fire 1 test lead → confirm Telegram** (joint). Open dep: meta-ads to confirm the old form `985131077477390` page subscription. |
| **meta-ads-specialist** | Confirm the old Free-Until-Rank form `985131077477390` isn't running ads on an UNsubscribed page (would drop leads). Joseph: the Meta leads are "shit leads" — the fix is better creative/videos, not chasing them. |
| **cold-outreach-specialist-6 (cos-10)** | Delivered the 1,017-roofer BROKEN_ROOFER_WEBSITE_REPORT (the proof spine). Field-research → content pipeline live. **Joseph's #1 channel now = outreach** — defer to cos when he pivots there. |
| **cyber-security-specialist-1 (CSS)** | Owns the security layers (`app/api/lead`, `lib/server/*`, `next.config.ts`, `scripts/security-audit.mjs`) on BOTH sites — don't clobber. |
| **vault-agent** | "ingest inbox" pending (the pricing brief + this arc's notes). |
| `human` (Joseph) | router · deploy gate · **final eyeball** · offer numbers his · **outreach is on him** · runs his own builders. |

## 10. Knowledge artifacts & file map (READ-ORDER · authority · staleness)
**Handoff home note (WE16):** going forward the website-engineer handoff lives at **cg-main root** (`C:/Users/josep/Claude Gravity/`), co-located with the work. The WE5→WE16 lineage history lives in `C:/Users/josep/OneDrive/Documents/Claude/Projects/American Master Works Redaux/AGENT-WEBSITE-ENGINEER-{5..16}-*.md` (read WE16 for the deeper carried corpus). A deliberate, recorded move — not drift.

**Firm-site lane (the active project) — read FIRST (all in cg-main root unless noted):**
1. **This file.** Then ⭐ **`king-maker-site/KING_MAKER_TEMPLATE_MANIFEST.md`** (the lossless home template — the propagation tool). Then the offer/pricing source: **`KM_SITE_WORKORDER_10.md`** (the pricing-tab WO, NOT built) + `KM_SITE_PRICING_BRIEF.md` + `KM_SITE_PRICING_BRIEF_REFINEMENTS.md` (+ vault `km-offer-497-subscription`). Then **`KM_SITE_WORKORDER_08.md`** (propagation) + **`KM_SITE_WORKORDER_09.md`** (the buyer's guide). Then `HANDOFF_website-engineer_firm-site_2026-06-27.md` (Joseph's BUILDER brief for /work+/system+/playbook — the two-voices + lead-wired notes). Older: the WE14/15 docs `KM_SITE_WORKORDER_01-07.md` + `KM_SITE_MAXIMALIST_BRIEF.md`.
2. **The firm-site CODEBASE** (`king-maker-site/`, UNCOMMITTED): `components/motion.tsx` (primitives) · `app/globals.css` (tokens + `.km-*` recipes) · `components/home/*.tsx` (the shipped maximalist sections) · `components/ui.tsx` · `components/Crest.tsx` (the brand-mark SVG → the IG logo PNGs) · `lib/claims.ts` (`CRM_DEMO`/`FINDABILITY_GAPS`/`MatrixGrid` — §4 matrix scan target) · `lib/guide-content/*.ts` (the buyer's-guide content, incl. `what-a-website-should-cost.ts`) · `app/api/lead/route.ts` (WIRED — not a no-op) · `llms.txt` (matrix-scan target). The new `/pricing` route lands here when WO_10 builds.
3. **The SKILL/GATE SPINE** (`~/.claude/`): `skills/{rapid-mode,edit-mode,create-template,launch-builder,kmwe,km-handoff,verify-before-claim}/SKILL.md` + `hooks/{skills-gate,skill-invoked-logger,verify-gate}.mjs`. brigade-mode is DELETED. Memory `reference_skills_gate`.
4. **The PROOF spine:** `king_maker_outbound/BROKEN_ROOFER_WEBSITE_REPORT.md` (the 1,017-site audit) · `KM_GHL_VS_ENTERPRISE_CLAIM_CARD.md` · the GHL audit `king_maker_outbound/.ghl-audit/REPORT.md`.
**Summit & Oak lane (PAUSED — only if un-paused):** `summit-oak-roofing/` (TRACKED, `d8bd051`, 146 URLs) · `SUMMIT_OAK_WORKORDER_22.md` (the combo removal) + WO_19/20/21 · `.so-seo-verify*.cjs`.
**Memory (auto-loads):** ⭐ `project_km_firm_site_rebuild` (the active-project state) · `feedback_component_first_builds` · `feedback_overstimulation_threshold` · `feedback_build_stop_outreach` · `feedback_premarket_ideas_fluid` · `reference_skills_gate` · `project_summit_oak_roofing` · `feedback_site_wins_pack` · `feedback_organic_over_mappack` · `project_content_first_gtm` · `feedback_copy_voice_howard_roark` · `feedback_ai_legibility_qa` (⚠️ client/SO only, NOT firm) · `feedback_built_not_reachable` · `feedback_dual_intent_forking` · `reference_seo_2026_rubric`. ⚠️ `project_km_brand_reposition` = OLD gold/black (SUPERSEDED). **NEW this arc (in vault inbox, pending ingest):** `km-offer-497-subscription` · `km-content-voice-register` · `km-meta-poller-quota-burn`.

---
*— WE16, 2026-06-27. THE ARC: shipped WO_08 (propagate) + WO_09 (the 11-cat/31-sub buyer's guide, rewritten) — both live + approved; WO_22 stripped SO's 9 incoherent combos (matrix = all-or-nothing, 146 URLs); killed the n8n quota-burning poller + confirmed /api/lead is WIRED (the no-op claim had drifted); wired kingmakerseo.com → the firm site; rendered the blue/white logo; converged the offer to $497/mo + drafted WO_10 (the pricing tab, ONE page, corrected scope — NOT built, WARM prompt out); deleted brigade-mode + hardened the speed skills. 🟢 Firm-site home + buyer's guide are SHIPPED + on the apex domain. OPEN #1 = land a CLIENT; building is sanctioned only where it unblocks outreach (the money page + the link-in-bio finish — /work + /system + /playbook, Joseph's own builder). Reread 3×, re-verify state, check the bus. **Verify PIXELS + deployed CONTENT; the user's eyeball overrides; NEVER "19%"/off-page-pivot; readability + industry-neutral are the firm-site gates, AI-legibility is NOT; two voices never crossed; matrix all-or-nothing; webhooks-only on n8n; money page deploys only on Joseph's confirm; claim ≠ done (tool-derived ledgers).** Compound this corpus — carry §4/§5/§8 verbatim, add yours.*
