• Let me read the brief file first.The brief says: read exactly this one file and nothing else: `C:/Users/josep/Claude Gravity/wo/WO_SEO_LANE_A7_GLOSSARY_POUR.md`. Read-only, produce markdown analysis only.Now I produce the adversarial audit. Read-only, markdown analysis only, output to stdout. The brief says "Read exactly this one file and nothing else" — I've read the brief and the WO. I should not go read the actual repo files (the brief says the artifact under audit is the WO file, and I have "NO session context"). So I analyze the WO text alone.

  Let me work through the audit targets carefully.

  **Target 1: Coverage holes**

  What does the WO fail to specify that a builder writing 31 encyclopedia entries would need?

  - Duplicate-concept collisions with existing 20: The WO lists the existing 20 slugs but doesn't map which new terms are semantically adjacent to existing entries. Concrete collisions:
    - `sub-fascia` vs existing `fascia` — the WO itself says /fascia "already part-ranks" for sub-fascia. If the fascia entry's body already discusses sub-fascia, the new page risks >40% 5-gram overlap AND keyword cannibalization. WO never tells the builder to check the fascia entry's content for sub-fascia discussion or to differentiate.
    - `step-flashing`, `counter-flashing`, `kick-out-flashing` vs existing `flashing` — three new terms that are subtypes of an existing entry. If `flashing` body covers step/counter flashing (very likely in 250-350 words on flashing), overlap + cannibalization risk. WO is silent.
    - `ridge-cap-shingles` vs `ridge-vent` — adjacent concepts, often discussed together.
    - `soffit-vent` vs `soffit`, `gable-vent` vs... soffit-vent pairs with soffit; ridge-vent exists. Ventilation cluster: ridge-vent (existing), soffit-vent, gable-vent, turbine-vent, attic-baffles — 4 new terms all in the attic-ventilation semantic cluster with the existing ridge-vent. High internal-collision risk among the NEW terms themselves too: soffit-vent/gable-vent/turbine-vent/attic-baffles will all say "intake/exhaust ventilation, cool the attic, prevent moisture" — pairwise similarity among the four new vent entries is a real doorway-check risk, and the WO gives no differentiation guidance.
    - `roof-overlay` (existing) vs `re-decking` and `tear-off` (existing) vs `re-decking` — re-decking is discussed during tear-off; existing roof-decking entry too. Three-way adjacency: roof-decking, tear-off, re-decking.
    - `actual-cash-value` + `replacement-cost-value` (existing) vs `recoverable-depreciation` — recoverable depreciation is literally the difference between RCV and ACV. The existing two entries almost certainly define it inline. Near-duplicate-in-disguise.
    - `roof-deductible` (existing) vs the insurance terms — fine.
    - `class-4-shingles` (existing) vs `hail-bruising` — class 4 is impact resistance; hail bruising is the damage. Adjacent.
    - `granules` (existing) vs `shingle-blistering`/`shingle-curling` — granule loss discussed in granules.
    - `drip-edge` vs `gutter-apron` — gutter apron is a drip-edge variant that directs water into the gutter. Duplicate-in-disguise risk with drip-edge.
    - `eave` vs `soffit`/`drip-edge` — eave is the overhang; soffit is its underside. Adjacent.
    - `rake-edge` vs `drip-edge` — rake is where drip edge also goes.
    - `architectural-shingles` vs `three-tab-shingles` — these two new entries will be defined against each other; pairwise overlap risk between the two new entries themselves ("the most common residential roofing material in the US" could appear in both).

  - Slug/term naming mismatches: WO gives slugs but not display `term` strings. Is the term "Sub-Fascia" or "Sub-Fascia Board"? "Three-Tab Shingles" vs "3-Tab Shingles"? "Xactimate" (trademark capitalization)? "Class 4 Shingles" pattern shows existing uses "class-4-shingles" slug; new `algae-resistant-shingles`... The builder must guess title-case and hyphenation of the human-facing `term`. The oracle greps for the `term` string in an h1 — but the WO never pins the exact `term` strings, so the oracle is self-consistent whatever the builder picks; mismatch risk is between slug, term, and what users search. E.g. "roof-warranty" — is the term "Roof Warranty"? Fine. "xactimate" → "Xactimate" (trademark, must be capitalized; using it in a glossary is nominative use but the WO gives zero trademark guidance — e.g. don't imply endorsement, mark as trademark of Xactware/Verisk).

  - Ordering within the array: The WO says "appended" and "sub-fascia first in priority" — "first in priority" is ambiguous: first of the 31 appended (position 21) or does priority imply something else (like index ordering)? Does the index page render in array order or sorted? If array order, the appended block leaves the index unsorted (20 old, then 31 new in listed order) — is that acceptable? WO never says whether the index is alphabetical; if the index renders array order, an append-only pour yields a non-alphabetical index — maybe fine, maybe a UX inconsistency with the existing 20 (which appear to be in a curated order, not alphabetical: roofing-square, flashing, underlayment... not alphabetical). WO silent.

  - Index-page layout assumptions: WO says index lists all 51 — but is the index hardcoded or data-driven from GLOSSARY_TERMS? §1 says routes render from data but doesn't explicitly confirm the index page composes from the array. NEVER-TOUCH says `app/**` is untouchable — if the index page hardcodes the 20 (or hardcodes a count, or a "20 terms" heading), the builder is stuck: can't edit app/**, and the success criterion "listed on the glossary index" fails. The WO assumes data-driven index but only explicitly says the [term] route renders from data ("the [term] route renders from data — no component edits"). Index page composition is unverified by the WO itself. Also: does the index paginate? At 51 items maybe layout assumptions break (grid columns, "showing 20 terms" text). Builder can't fix app/** — trapped if so.

  - Also missing: metadata. Do glossary pages generate per-term metadata (title/description) from the entry? If generateMetadata reads the entry, fine; the WO doesn't mention title/meta description requirements at all — the `GlossaryTerm` type has no meta fields per §1 ({slug, term, shortAnswer, body, relatedMoneySlug}), so presumably metadata is derived. Fine but unverified.

  - Structured data: does the glossary emit FAQPage/DefinedTerm schema? If it derives from entries, fine. Not mentioned.

  - Word-count gate: 60-100 for shortAnswer, 250-350 for body. "5-7 paragraphs" within 250-350 words means ~50 words/paragraph — consistent-ish. But "body: ~250-350 words" has a tilde (approximate) while report format demands word counts per entry as a table — the tilde invites drift; is 240 acceptable? 360? No hard bound.

  - The WO doesn't specify how to count words for the table (whitespace split? hyphenated count as one?). Minor.

  - No guidance on `shortAnswer` vs `body` relationship: is shortAnswer repeated verbatim on the page? If the page renders shortAnswer then body, and both say similar things, does the 5-gram check treat them as one page? Not a WO hole per se.

  - The report path: `wo/BUILD_REPORT_SEO_LANE_A7.md` "in the worktree" — but §3 ONLY-THESE-FILES lists only glossary.ts and maybe the sitemap file. Is writing the build report into the worktree's `wo/` directory authorized? NEVER-TOUCH doesn't list it, and §8 commands it, so implicit — but §3 says ONLY these files. Minor contradiction: the report file is a file creation not covered by §3. Also committing the report on the branch ("Commit on the branch") — is the report committed too? "Commit on the branch. Do not push." — ambiguous whether the report is included in the commit. If committed, does the repo tolerate wo/ files on the branch? Probably fine.

  - The I1 verification: "git diff shows only additions inside the array + nothing above the first new entry" — but the I3 exception allows editing the gate file (scripts/** or wherever doorway-check lives — wait, NEVER-TOUCH says `scripts/**` — is doorway-check.mjs in scripts/? Likely scripts/doorway-check.mjs. The I3 exception explicitly overrides never-touch-gates. But §4 NEVER-TOUCH lists `scripts/**` and `tests/**` without noting the I3 exception — §9 mentions the exception. So a literalist builder reading §4 might refuse, or reading §9 comply. Minor internal inconsistency: §4 doesn't cross-reference the exception.)

  - Prettier/format: byte-identical existing entries — if the repo has a formatter and the builder's edit triggers format-on-save across the file, I1 breaks. WO doesn't say how to protect against formatter churn (e.g. "do not run prettier --write on the file" or "match existing formatting exactly"). Real failure mode on Windows with editor tooling.

  **Target 2: underspecification sweep** — places the builder must guess:

  1. Display `term` strings (capitalization, hyphenation, trademark casing for Xactimate).
  2. Array insertion point: "appended" = after roof-deductible, in listed order? Presumably, but "sub-fascia first in priority" muddies.
  3. Whether index auto-renders (WO asserts for [term] route, not index).
  4. Which relatedMoneySlug per term — deliberately left to builder ("genuinely-nearest") but no examples; some terms are ambiguous (storm-chaser → roof-repair? roof-inspection? neither fits a definition of a scam pattern; gutter-apron → /services/gutters obviously; xactimate → which money page? roof-inspection? An insurance software term linking to a service page is a stretch — the constraint forces a link even where none is natural, which is an internal-linking quality risk).
     Actually — is relatedMoneySlug allowed to be... the type says `relatedMoneySlug` singular and mandatory. Every entry must have one. For `xactimate` and `storm-chaser` and `recoverable-depreciation`, any money-page link is awkward. The WO could have allowed null. Instead forces relevance-stretched internal links — mild SEO quality risk (irrelevant internal links), but survivable.
  5. The exact 40% threshold mechanics: how does doorway-check tokenize? The builder must read the script. Fine.
  6. "Match the register... read at least 5" — fine, but "register exactly" is unverifiable; the WO gives exemplars, good.
  7. Word count bounds: "~250-350" tilde vs "60-100" hard. Guess whether 351 fails.
  8. What "climate references stay generic" allows — "hot, humid summers" given as OK; is "hail season" OK? Guess.
  9. Numbers: "defensible range" — who defends? Builder must guess ranges for hail size ("quarter-sized" is the standard 1-inch threshold — is stating that an invented statistic? The NOAA 1" severe threshold is a fact; the WO's rule might scare the builder into vague mush or into a specific claim it then can't source).
  10. Whether to update any count text elsewhere (e.g. if index says "20 terms" — can't touch app/**; trapped).
  11. Sitemap: "READ lib/sitemap-registry.ts / app/sitemap.ts FIRST and report" — but app/** is NEVER-TOUCH. Reading is fine, editing only "the one file the existing 20 use for sitemap membership" — if that file is app/sitemap.ts, there's a direct §3 vs §4 conflict: §3 authorizes "the one file" but §4 bans app/**. If sitemap membership derives from app/sitemap.ts reading GLOSSARY_TERMS, no edit needed; but if the fix belongs in app/sitemap.ts, the builder is trapped by §4. The WO should have named the exact file conditionally editable.
  12. Gate list extension: "extend your LOCAL copy of the gate's GLOSSARY array to all 51" — with what? The new slugs? The full list? Presumably add the 31 slugs. But "record the exact edit" — format unspecified. Also: does the edit get committed on the branch? If committed, it collides with Lane M's merge (the WO says orchestrator reconciles — OK, acknowledged). If NOT committed, then CI/any rerun of gates on the branch by the verifier uses the un-extended gate and the 31 new pages are unchecked — the invariant I3 as verified is only as good as a local uncommitted hack. WO says "record the exact edit in the report" — doesn't say commit or don't. Builder guesses.
  13. Server port 3210 for "the last three" gates — but `npm run doorway-check` — does it hit the server or the filesystem? The WO says server needed for last three; fine. But `npx next start -p 3210` after `npm run build` — build output... fine.
  14. The oracle greps rendered text for term in h1 — but oracle is run by whom? The builder reports count-proof. OK.

  **Target 3: term list critique**

  Duplicates-in-disguise vs existing 20:
  - `recoverable-depreciation` vs `actual-cash-value` + `replacement-cost-value`: recoverable depreciation IS (RCV − ACV). Any 250-word body on recoverable depreciation must restate ACV and RCV definitions → 5-gram overlap with two existing entries is structurally likely. This is the single highest doorway-check-failure risk in the list. Not a reason to drop the term (high search value in insurance-claim queries), but the WO should have flagged it explicitly like it did storm-chaser. It flagged storm-chaser but not this one — inconsistent risk flagging.
  - `sub-fascia` vs `fascia`: WO says fascia "already part-ranks" for sub-fascia — meaning Google already associates the fascia page with the query. Creating a separate sub-fascia page can cannibalize: the #26/#50 rankings belong to /fascia; a new /sub-fascia page may split signals and neither ranks. The SEO-sound play is often to expand the fascia entry... but existing entries must stay byte-identical (I1), so expansion is banned. The WO never addresses the cannibalization tradeoff; it cites the part-ranking as justification without noting the risk. MED. Also 5-gram overlap: sub-fascia definition inevitably shares phrases with fascia definition ("board that runs along the lower edge of the roof... behind the gutters"). Watch item.
  - `drip-edge` vs `gutter-apron`: gutter apron is drip edge with a longer leg; overlap risk.
  - `step-flashing` / `counter-flashing` / `kick-out-flashing` vs `flashing`: the existing flashing entry exemplar almost certainly names step/counter flashing as types. If its body has a "types" passage, three new entries overlap. Also the three new flashing entries share phrases among themselves ("where the roof meets a vertical wall" appears in step and kick-out both).
  - Vent cluster `soffit-vent`/`gable-vent`/`turbine-vent`/`attic-baffles` vs `ridge-vent` and each other: five pages about attic ventilation → pairwise similarity risk inside the pour itself. The gate checks pairwise across all glossary pages, so the builder will hit this; the WO gives no differentiation strategy (e.g. soffit=intake, ridge=exhaust, gable=exhaust-through-wall, turbine=mechanical-assist exhaust, baffles=airflow-channel-hardware — that's the differentiation spine and the WO could have included it in one line each).
  - `roof-sagging` vs `roof-decking`: sagging is often deck failure; moderate.
  - `shingle-blistering`/`shingle-curling`/`nail-pops`: three defect pages sharing "thermal expansion/moisture/aging" vocabulary; pairwise risk.
  - `hail-bruising` vs blog `spotting-a-storm-chaser`? No — vs article corpus: articles include roof-insurance-supplement and acv-vs-rcv article — wait, the brief says articles include `acv-vs-rcv-roof-insurance-claims`. So there's an ARTICLE about ACV vs RCV AND two glossary entries AND now recoverable-depreciation. The acv-vs-rcv article surely explains recoverable depreciation — a glossary entry restating it may hit >40% 5-gram against that article. The WO never mentions the acv-vs-rcv article. The I3 invariant covers "against the article/blog corpus" so the gate would catch it, but the WO's per-term risk flagging missed the biggest one: it flagged storm-chaser (blog exists) but not recoverable-depreciation (article + 2 glossary entries exist) nor xactimate (the roof-insurance-supplement article surely mentions Xactimate — supplements are written in Xactimate!). That's a real coverage hole in the WO's risk guidance.
  - `storm-chaser`: flagged, good. But also: is a glossary definition of storm-chaser a term a homeowner searches? "storm chaser roofing" — yes, homeowners do search it after being approached. OK.
  - `xactimate`: trademark — Xactimate is Verisk's proprietary estimating software. A glossary page defining it is nominative fair use, low legal risk IF neutral, but the WO's only guidance is "factual and neutral" for insurance terms — it does say that. But it doesn't say: don't reproduce their UI/claims, include trademark attribution? Not legally required. The bigger risk: a roofing contractor defining the insurer's tool can drift into adversarial framing ("what adjusters don't want you to know") — WO forbids that ("never advise disputing an adjuster"). Adequately covered, though "never even mention deductible waiving" is smart.
    Search value: "xactimate" is searched by homeowners who got an estimate — decent. But homeowner-facing? Mostly contractors search it. Borderline but defensible.
  - `roof-warranty` — high search, fine. Legal risk: warranty terms vary; "commonly 25 to 50 years" style guidance given. Must not imply the contractor's own warranty terms — the site presumably has warranty pages; a glossary definition conflicting with the company's actual warranty page is a consistency risk the WO doesn't address (no instruction to read the warranty-related service content). MED-LOW.
  - `re-decking` vs `roof-decking` + `tear-off`: re-decking happens during tear-off; overlap risk with two existing entries.
  - Terms a homeowner would never search:
    - `shingle-exposure` — "exposure" (the visible portion of a shingle course) is contractor jargon; homeowners don't search "shingle exposure." Low search volume, but harmless; glossary fodder for AI citation. Not a defect, weak value.
    - `attic-baffles` — homeowners search "attic baffles" occasionally; fine.
    - `turbine-vent` — searched. fine.
    - `xactimate` — mostly contractors; but homeowners who receive an estimate do google "what is xactimate". Defensible.
    - `sub-fascia` — 1,000/mo claimed for "sub fascia" — but that ranking claim was for the /fascia page; the query volume likely includes contractor searches. Fine.
    - `chimney-cricket` — homeowners search "chimney cricket" after hearing it; fine.
    - `nail-pops` — searched. fine.
    - `kick-out-flashing` — searched by homeowners told they need it. fine.
    - Overall the list is strong; shingle-exposure and maybe xactimate are the weakest homeowner-fit.
  - Missing from list considerations is out of scope (list is final: "do not add, drop, or substitute") — the audit target asks me to evaluate the list, so above stands.

  Also: "hip-roof" and "gable-roof" — pairwise twins; definitions share "two sloping sides..." vocabulary. Risk manageable.

  **Target 4: I3 scoped gate exception**

  Letting the builder edit the gate's hardcoded list locally:
  - Sound in principle because the hardcoded list IS the defect and Lane M is fixing the derived version. The exception is scoped, recorded, and reconciled at merge.
  - The cheat it opens: "extend your LOCAL copy of the gate's GLOSSARY array" requires editing the gate file. A builder that can't get under 40% can, while in the file, nudge the threshold (0.40 → 0.60), change the n-gram size, weaken tokenization, or exclude the new terms from the corpus pairing — and "record the edit" in prose that doesn't mention the threshold change. The exception normalizes touching the gate, which breaks the fleet's own "red gate is fixed in the code, never in the gate" law — the WO even has to carve an exception to its own SIX NEVERS, which is a smell.
  - Failure scenario: builder hits 43% on recoverable-depreciation vs acv-vs-rcv article; rather than rewrite the entry (hard), it edits the gate and reports "extended GLOSSARY list to 51" while the threshold line also changed. Orchestrator reconciling with Lane M will diff the GLOSSARY array maybe, but the report only must record "the exact edit" — self-reported.
  - Minimal safeguards (pick): (a) require `git diff scripts/doorway-check.mjs` (full verbatim diff, not a prose description) pasted into the build report — one line in the WO; (b) pin the invariant numerically: "the only permitted change is adding slug strings to the GLOSSARY array; the 0.40 threshold, n-gram size, and corpus paths must be byte-identical — verify with `git diff -U0 | grep -E '^[+-]' | grep -v slug` returns nothing" or simpler: run the gate BEFORE the edit on the existing 20 and AFTER, and require identical pass/fail on the old set; (c) simplest strong one: the builder does NOT commit the gate edit — report includes the diff; verifier applies Lane M's derived gate and reruns. Actually the cleanest: the WO should have required the gate edit to remain UNCOMMITTED (local-only) so the branch never carries a modified gate; the verifier reruns with Lane M's version. The WO is silent on commit-vs-not for the gate edit — that's the hole.
  - Also: does the gate check pairwise including glossary↔articles? I3 says yes ("and against the article/blog corpus"). If the hardcoded gate's corpus pairing for glossary↔articles is keyed off the same GLOSSARY list, extending it covers it. Fine.
  - Also the exception says "if Lane M's derived version is not yet merged" — how does the builder in an isolated worktree know? It must check scripts/… for a derived implementation. Guessable. Minor: give the builder the check ("if the gate reads GLOSSARY_TERMS from lib/articles/glossary.ts, do nothing").

  **Target 5: oracle weaknesses (§7)**

  Oracle: curl 200 for all 31 AND page's rendered text contains its own `term` string in an h1/heading; index lists all 51; count-proof via grep index HTML for each slug.

  Failure modes that pass the oracle:
  - Pages 200 but body empty: if [term]/page.tsx renders h1 from `term` but body/shortAnswer render from fields the builder left empty or whitespace — does typecheck catch empty strings? No, empty string is a valid string. Builder could fill shortAnswer with 60-100 words but body empty... build succeeds, oracle passes (h1 present), word-count table is self-reported. The oracle never checks body presence/length on the rendered page. Minimal fix: oracle greps each page for its own shortAnswer's first sentence (or a page-level word-count floor, e.g. page text ≥ 300 words).
  - notFound vs 200: if the [term] route uses generateStaticParams from GLOSSARY_TERMS, a slug missing from the array 404s — covered by 200 check. But if the route falls through to a catch-all or the dynamic route renders a soft-404 with 200 status (Next default notFound() returns proper 404, but a custom empty state could 200) — the term-in-h1 check mostly covers it, since a soft-404 wouldn't contain the term... unless the soft-404 page echoes the slug. Edge case.
  - Index lists slugs but links 404: oracle greps index HTML for slug strings — if the index renders slug text but wrong href (e.g. wrong base path `/glossary/` instead of `/resources/glossary/`), the grep passes while every link 404s. reachability-check gate may catch this (that's what it's for) — if reachability crawls from the index. Unknown; the WO relies on it. Oracle itself doesn't curl the index's links. Minimal fix: oracle greps index for `href="/resources/glossary/<slug>"` not bare `<slug>`.
  - Sitemap silently missing: §2 says "present in the sitemap (137 → 168 if auto-composed)" — "if auto-composed" is conditional and §1 says REPORT whether auto-composed. The ORACLE (§7) does not include a sitemap check at all! Success criteria in the brief say "all pages join the sitemap". The WO's target behavior says sitemap 137→168, but §7 oracle = 31 pages 200 + index 51/51 only. So sitemap can silently miss all 31 and the oracle still passes; the only guard is the §8 report's "sitemap composition finding" — a finding, not a gate. If the builder reports "not auto-composed, added coverage" — fine — but if it misreports or the mechanism silently drops them (e.g. sitemap hardcodes count, or builds from a different list), nothing fails. Minimal fix: add to oracle: `curl localhost:3210/sitemap.xml | grep -c glossary` (or grep for the 31 slugs; 168 total). Wait — does next start serve /sitemap.xml? app/sitemap.ts → yes, /sitemap.xml. One line. This is a real oracle hole: HIGH.
    Also 137 → 168: 137+31 = 168. ✓ math checks.
  - Byte-identical existing 20: I1 verified by git diff — fine, that's solid (assuming builder runs it and reports verbatim).
  - The oracle doesn't verify "no city tags" or insurance-neutrality — those are content laws with no automated check; PLUS-JUDGE mode presumably has a judge. The WO has no judge-pass gate listed in §6, but Mode: PLUS-JUDGE implies orchestrator-side judgment. Fine to note as residual.
  - The "grep the index HTML for each slug, print 51/51" — if the index page renders all terms on one page, fine; if the index paginates or lazy-renders, grep fails though feature works — false negative direction, less dangerous.
  - Also: curl against `next start` — if the glossary pages are statically exported (SSG), `next start` serves them; if dynamic with ISR... fine.
  - One more: oracle checks h1 contains term string — but never checks the CANONICAL/noindex. If glossary pages ship with a stale canonical or noindex from some template... beyond WO scope; reachability/spam-410 gates don't check SEO meta either presumably. Not required.
  - The 40% pairwise gate: 51 pages → C(51,2)=1275 glossary pairs plus corpus — gate handles it; not an oracle hole.

  **Target 6: THE ONE THING**

  Candidates:
  1. Pin the oracle to actually prove content + sitemap: extend §7 to (a) grep each page for its own shortAnswer first sentence / word floor, (b) grep index for full hrefs, (c) grep sitemap.xml for all 31 slugs with count-proof 168. This converts the three silent-pass holes into hard checks. 
  2. Add the duplicate-adjacency map (differentiation spines) for the ~8 high-collision terms.
  3. Fix the I3 exception to forbid committing the gate edit + require verbatim diff.

  The one thing: I'd pick the oracle/sitemap+content-proof extension, because the WO's own success criteria (pages render = real content, sitemap membership) are exactly what the oracle fails to prove, and it's a one-paragraph change with zero design judgment needed. The differentiation map is valuable but the gate does catch overlaps (I3 enforced, assuming extended gate run honestly) — i.e. collisions surface as red gates; empty bodies and missing sitemap entries surface as NOTHING. The un-detected failures beat the detected ones.

  Actually wait — one more consideration for THE ONE THING: the §3 vs §4 trap where sitemap membership might require editing app/sitemap.ts which is NEVER-TOUCH. If glossary is NOT auto-composed and the composition point is app/sitemap.ts, the builder is in an impossible contradiction (must add sitemap coverage, cannot touch app/**). "the one file the existing 20 use for sitemap membership" — if the existing 20 get into the sitemap via app/sitemap.ts reading GLOSSARY_TERMS, then auto-composed is true and no edit needed. If NOT auto-composed, the mechanism might be lib/sitemap-registry.ts (editable, not in app/**). The WO says read both first. The contradiction only bites if the needed file is under app/**. lib/sitemap-registry.ts is under lib/, so probably editable. Risk is moderate, not certain. The oracle hole is certain. Keep my pick.

  Now severity assignment and findings list (F1..Fn):

  - F1 (HIGH, §7 oracle): no sitemap membership check → success criterion "all pages join the sitemap" can silently fail. Minimal fix: add curl sitemap.xml grep count-proof to §7.
  - F2 (HIGH, §7 oracle): no content-presence check → 200 + h1 passes with empty/short body. Fix: grep rendered page for the entry's own shortAnswer first sentence (or ≥N words).
  - F3 (HIGH, §5 I3 + §9): gate-edit exception doesn't say whether the edit is committed, and self-reported "exact edit" is the only control → threshold-tamper cheat. Fix: gate edit must remain uncommitted (or committed separately), verbatim `git diff` of the gate file required in report, threshold/n-gram lines pinned byte-identical.
  - F4 (MED, §2 term list): recoverable-depreciation vs existing actual-cash-value/replacement-cost-value entries + acv-vs-rcv-roof-insurance-claims article — structural near-duplicate, highest 5-gram risk, unflagged (storm-chaser got a warning; this didn't). Fix: add the same "read the article first, different bones" instruction.
  - F5 (MED, §2 term list): sub-fascia cannibalization — WO's own evidence says /fascia part-ranks for the query; I1 bans strengthening /fascia; new page may split rankings. Plus overlap risk with fascia entry. Fix: explicitly direct differentiation (sub-fascia = structural board beneath fascia; fascia = finished board) and accept/stated-mitigate cannibalization.
  - F6 (MED, §2): duplicate-adjacency clusters unflagged: flashing→step/counter/kick-out; ridge-vent/soffit/gable/turbine/attic-baffles vent cluster; roof-decking/tear-off/re-decking; drip-edge/gutter-apron; architectural vs three-tab. Pairwise 40% among the new entries themselves is the likely first red gate; builder gets no differentiation spines. Fix: one line per cluster stating the differentiating axis.
  - F7 (MED, §3/§4): ONLY-THESE-FILES vs sitemap fix location ambiguity — if the composition fix lives in app/sitemap.ts, §4 NEVER-TOUCH app/** traps the builder (must-fix vs never-touch). Fix: name the exact conditionally-editable file, or state "if the fix requires app/**, STOP and report."
  - F8 (MED, §2): relatedMoneySlug forced for all 31 — xactimate/storm-chaser/recoverable-depreciation have no natural money page; forced relevance-stretched internal links. Fix: allow null or designate a fallback (roof-inspection) explicitly.
  - F9 (MED, §2): index page composition unverified — WO asserts [term] route is data-driven but never states the index composes from GLOSSARY_TERMS; if index hardcodes 20 (or a "20 terms" string), success criterion fails and app/** is never-touch. Fix: probe it in §1 like the sitemap probe ("READ app/resources/glossary/page.tsx and report"; reading app/** is allowed, only touching is banned — actually §4 says NEVER-TOUCH app/**, reading is fine, and §1 already tells builder to READ app/sitemap.ts).
  - F10 (LOW, §2): display `term` strings unpinned — "Xactimate" trademark casing, "Three-Tab" vs "3-Tab", "Sub-Fascia" vs "Sub-Fascia Board"; oracle self-consistent so it can't catch a bad choice. Fix: include term strings in the list.
  - F11 (LOW, §2): word bounds ambiguous — "~250-350" tilde vs hard 60-100; 5-7 paragraphs inside 250-350 is tight (36-70 words/para, fine actually); report demands word-count table but no counting method. Fix: hard bounds, state the counter.
  - F12 (LOW, §2/§4): array ordering + index order unspecified — "sub-fascia first in priority" ambiguous; if index renders array order, appended block yields non-alphabetical index. Fix: state "append in listed order; index renders array order, that is intended" or otherwise.
  - F13 (LOW, §5 I1): formatter-churn threat to byte-identical — no instruction to disable format-on-save/avoid prettier on the file. Fix: one line.
  - F14 (LOW, §8 vs §3): build report file creation not in ONLY-THESE-FILES; commit scope unclear (is the report committed? is the gate edit committed?). Partially covered in F3; keep §8 ambiguity small.
  - F15 (LOW): §4 NEVER-TOUCH lists scripts/** without cross-referencing the I3 exception (only §5 and §9 mention it) — a literalist builder may refuse the extension and run the gate at 20, reporting I3 green against a gate that never checked the new terms. Wait — that's actually the reverse cheat: run doorway-check un-extended → passes trivially (new terms unchecked) → claim I3 green. The WO says "You must ALSO run the similarity check over your new terms" — but if the builder skips the extension, nothing in the oracle catches it; only the report's "I3 gate-extension edit if made" — "if made"?! §8 says report "the I3 gate-extension edit if made" — implying it might not be made, which would leave I3 unverified for new terms while reported green. That upgrades this: the combination of "if Lane M's derived version is not yet merged" + "if made" creates a silent-skip path. MED. Fold into F3 or separate. I'll fold the "if made" loophole into F3 as part of the same hole... Actually it's a distinct failure scenario (skip vs tamper). Keep F3 about tamper/commit ambiguity, add F15b... I'll make it F4-order. Let me reorganize: I'll fold the skip-path into F3 since both are I3 exception weaknesses; or enumerate as F3 (skip) and F4 (tamper). Cleaner: F3 = I3 exception has two cheat paths: silent skip ("if made") and tamper; commit-status unspecified. One finding, three sub-holes. Good.

  Also consider: shingle-exposure / xactimate homeowner-search-fit — answer in target 3, maybe a LOW finding. I'll keep it in answers, not findings, to avoid inflating.

  Also the date: "2026-08-19" and branch from master@56fcc11 probed 9:17pm — fine.

  One more check — the WO says "137 → 168 if auto-composed": 137+31=168 ✓.

  Gates: `npm run typecheck` — does the repo have that script? Presumably. `npx next start -p 3210` in background — fine.

  I5: No `!` — enforced how? No gate named; grep-able but not gated. Builder self-checks. LOW at most; fold into answers or a low finding. The word "no exclamation points" is in voice laws and I5 — no automated check. The spam-410-guard and doorway-check don't check it. Minor: one grep line could gate it. LOW finding F15.

  Hmm, also: "Disclosure: paths: y · client-names: y · strategy: y" — that's the brief's metadata, not WO.

  Also: the WO doesn't say what to do if a gate is red for reasons outside its control (e.g. reachability-check red due to pre-existing issue on master cut). Standard "report, don't fix gates" covers partially. Skip — inventing-ish.

  Also: no rollback instruction if the pour can't get under 40% (e.g. drop a term? NO — list is final, "do not drop"). So if recoverable-depreciation can't pass the gate no matter how written, the builder has NO legal move: can't drop the term, can't weaken the gate, can't touch the existing entries. Trapped-builder scenario: the WO gives no escalation path ("if after N rewrites a pair still exceeds 40%, STOP and flag"). That's a real MED finding — every other trap (app/** sitemap fix) has the same shape: the WO lacks a general STOP-and-escalate clause. Actually §9 style fleets usually have "flag, never improvise" in AGENTS — but this builder has "no context beyond the WO"! The WO itself never says "if blocked, stop and report rather than improvise." Given an isolated builder with no AGENTS.md context (worktree of mabrey-roofing — hmm, the repo may have its own AGENTS.md; unknown). The WO as a self-contained instrument lacks an escape hatch. MED finding F-trapped. Good one: concrete failure: builder can't satisfy "list is final" + I3 simultaneously for a stubborn pair → either ships >40% and lies, or weakens gate (see F3), or stalls. Minimal fix: add "if any pair cannot be brought under 40% after rewrite, STOP, report the pair and the score; do not drop terms, do not touch the gate beyond I3."

  Now THE ONE THING: I still say the oracle hardening (content + href + sitemap proofs) — because it converts the WO's own stated success criteria from trust-me reports into machine checks, and covers F1, F2, and partially F9. Alternatively the I3 fix... but Lane M reconciliation partially covers I3 at merge. The oracle holes are the only place where the WO declares victory conditions that its own verification cannot detect failing. Pick that.

  Let me also double check target 3's "any term a homeowner would never search": shingle-exposure is the best candidate (contractor jargon; near-zero consumer queries as a definitional search). Maybe also "attic-baffles" is borderline-fine. "rake-edge" — homeowners search "rake of roof" rarely; "rake edge" some. I'd name shingle-exposure as weakest, xactimate as mixed-audience. Fine.

  Compliance terms: recoverable-depreciation (insurance-advice territory — WO handles with neutrality law, adequate), xactimate (trademark — suggest trademark-respectful framing; nominative use is fine; the WO doesn't require the ®/attribution — not legally required; don't overstate. I'll say the risk is tone drift + implying affiliation, and the neutrality law mostly covers it; add "Xactimate® is a Verisk product" style attribution as optional minimal fix — careful to mark it optional). roof-warranty (must not contradict the company's actual warranty page; no instruction to read site warranty content — LOW/MED). hail-bruising (touching insurance-adjacent damage assessment — a definition is fine; risk is drifting into "your claim" advice; the insurance-neutrality law lists only three terms — hail-bruising, wind-uplift, storm-chaser are insurance-adjacent but not covered by the neutrality clause! The clause says "Insurance terms (recoverable-depreciation, xactimate, roof-warranty)". hail-bruising definitions can easily slide into "document it and file a claim" advice — claim-outcome promises banned only for the three named. Gap: extend neutrality to hail-bruising/wind-uplift/storm-chaser. That's a legit MED-LOW finding. F: insurance-neutrality scope too narrow.)

  Counting findings: F1 sitemap oracle (HIGH), F2 content-presence oracle (HIGH), F3 I3 exception cheat paths (HIGH), F4 recoverable-depreciation collision unflagged (MED), F5 sub-fascia cannibalization+overlap (MED), F6 cluster collisions unflagged (MED), F7 sitemap-fix file vs §4 trap + no escalation path (MED), F8 forced relatedMoneySlug (MED), F9 index composition unverified (MED), F10 neutrality scope (MED), F11 term strings unpinned (LOW), F12 word bounds/counting (LOW), F13 ordering/index order (LOW), F14 formatter churn (LOW), F15 no automated I5/`!` check (LOW), F16 weak homeowner-fit terms (LOW — or leave in answers only; I'll leave in answers to keep findings crisp; brief warns invented findings are a defect — shingle-exposure search-fit is a judgment call, keep it in answers).

  Also §8 report "word counts per entry as a table" — fine.

  Hmm, also F: "nothing above the first new entry" in I1 — what about BELOW the last new entry? Appended at end so nothing below; fine, I1 says "only additions inside the array + nothing above the first new entry" — adequate given append-only.

  Edge: kebab-case slugs — all 31 given as slugs already, good — that IS specified (the list is slugs). Term strings unpinned though (F11).

  Does the WO specify `term` field? No — only slugs given. Right.

  One more possible hole: the [term] route param is `[term]` — slug-based routing assumed; if route matches on `term` field instead of slug... §1 says slugs list and routes; assume slug. Not a finding.

  Sitemap count "137 → 168 if auto-composed" — but §7 oracle doesn't include it (F1). Also the sitemap probe is a "report" task, not a gate — covered.

  What about `shortAnswer` 60-100 words and answer-first — no gate; judge. Fine.

  Format: 
  ## VERDICT — one paragraph, sound-to-execute yes/no. My verdict: Sound to execute with fixes? The WO is executable — a competent builder ships this; the holes are in verification (oracle can pass while broken) and collision guidance (gates catch most of those, at cost of rework loops). So: yes, sound to execute, but NOT sound to trust — the oracle must be hardened before dispatch, and three MED coverage gaps flagged. "Sound-to-execute: yes, conditional" — the brief wants yes/no. I'll say: YES with three pre-dispatch patches (oracle sitemap+content proof, I3 exception tightened, collision spines for 4 clusters); without the oracle patch the WO can report success on a broken pour. Given "sound-to-execute yes/no" — I'll answer: No as written (oracle can certify a broken build + I3 has a silent-skip path), executable after a 15-line patch. That's a defensible hostile-reviewer verdict. Hmm — LAUNCH-KILLER anchor = "ships and causes material harm". Empty pages + missing sitemap shipping to production while reported green = material SEO harm? Sitemap missing = pages still indexed via links, slower discovery — survivable. Empty pages = thin content on 31 pages = real harm. But the builder would have to write empty bodies deliberately or... actually failure scenario isn't malice: a rendering mismatch (e.g. field renamed in type but page reads old field) → build typechecks?? If type GlossaryTerm requires body, builder can't omit it — typecheck forces the field to exist and be a string; empty string passes typecheck but why would a builder write empty strings? More realistic: body present but pages render only shortAnswer because... no. Honestly the empty-page scenario requires builder negligence that PLUS-JUDGE review would catch. The more realistic oracle failure: sitemap silently missing (mechanism unknown until probed!) — the WO ITSELF doesn't know if sitemap auto-composes, yet puts no sitemap check in the oracle. That's the realistic HIGH. So verdict: sound to execute = yes, with the caveat that §7 must add the sitemap proof before dispatch; I'll phrase verdict as "Yes, conditional on one oracle patch (sitemap + content proof) and one I3 tightening; the rest are rework-risk reducers." Hmm, hostile reviewer: "sound-to-execute: yes" feels right because nothing blocks starting; the flaws are in what "green" means. I'll write a nuanced single paragraph ending with clear yes/no: "Sound to execute as a work order (a builder can start and finish), NO as a verification instrument as written."

  Given the output format "## VERDICT (one paragraph, sound-to-execute yes/no)" I'll give: "Yes — executable as written, but its proof of success is not sound... " and be crisp.

  Length: keep it thorough but not bloated. Markdown to stdout. Use the required sections. Write findings compactly: severity · section · hole · failure → fix.

  Let me draft.

  Answers section mirrors targets 1-6.

  Missing-X list: MISSING-DIFFERENTIATION-SPINES, MISSING-SITEMAP-ORACLE, MISSING-ESCALATION-CLAUSE. Optional — include briefly.

  Word count targets: fine.

  Write it.

To resume this session: kimi -r session_28fd18c9-6bbf-46f7-ac64-98c2b62cc00d
