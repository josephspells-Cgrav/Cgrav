# Disposition ledger — kimi baton on km-engine Mabrey-integration upgrade (2026-08-20 ~4:37pm ET)

Audit: KIMI_OUT_KMENGINE_UPGRADE_20260820-1632.md · Verdict AMEND-NEEDED · 7 MAJOR + 7 MINOR, all dispositioned:

| # | Finding | Disposition |
|---|---|---|
| 1 MAJ | No-book fallback inverted §17 (Tier-2 needs a book) | **FIXED** — pull #9 rewritten: no-ACCESS → county hubs only, roster deferred; no-HISTORY → county hubs + EXPANSION register (zero work-claims). Cases never conflated; deliverable names which applies. |
| 2 MAJ | "(or genuine service capacity)" = open bypass | **FIXED** — phrase deleted from the roster law; replaced by a defined EXPANSION register: operator-attested towns only, third register with zero completed-work language, labeled EXPANSION in the deliverable. Fold-or-keep ownership extended to ALL 1-9-job towns (operator decides, surfaced with counts). |
| 3 MAJ | Laws bound architecture only; audit sells first | **FIXED** — AUDIT + PLAN mode lines now carry the binding: §18 verify-or-cut on audit-page specifics, §20 on all location promises, and the one-roster rule (audit sells only what architecture would build). Pull #9 runs in `all` mode. |
| 4 MAJ | Template header "one per city" contradicted §17 | **FIXED** — header now reads "location pages for every EVIDENCE-SUPPORTED city (doctrine §17 overrides any 'one per city' reading)". |
| 5 MAJ | Gates = false precision, unmeasurable metric | **FIXED** — similarity metric defined (word n-gram Jaccard, n≈5, rendered MAIN-CONTENT only, chrome/schema excluded); 8+-word-clause rule scoped to main-content prose; calibrate-don't-worship rider added covering all thresholds. |
| 6 MAJ | Standalone architecture can't fill its template | **FIXED** — architecture mode now owns pulls 2-5 + 9 (+10 on existing domain); reuse a <1-quarter-old data layer per pull-ONCE; no data → no projection section, stated. |
| 7 MAJ | Spam-legacy gated on job history | **FIXED** — split into independent pull #10, conditioned only on an existing domain, any mode. |
| 8 MIN | "≥10 jobs" no unit; Tier-2 per-hood counts unstated | **FIXED** — per-TOWN explicit + calibrate note; "no per-neighborhood counts/years/dollars in ANY tier or register" promoted to its own global bullet. |
| 9 MIN | §16 radius-implying language, no §17 pointer | **FIXED** — §16 location line now points at §17 (evidence roster decides; market only sets the ceiling). |
| 10 MIN | §16 ~160 ceiling vs §21's 168 proof point | **FIXED** — range updated to ~100-180 with the Mabrey 168 cited. |
| 11 MIN | Total Scope omitted glossary → floor-gate undercount | **FIXED** — GL glossary term added to the formula + note that the sitemap floor derives from TOTAL. |
| 12 MIN | Book-vs-claimed-radius unaddressed | **FIXED** — new §17 bullet: surface the gap to the operator; projection may denominate on the measured radius but the built roster is stated beside it. |
| 13 MIN | Verify-or-cut didn't reach the audit page | **FIXED** — §18 opening extended to the client-facing audit page + AUDIT mode line carries it. |
| 14 MIN | Scrub-table reviewer unnamed | **FIXED** — auditor named: the §18 independent judge when fielded, else the operator. |

No finding rejected. Backups: SKILL.md/doctrine.md/output-template.md `.bak-2026-08-20`. aggregateRating removal was audited CLEAN by the baton (three sites + locks, supersession noted).
