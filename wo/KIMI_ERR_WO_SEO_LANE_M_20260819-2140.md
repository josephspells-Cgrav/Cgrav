• The user wants me to read a work order file and produce the written audit it specifies. Let me read the file first.

• I need to read the artifact under audit: `C:/Users/josep/Claude Gravity/wo/WO_SEO_LANE_M_410_DIRECT_DOORWAY_DERIVE.md`. The brief says "Read exactly this one file and nothing else" — that's the artifact under audit. The brief is hostile-review, read-only, markdown analysis only.

  Let me read the artifact.

• Now I need to produce a hostile adversarial audit of this work order. The brief says: read exactly this one file (the WO) and nothing else. I've done that. Now I produce markdown analysis only, no file creation, no modifications. Output goes to stdout as markdown.

  Let me think hard about the audit targets.

  The setup: Next.js 16 static marketing site. Middleware 410s spam. The problem: Next's built-in trailing-slash normalization (308 redirect to slashless) runs BEFORE middleware, so `/spam/` gets 308 → `/spam` → 410. GSC removals want direct 410s.

  The WO's intended design (§2.3):
  - `next.config.ts`: add `skipTrailingSlashRedirect: true`
  - `middleware.ts`: normalize pathname (strip trailing slash except root), run spam checks against normalized path, if spam → gone() (410), if not spam and raw had trailing slash → 308 to normalized, A3/B operate on normalized path.

  Now let me attack this from the audit targets:

  **1. Coverage holes in the oracle matrix.**

  The oracle matrix (§7) has 15 rows. What's missing:

  - **Multiple trailing slashes**: `/online-casinos-curacao-2026//` — the WO says "handle multi-slash tails defensively" in the normalized derivation, but the oracle doesn't probe it, and the normalization code `pathname.slice(0, -1)` only strips ONE slash. So `/spam//` → normalized = `/spam/` → spam check against `/spam/`... wait, but if A2 matches against the normalized path after stripping only one slash, then `/spam//` normalizes to `/spam/` which may not match the spam pattern (if patterns expect no trailing slash or single trailing slash). And then "If NOT spam and the raw pathname had a trailing slash → 308 to normalized" — so `/spam//` → 308 → `/spam/` → second middleware run → normalized `/spam` → spam → 410. Hmm, so it converges in two hops. But wait — what does the WO mean by "handle multi-slash tails defensively"? The code given literally only strips one slash: `pathname !== "/" && pathname.endsWith("/") ? pathname.slice(0, -1) : pathname`. The parenthetical "(handle multi-slash tails defensively)" contradicts the literal code. That's an underspecification: should it be `pathname.replace(/\/+$/, "")`? If the builder strips only one slash, `/spam//` → 308 hop → `/spam/` → 410 — a redirect hop for a spam URL, which is exactly what the WO exists to eliminate, and the guard (probing both forms) only tests `/path` and `/path/`, not `/path//`. So a multi-slash spam URL still 308s first. Does that matter? Google indexes single-slash forms mostly, but a hostile reviewer notes: the stated success criterion (1) says "every known spam URL returns HTTP 410 on the FIRST response in both slash forms" — only both slash forms, singular. So multi-slash is arguably out of scope of the success criteria, but the WO itself raises multi-slash tails and then gives code that doesn't handle them. Underspecification + guard doesn't test it. Also `//` at root: `//` → normalized? `pathname !== "/"` is true for `//`, endsWith("/") true, slice → `/`. Then not spam, raw had slash → 308 to `/`. Hmm, `//` → 308 → `/` → 200. Today what happens? Next normalizes `//`? Actually the request `GET //` — weird. Probably fine.

  - **Encoded slashes %2F**: `/online-casinos-curacao-2026%2F` or `%2f`. Next.js decodes `%2F` in pathname? Actually in Next.js, `%2F` in a path is decoded by the server and `nextUrl.pathname` would contain the decoded `/`. So `/spam%2F` might appear to middleware as `/spam/` — that's actually handled by the same logic. But `/spam%2F%2F`? Also `%2F` alone: `/%2F` → decoded `//`. These are edge cases the oracle doesn't probe. Whether they matter: a spam URL with an encoded slash in Google's index is plausible (WordPress-era hacked URLs with encodings). The matrix misses them. Also case: `/ONLINE-CASINOS-CURACAO-2026/` — does matchesSpam410 handle case? Next normalizes? Not necessarily. Oracle doesn't probe case variants. Hmm, the brief says think about encoded slashes, query strings, HEAD vs GET, path exactly "/", matcher-excluded set.

  - **Query strings on spam URLs**: The matrix has `/?p=47408` (A1 query-param 410). But what about `/online-casinos-curacao-2026/?foo=bar` — slash form spam WITH query string? Under the new middleware: normalized = `/online-casinos-curacao-2026`, spam → gone() → 410. Fine. And the legit trailing-slash 308 "keep search" — the WO says clone nextUrl, set pathname, keep search — good, that's specified. But what about spam query-param keys on legit paths: `/locations/cary-nc/?p=47408`? A1 fires on WP query keys — presumably 410s any path with `?p=`. That's existing behavior, unchanged. But the interaction: A1 runs against normalized path now — does A1 care about path at all? Probably checks searchParams. Running it against normalized path shouldn't change it. OK.

  - **HEAD vs GET**: The guard probes — does it probe HEAD? GSC validation typically does GET. But Googlebot also issues HEAD sometimes. Next middleware: a 410 response to HEAD — NextResponse with status 410 works for HEAD (body stripped). Probably fine, but the oracle doesn't specify HEAD, and the guard's assertion `location header absent` on HEAD 308s... For the legit trailing-slash 308, a HEAD request gets a 308 with Location — fine. Not a big finding, but the matrix doesn't define HEAD behavior; Next's gone() helper presumably returns a body — for HEAD Next strips it. LOW at most. Actually more interesting: the guard script probes with what method? If it uses fetch default (GET), fine. HEAD is an underspecification but low impact.

  - **Path exactly "/"**: The normalization explicitly guards `pathname !== "/"`. Good — they thought of it. But `/` with trailing slash is just `/`. What about `//`? Covered above. What about `/index.html`? Dotted → excluded from matcher, so middleware never sees it; static file or 404 via Next. Fine.

  - **The matcher-excluded set**: `/quote` is excluded from the matcher. Under `skipTrailingSlashRedirect: true`, Next's built-in trailing-slash 308 is disabled for ALL paths, including `/quote/`. The WO's I5 covers `/quote/` — probe, record, and if 404 add a redirects() entry. Good — they anticipated it. But wait: is `redirects()` in next.config the right fix, and does `redirects()` run before the filesystem? In Next.js, `redirects()` from next.config run BEFORE filesystem check? Order: headers, redirects... actually Next.js checks: `redirects` from next.config run before filesystem routes? The docs order: headers → redirects → middleware → beforeFiles rewrites → filesystem → afterFiles → fallback. Hmm, actually middleware runs AFTER next.config redirects? Let me recall: In Next.js, the order is: `headers` (config) → `redirects` (config) → middleware → `rewrites` beforeFiles → filesystem (pages/public) → `rewrites` afterFiles → dynamic routes → `rewrites` fallback. Yes, config redirects run before middleware. The WO claims this ("config redirects run before middleware and don't touch the matcher") — correct per docs.

    But here's a subtlety: does `redirects()` with `source: "/quote/"` work — Next normalizes sources? With `skipTrailingSlashRedirect: true`, a redirect source `/quote/` should match literally. OK.

    Deeper: `/quote` is EXCLUDED from the matcher, so middleware never runs for `/quote`. With skipTrailingSlashRedirect, what serves `/quote/`? Next's filesystem router: `quote` page exists (app router? or pages?). The site is "Next.js 16 static marketing site" — app router presumably, with `output: export`? "static marketing site" — could be `output: "export"`. Hmm! If it's a full static export (`output: 'export'`), then... middleware doesn't run at all in static export! But middleware DOES run (they probed 410s in prod), so it's not a pure static export, or middleware runs at the edge before static files. Actually — wait. If the site were `output: 'export'`, there'd be no middleware in prod. They probed `410` from middleware in prod, so middleware runs in prod. So it's statically-rendered pages but served by a Node server (next start) — the gates run `npx next start -p 3210`. So it's `next build` + `next start` with static generation, not export. Fine.

    With `next start`, the trailing-slash 308 for `/quote/`: currently Next's built-in normalizer handles `/quote/` → 308 → `/quote` regardless of the matcher (it's routing-level, before middleware). With skipTrailingSlashRedirect: true, that normalizer is off; middleware never sees `/quote/` (matcher excludes it); so `/quote/` falls to the filesystem router. Does Next's filesystem match `/quote/` to the `quote` route when trailingSlash: false (default)? With default `trailingSlash: false`, `/quote/` is... hmm. Historically, with trailingSlash: false, Next serves `/quote` and 308-redirects `/quote/` → `/quote` (that's the normalizer). If you disable the normalizer, `/quote/` hits the router and... I believe it 404s, because route matching is strict-ish. Actually I recall that with skipTrailingSlashRedirect: true, `/quote/` may still match the route in app router? Not sure — that's exactly why I5 says "record actual; if 404, add redirects() entry". So the WO hedges correctly. Good. But: the WO's fix (`redirects()` entry) adds a redirect for `/quote/` → is that acceptable per the success criteria? Success criterion (6): "no regression on /quote (a paid-traffic fast path excluded from the middleware matcher)". If today `/quote/` → 308 → `/quote` (via built-in normalizer) and after the change `/quote/` → 308 → `/quote` (via config redirect), behavior preserved. If they forget the redirect and `/quote/` 404s, I5 catches it. OK — but here's the hole: the matcher excludes `quote` — note the matcher pattern: `"/((?!api|_next/static|_next/image|_next/data|favicon.ico|robots.txt|sitemap.xml|quote|.*\\.).*)"`. The negative lookahead excludes any path STARTING with `quote`?? The lookahead `(?!...|quote|...)` — this is a lookahead at the position right after `/`, so it excludes paths beginning with `quote` — that includes `/quote`, `/quote/`, but ALSO `/quote-anything`, `/quotes`, `/quotester`. Probably intended for the quote funnel, fine, existing behavior, never-touch.

    Also `/quote` excluded means: what if a spam URL lived under `/quote/...`? Not the case. Skip.

  - **Dotted paths**: excluded via `.*\\.` — any path containing a dot bypasses middleware. So spam URLs with dots? e.g. `/author/rrsc/page/2597/` no dot. Fine. But what about spam URLs with dots — do any exist? `/online-casinos-curacao-2026` no dot. If a spam URL contained a dot, it would bypass middleware entirely and 404 (not 410) — but that's pre-existing behavior, unchanged by this WO. Not this WO's hole, though worth noting the guard's positive controls presumably cover the known 33. Skip or LOW.

  - **Apex vs www**: The matrix rows don't say host. §2.1 says host-agnostic. The guard probes a local server — no host dimension locally. The orchestrator re-runs live post-deploy. But is there any host-based redirect (apex → www or reverse) that runs BEFORE middleware (e.g., Vercel-level domain redirect)? If Vercel redirects apex → www at the edge before middleware, then apex spam URL returns 308 to www FIRST — violating "direct 410 on first response on apex". Hmm — "on apex and www". If the platform 301s apex→www before middleware, the first response on apex is a 301, not a 410. The WO claims "host-agnostic — middleware already is" but doesn't address platform-level host redirects. Is that real? If mabreyroofing.com apex is configured to redirect to www (or vice versa) in Vercel domain settings, that redirect happens before middleware. The WO's success criterion (1) explicitly says "on apex and www" for first-response 410. The WO never asks the builder to verify whether apex requests even reach middleware without a preceding host redirect. That's a genuine coverage hole — though arguably out of the builder's control (Vercel config). But the WO could have said: probe apex live, and if the platform issues a host redirect first, flag it (GSC removals would then target the www form after the host hop — actually a 301 → then 410 is fine for GSC? The F7 sequencing law wants direct 410s; a host canonicalization redirect might be acceptable to GSC. Unknown). It's a legitimate audit point: the matrix doesn't specify the host dimension at all despite success criterion (1) requiring apex+www. MED.

  - **`/sitemap.xml//robots.txt`** — the brief's success criteria mention "/sitemap.xml//robots.txt" — that's a typo in the brief for "/sitemap.xml / robots.txt" (double slash as separator). I6 covers sitemap.xml and robots.txt bypass. Fine.

  - **Encoded slash %2F deeper**: Next.js — when a request comes in with `%2F`, the URL pathname as parsed by the server... In Next.js middleware, `nextUrl.pathname` is decoded. So `/online-casinos-curacao-2026%2F` arrives as pathname `/online-casinos-curacao-2026/` → middleware treats as slash form → 410 direct. But the RESPONSE is for the request URL with `%2F` — GSC removal for the literal URL `...%2F` — would Google see a 410? If the server responds 410 directly, yes. OK so encoded slashes may just work. But what about `%2f` in the MIDDLE creating double slashes: `/blog%2F/page/9` → pathname `/blog//page/9` → normalized only strips TRAILING slash; inner `//` remains → matchesSpam410 against `/blog//page/9` — does the pattern tolerate inner double slash? Probably not → falls through → 404, not 410. Pre-existing? Today, Next's normalizer doesn't collapse inner double slashes either (actually Next DOES normalize `//` in paths? There's `cleanPath`? Vercel's edge may collapse). Uncertain — but per the audit brief, %2F is listed as a thing to think about, so include as a matrix gap: no encoded-slash row. MED/LOW.

  - **Case sensitivity**: `/AUTHOR/rrsc/` — WordPress URLs were lowercase mostly. Skip.

  **2. Underspecification sweep — places the builder must guess:**

  1. **"handle multi-slash tails defensively" vs the literal code** that strips exactly one slash. Contradiction. Builder must guess which is authoritative. (Also intersects with the guard: no multi-slash control.)
  2. **Where in the middleware chain does the trailing-slash 308 go?** §2.3 says: spam checks on normalized path; if not spam and raw had slash → 308. But relative to A1 (query-param 410)? Order stated: "Run A1/A2 spam checks against the normalized path." So A1 also on normalized. Then "A3/B sections operate on the normalized path". But does the 308 for non-spam trailing-slash happen BEFORE A3/B or AFTER? Logically: if raw has slash and not spam → 308 immediately (before A3/B). But consider a LEGACY_301 slug with trailing slash: `/roof-replacement/` — matrix says expect `301 → /services/roof-replacement`. Hmm! If the builder 308s ALL non-spam trailing-slash URLs immediately, then `/roof-replacement/` → 308 → `/roof-replacement` → middleware again → A3 LEGACY_301 fires → 301 → `/services/roof-replacement`. TWO hops. But TODAY: `/roof-replacement/` → Next normalizer 308 → `/roof-replacement` → middleware → 301 → `/services/roof-replacement` — also two hops today. So parity. But the matrix row says `/roof-replacement/` expect `301 → /services/roof-replacement` — as if ONE response is a 301. If the builder implements "slash → 308 first", the actual result is 308 first, and the matrix row FAILS as written (expects 301 direct). Contradiction between §2.3 design and §7 oracle! Unless the builder is meant to run A3/B BEFORE the trailing-slash 308 — i.e., check legacy-301 on the normalized path first, and only 308-normalize if nothing else claims the response. The matrix implies: `/roof-replacement/` → 301 direct (legacy rule wins over normalization), while `/locations/cary-nc/` → 308 → slashless → 200. So the intended order must be: normalize → A1/A2 spam (410) → A3 LEGACY_301 on normalized (301 if match) → B migrations → ELSE if raw had trailing slash → 308 to normalized → else next(). But §2.3's prose says "If NOT spam and the raw pathname had a trailing slash → 308 redirect" — listed BEFORE "A3/B sections operate on the normalized path". Reading order implies 308 before A3/B, which contradicts the oracle row for `/roof-replacement/`. THIS is a real, concrete underspecification/contradiction: the builder cannot satisfy both a strict reading of §2.3 and the §7 matrix row. Which wins? The WO says deviations from §2 must be argued; but the matrix is "ORACLE". A builder following §2.3 literally produces 308 → /roof-replacement → 301 (two hops), and the oracle row as written (`301 → /services/roof-replacement`) would record a fail or at least an ambiguity ("expect" column doesn't say "first response is 301"; maybe it means eventual). The matrix says "request | expect" — for spam it says "410 direct" (explicit about directness). For `/roof-replacement/` it says `301 → /services/roof-replacement` without "direct". So two-hop might be acceptable and the row just describes the chain loosely. Still: ambiguity → builder guess → possible false alarm in the report or wrong implementation. HIGH-ish/MED. Also TODAY's behavior for `/roof-replacement/`: 308 (normalizer) then 301 — wait, actually today the normalizer runs BEFORE middleware, so today it's 308 → 301, two hops. The matrix row as "expect" after the change... if they wanted parity, the row should read "308 → /roof-replacement → 301". If the row intends "301 direct", that's a BEHAVIOR CHANGE (improvement) requiring A3-before-308 ordering. The WO never states the ordering explicitly. Defect: order of the new 308-normalization relative to A3 LEGACY_301 and B migrations is unspecified, and the oracle's `/roof-replacement/` row is ambiguous. HIGH (likely rework / wrong first implementation).

     Also B (legacy route migrations) — same question.

  3. **What does "clone nextUrl, set pathname, keep search" mean for the 308 status code**: NextResponse.redirect defaults to 307; you must pass `{ status: 308 }`. The WO says "308 redirect" — specified. OK, not a hole. But `NextResponse.redirect(url, 308)` vs `new NextResponse(null, {status: 308, headers})` — either works. Fine.

  4. **The 308 must preserve the HOST?** clone nextUrl — yes host preserved. Fine.

  5. **guard extension §2.5**: "every positive-control path is probed in BOTH forms ... asserting statusCode === 410 on the FIRST response and location header absent." The guard "probes a live server on :3210". How does the guard currently follow redirects? If it uses fetch with redirect: 'follow' default, the FIRST response isn't observable — must use `redirect: 'manual'`. Does the existing guard already use manual? Unknown — builder must read the file. Also: the guard is in NEVER-TOUCH-adjacent? No — `scripts/spam-410-guard.mjs` is in ONLY-THESE-FILES. But `package.json` scripts are never-touch — the guard's npm script stays the same. OK. Also "location header absent" — for spam both forms. And the third control: sitemap URL + slash → 308 with Location = slashless. "sample ≥20 sitemap URLs if probing all 137 is slow — say which in the report" — underspecified: WHICH 20? Random? First 20? Stratified by section (locations/services/blog/glossary)? A builder sampling the first 20 sitemap URLs might get 20 homogeneous /locations/ pages and miss a section whose template breaks. That's a vacuity risk (target 5). Also where does the guard GET the sitemap list — fetch /sitemap.xml from :3210 and parse? Or read lib/sitemap-registry.ts? NEVER-TOUCH includes lib/sitemap-registry.ts as never-MODIFY (it's in the NEVER-TOUCH list) — but reading/importing is fine. Underspecified: source of sitemap URLs (fetch+parse XML vs import registry). Both defensible; the WO doesn't say. MED/LOW.

  6. **doorway-check §2.4**: "derive if they exist, otherwise leave hardcoded WITH a comment" for ARTICLES/BLOG — that's a judgment call the WO allows, fine, it's explicit. But: the existing doorway-check has `CITY_NAMES` (14) — a list or a map? WO says derive `Object.fromEntries(CITIES.map(c=>[c.slug, c.name]))` — implies CITY_NAMES becomes a lookup object; but if the script's existing code uses CITY_NAMES as an ARRAY (e.g., `CITY_NAMES.includes(name)`), changing its SHAPE breaks the script's internals — the WO prescribes the derivation expression without knowing (or telling the builder to check) how CITY_NAMES is consumed. If consumption expects an array of names, the correct derivation is `CITIES.map(c=>c.name)`. The WO's literal prescription might be wrong for the script. Builder must reconcile — underspecified, and following the WO literally could break the gate or force the builder to rewrite consumption code (scope creep beyond "derive lists"). MED. Also does `CITIES` entries have `.name` field? §1 says lib/cities.ts has `CITY_SLUGS = CITIES.map(c => c.slug)` — implies CITIES is an array of objects with slug; name assumed. Probably fine.

  7. **The `gone()` helper preserves `x-robots-tag: noindex` + `cache-control: no-store`** — the WO says preserved; fine.

  8. **skipTrailingSlashRedirect + base path / i18n?** Not present presumably. Skip.

  9. **`skipTrailingSlashRedirect` semantics with `next start` vs Vercel**: The config is build-time; Vercel respects it. Does Vercel's edge ALSO do trailing-slash normalization BEFORE middleware regardless of config (platform-level)? Historically Vercel normalizes trailing slashes at the edge for some configs... Actually the whole premise: on Vercel, the 308 observed in prod — does it come from Next.js or from Vercel's edge? They probed prod and saw 308 → 410. If the 308 is emitted by VERCEL's infrastructure (not Next's router), then `skipTrailingSlashRedirect: true` in next.config might NOT disable Vercel's edge-level normalization, and the whole change fails in prod while passing locally on :3210. THIS is the biggest "assumption that could be wrong" for target 3. Vercel docs: trailing slash handling — Vercel follows the Next.js config (`trailingSlash` / `skipTrailingSlashRedirect`)... I believe Vercel honors the framework config and normalizes at the edge accordingly. When `skipTrailingSlashRedirect: true` is set, Vercel should stop normalizing. I recall Vercel's build output respects `trailingSlash` from next.config for its own routing. Reasonably confident it works, but the WO never tells the builder to verify that the 308 observed in prod is actually emitted by Next and not by Vercel edge (e.g., check `server` / `x-vercel-*` headers on the 308). The orchestrator re-runs the matrix live post-deploy — so if it fails in prod, it gets caught — but the WO's local gates would be green while prod is broken, and the WO treats local :3210 as oracle. The empirical check the builder SHOULD do: probe prod 308 response headers (server: Vercel, x-matched-path, etc.) — actually can't tell definitively from headers. Better: after deploy, orchestrator verifies. So the WO's safety net is post-deploy verification; the risk is a wasted deploy cycle. MED.

  10. **Node version for native TS import**: §1 says spam-410-guard uses "Node ≥22.6 type-stripping precedent". §2.4 says "same Node ≥22.6 mechanism". But type-stripping in Node 22.6 requires `--experimental-strip-types`; Node 22.18+/23 has it on by default? Actually: Node 22.6 added `--experimental-strip-types` (flag required). Node 22.18 enabled type stripping by default (backported), Node 23.6 unflagged. If the existing guard's npm script passes the flag explicitly, fine. But the doorway-check npm script (`npm run doorway-check`) — does ITS script entry pass `--experimental-strip-types`? package.json is NEVER-TOUCH ("package.json scripts" in six nevers, and package.json in NEVER-TOUCH list). If the existing `doorway-check` script is plain `node scripts/doorway-check.mjs` WITHOUT the flag, and the local Node is 22.6–22.17 (flag required), then importing `../lib/cities.ts` crashes with ERR_UNKNOWN_FILE_EXTENSION or syntax error, and the builder CANNOT fix it by editing package.json (never-touch) — must rely on ambient Node version being ≥22.18/23.6, or use a different mechanism. The WO says "same Node ≥22.6 mechanism spam-410-guard.mjs already uses" — implying the mechanism is proven in-repo; but if the guard's invocation passes the flag via its npm script and doorway-check's doesn't, the builder is stuck between a red gate and a never-touch. ALSO `lib/cities.ts` type-stripping caveat: type stripping (not transformation) fails on enums, namespaces, parameter properties, and certain syntax requiring code generation; if cities.ts uses an `enum` or `import ... = `, plain stripping fails. WO says "lib/cities.ts imports nothing beyond types (verify)" — but doesn't mention enum/non-erasable-syntax risk. And transitive imports (target 4): glossary.ts might import from a shared types file or from lib/articles/utils which imports Next. `GLOSSARY_TERMS: GlossaryTerm[]` — where does `GlossaryTerm` come from? A type-only import — erasable, fine. But if glossary.ts has a runtime import (e.g., from another articles file that imports `next/...` or reads fs at module scope), plain node breaks. The WO's §2.4 ⚠️ covers this for cities.ts ("if the import chain pulls in Next/React... STOP and report") — actually it says verify lib/cities.ts imports nothing beyond types. It does NOT extend the same caution explicitly to glossary.ts (target 4 asks about lib/cities.ts natively, but the same applies to glossary.ts — and glossary.ts is being EDITED by a parallel lane, so its import chain can CHANGE under this WO's feet). Also: importing a `.ts` file from an `.mjs` file under type-stripping: the import specifier must include the extension `../lib/cities.ts` — with `.ts` extension explicitly; Node's strip-types requires explicit `.ts` extensions in relative imports. If cities.ts internally imports `./types` without extension (TS style, allowed in Next), Node ESM resolution FAILS (needs `./types.ts`, and even then, allowImportingTsExtensions...). Wait — cities.ts "imports nothing beyond types" — type-only imports with `import type { X } from "./types"` are ERASED during stripping, so no resolution happens. Fine. But if it's `import { type X }` inline or a value import, resolution occurs. The WO's verify-step covers this partially. The bigger one: **does Node type-stripping apply to files imported from node_modules or only entry?** Applies to any .ts. Fine. Also `--experimental-strip-types` flag presence in the doorway-check npm script — I'll flag as MED/HIGH underspecification: WO assumes mechanism transfers; never-touch on package.json may trap the builder.

  11. **`next start` vs `next dev`** — gates use next start on a build; fine.

  12. **§2.5 guard: "asserting ... location header absent"** — for the 410s. Fine.

  13. **I7: "the A1 query-param check must not fire on empty params"** — `/` with no query. Existing. Fine. But what about `/?` trailing empty query — trivial. Skip.

  14. **What about the matcher excluding `.*\\.`** — the negative lookahead `(?!...|.*\\.)` — `.*\.` means "contains a dot anywhere". A spam path with an encoded dot `%2E`? Decoded to `.` → excluded → 404 not 410. Edge. Skip or fold into encoded-hole.

  15. **Middleware runtime**: Next 16 middleware runs in Node.js runtime by default? (Next 15.2+ supports node runtime for middleware; 16 made... not sure). Irrelevant to WO holes.

  16. **`skipTrailingSlashRedirect` interplay with `trailingSlash` unset** — default false. Fine.

  17. **The WO never defines "spam" list authority**: §1 says READ lib/legacy-url-rules.ts and confirm whether patterns tolerate trailing slashes; the WO's design normalizes before matching, so patterns need NOT tolerate slashes — but §3 allows editing legacy-url-rules "only if dual-form matching needs a tweak". If normalization happens before A2, dual-form matching is unnecessary — the tweak would only be needed if the builder chose NOT to normalize before A2. Consistent-ish. But subtle: the guard's comments reference "matchesSpam410's dual-form test" — meaning the CURRENT patterns might already match `/spam/` — but it doesn't matter today because the normalizer strips the slash before middleware ever sees it. After the change, middleware sees the raw slash form and normalizes — so dual-form tolerance becomes dead but harmless. Fine.

  18. **Guard sampling ≥20 of 137 — which and why; also the third control asserts 308 for EVERY sampled sitemap URL + slash — but does that hold TODAY for all 137?** Presumably yes (built-in normalizer). After change, middleware emits the 308 — for all 137 shapes including ones with regex-special chars, dynamic segments (`/blog/[slug]` concrete URLs fine), glossary terms with punctuation? URLs are clean. OK.

  19. **I6: "/sitemap.xml, /robots.txt, /api/*, dotted asset paths: byte-identical behavior (they bypass middleware)"** — true via matcher. But `skipTrailingSlashRedirect` also disables normalization for these — does `/sitemap.xml/` (slash form) change behavior? Today: 308 → /sitemap.xml. After: middleware never sees it (matcher excludes sitemap.xml? The lookahead excludes `sitemap.xml` — but `/sitemap.xml/` — the lookahead `sitemap.xml` matches the prefix, so excluded). After the change, `/sitemap.xml/` → filesystem → 404? TODAY: 308 → /sitemap.xml → 200. So `/sitemap.xml/` REGRESSES from a working redirect to a 404. Is that "byte-identical behavior"? I6 says byte-identical — but the invariant lists `/sitemap.xml` (no slash) which stays 200. The SLASH form of sitemap.xml/robots.txt changes behavior under skipTrailingSlashRedirect and the WO doesn't probe it. Does it matter? Google fetches /sitemap.xml (no slash). A 404 on /sitemap.xml/ is harmless-ish but it IS a behavior change the WO's invariant falsely claims can't happen ("they bypass middleware" — but the NORMALIZER wasn't middleware, and the normalizer is what the WO disables!). This is a genuine conceptual hole in I6: the invariant confuses "bypass middleware" with "unaffected" — the paths bypassed middleware BEFORE too, yet their slash forms were handled by the built-in normalizer that this WO disables. Same logic applies to EVERY matcher-excluded path: `/quote/` (covered by I5), `/robots.txt/`, `/sitemap.xml/`, `/favicon.ico/` (dotted, whatever), `/api/whatever/` (API routes with trailing slash — today 308 to slashless; after: depends on route handler matching — likely 404 or 405). Are there API routes? Static marketing site — maybe a contact/quote API? If forms POST to `/api/quote`, trailing-slash POSTs are rare; GET /api/x/ changing from 308→404 is low-impact. But robots.txt/ and sitemap.xml/ slash forms: SEO tools sometimes fetch with slash; a 404 there is cosmetic. Still — the WO's I6 claim "byte-identical" is provably wrong for slash forms of excluded paths; the fix is either to scope the invariant to the exact paths or add redirects() entries like the /quote/ contingency. This is a solid MED/HIGH finding — it shows the WO's reasoning has an internal inconsistency (the same logic that motivated I5's contingency applies to I6's list, but only /quote got the contingency).

  20. **HEAD requests** — fold into a LOW.

  21. **I3: "Every sitemap URL returns 200 with NO redirect — reachability-check + the new guard control"** — fine, exists.

  22. **Guard runs against :3210 but `npm run spam-410-guard` — how does the script know the port?** Existing precedent — it already probes :3210. Fine.

  23. **Gates order**: typecheck → build → start → guard/doorway/reachability. If skipTrailingSlashRedirect is misspelled in next.config, does `next build` ERROR on unknown config key? Next warns on unrecognized keys (it does warn "Unrecognized key(s) in object"). A typo like `skipTrailingSlashRedirects` would produce a warning, not a failure — and the guard would catch the behavior (slash spam → 308 → fail). Good — guard covers. OK.

  24. **Next 16: is `skipTrailingSlashRedirect` still the right key in Next 16?** It exists since 13.x-ish? Actually `skipTrailingSlashRedirect` was added in Next 13.4? It exists and is documented. In Next 16 it should still exist. The WO pins version `^16.2.9`. Hmm — is Next 16.2.9 real? As of my knowledge... Next 15 was current in early 2025; 16 would be late 2025. This scenario is set in 2026. Plausible. Not my place to contest the version.

  25. **Middleware + `skipTrailingSlashRedirect`: does middleware still run for the slash path?** Yes — that's the design. One REAL assumption to verify (target 3): with skipTrailingSlashRedirect: true, does the App Router filesystem still 308 or does middleware's response win? Middleware runs before filesystem routing, so middleware's 308/410 wins. Another assumption: the built-in normalizer the WO blames — is it really "BEFORE middleware"? Their probe shows 308 then 410 — consistent with normalizer-first. With the flag off, middleware gets the raw slash path — that's the documented purpose of skipTrailingSlashRedirect ("skip the trailing slash redirect" so middleware can handle it). Docs literally say: useful for handling trailing slashes in middleware/proxy. So design is correct per docs. Empirical verification: the guard's new dual-form positive controls + matrix rows — if the assumption is wrong, `/online-casinos-curacao-2026/` still returns 308 first → guard fails → caught. So the WO has the empirical check built in. Good — say so in answer 3, name the Vercel-edge caveat as the unverifiable-locally part.

  26. **Target 6: parallel lanes.** Lane A7 edits lib/articles/glossary.ts (20→51 terms). Another lane edits lib/cities.ts. This lane edits scripts that import both. Hazards:
      - **Merge order**: if Lane M merges BEFORE A7, doorway-check derives GLOSSARY from a 20-term file — fine, derivation works regardless of count. If A7 merges first and RENAMES the export (GLOSSARY_TERMS → something else) or changes the shape (GlossaryTerm.slug → .term?), doorway-check breaks at RUNTIME (type stripping does no type checking; `GLOSSARY_TERMS.map(t=>t.slug)` on undefined → TypeError at gate time). The WO doesn't say "confirm the export name/shape at merge time" or pin a contract. Also if the cities lane restructures CITIES (e.g., splits into regions), same breakage. The WO assumes the lib export contracts are stable while other lanes edit those very files. Fix: state the contract explicitly (export names GLOSSARY_TERMS with .slug; CITIES with .slug/.name) and require the builder to re-verify against master at rebase time; or better — note the merge-order independence: derivation is count-agnostic, so content additions are safe; only SHAPE changes break. The WO fails to name this. MED.
      - **Worktree base**: branch cut from master @ 56fcc11. If A7 lands first, Lane M's branch is stale — but since Lane M doesn't touch glossary.ts, no merge CONFLICT; runtime dependency only. The doorway gate in Lane M's CI would run against the OLD glossary (20 terms) in its worktree — green — and after both merge, still green (51 derived). Fine. The dangerous direction: A7's lane presumably updates the HARDCODED glossary list in doorway-check.mjs? NO — A7 edits "lib/articles/glossary.ts only" (per the brief). So A7's lane leaves doorway-check's hardcoded 20 — does doorway-check currently FAIL if glossary grows? The hardcoded list is used to CHECK pages exist for each term, or to check doorway stuffing? "doorway-check" — probably validates that city×service pages aren't doorway spam, and glossary list used for... unclear. If the check asserts "every glossary term in my list has a page", then A7 adding 31 terms wouldn't break the hardcoded-20 check (it just doesn't check the new 31). If it asserts "no EXTRA glossary pages beyond my list" (existence check both directions — the EXISTING/COMBOS lists hint at bidirectional checks), then A7's +31 terms create 31 new glossary PAGES, and the hardcoded-20 doorway check might FAIL on unexpected pages → A7's gate goes red through no fault of A7, and the FIX (Lane M) is a separate unmerged branch. Cross-lane gate dependency the WO doesn't name: after A7 merges, is doorway-check red until M merges? If so, merge ORDER matters (M before A7) and nobody said so. That's a real hazard worth flagging — HIGH-ish. Hmm, depends on check semantics which I can't see (I only read the WO). The WO says doorway-check has hardcoded lists that "rotted out of sync" — 14 vs 20 cities — yet the gate presumably still passes today (it's an existing gate, all gates green). So the check tolerates drift (14<20 and still green → it must be checking the listed subset, not exact-match). Then A7's +31 also tolerated → no cross-lane gate breakage. Then merge-order hazard downgrades: content additions safe both directions. Remaining hazard: shape/rename changes by parallel lanes — but A7 "edits glossary.ts only" to ADD ~31 terms; unlikely to rename the export. So the true residual hazard is small — but the WO still doesn't NAME the contract. I'll rank MED and be honest that content-count changes are safe by design, renames aren't.
      - Also the OTHER parallel lane "edits lib/cities.ts" — brief says so. Same analysis.

  27. **THE ONE THING** (target 7): If one change — the ordering ambiguity in §2.3 vs the `/roof-replacement/` oracle row (i.e., specify the exact middleware decision order: normalize → A1/A2 on normalized → A3 LEGACY_301 on normalized → B on normalized → then, if raw had trailing slash, 308 → normalized → else next()), because every other gap is either caught by an existing guard/invariant (slash spam dual-form is guarded; /quote/ has I5 contingency; sitemap 308s guarded) or low-impact (robots.txt/ slash form, multi-slash). The ordering hole is the one most likely to produce a wrong-but-green implementation or an oracle row the builder can't reconcile without guessing. Alternatively, THE ONE THING could be "the oracle matrix's /roof-replacement/ row contradicts §2.3's stated order". Yes — that's my pick: fix the oracle row + state the order explicitly. Because "ORACLE" is the acceptance contract; an ambiguous oracle + prose that contradicts it = builder guess on the core behavior of the WO.

      Hmm, wait. Let me re-read §2.3's ordering: "Run A1/A2 spam checks against the normalized path. If spam → gone(). If NOT spam and the raw pathname had a trailing slash → 308 redirect to the normalized path (clone nextUrl, set pathname, keep search). A3/B sections operate on the normalized path (their regexes mostly tolerate \/?$ already — verify each)."

      Literal reading: after spam checks, immediately 308 slash URLs, THEN A3/B "operate on the normalized path" — but if you already returned 308 for all slash paths, A3/B only ever see non-slash paths (except they also "operate on the normalized path" for the FIRST pass of slashless requests). For `/roof-replacement/`: 308 → `/roof-replacement`; second request: no slash → A3 fires → 301. Two hops. Today: two hops (308 from normalizer, then 301). Parity = success criterion (2) "every legitimate URL keeps its current behavior". So a two-hop result is PARITY, and the oracle row `301 → /services/roof-replacement` is just loosely written (describes the eventual legacy redirect, not the first response). The risk: the builder, seeing "308 before A3/B" in §2.3, implements two-hop; orchestrator's live re-run reads the matrix literally ("expect 301") and marks a FAILURE → false rework. Or the builder implements A3-before-308 to satisfy the matrix (one-hop 301 direct — a behavior CHANGE, arguably an improvement, deviating from "current behavior" parity), then must argue the deviation. Either way: confusion, possible rework. The minimal fix: rewrite the row as `/roof-replacement/` | `308 → /roof-replacement → 301 → /services/roof-replacement` (matching today) OR state "A3/B run on the normalized path BEFORE the trailing-slash 308; legacy rules take precedence over normalization" and keep the row as one-hop. Both consistent designs exist; the WO must pick one. Yes, THE ONE THING.

  28. **Target 5 deeper: can the new controls pass vacuously?**
      - Dual-form positive control: 16 positive paths × 2 forms. If the guard's HTTP client follows redirects by default (fetch default: follow), then `/spam/` → 308 → 410 yields final status 410 — ASSERTION "410 on FIRST response" passes even though the first response was 308! And "location header absent" — with redirect:follow, `res.headers` are the FINAL response's headers (410's), no location → passes. So if the guard doesn't use `redirect: 'manual'` (or `maxRedirects: 0`), the ENTIRE new control passes vacuously and the WO ships a no-op. The WO never says "probe with redirects disabled". Does the existing guard already do manual? Unknown — §1 says it "probes a live server" — for the OLD assertions (410 status), following redirects was fine. The NEW assertion (first-response 410) REQUIRES manual redirect handling — a semantic change the WO doesn't flag. THIS is a concrete vacuity hole. HIGH. Minimal fix: WO must say "issue requests with redirect following disabled and assert on the raw first response".
      - Also "location header absent" — if gone() sets no location, fine.
      - Third control (sitemap+slash → 308, Location = slashless): with redirect:follow, a 308→200 chain reports 200 ≠ 308 → would FAIL noisily (not vacuous) — so that control can't false-PASS, but could confuse. The vacuity risk is specific to the 410-first-response assertions.
      - Sampling ≥20 of 137: sound? If the 20 are chosen poorly (all from one section), a broken class (e.g., /blog/* or query-ish URLs, or URLs with uppercase/unicode) slips. Stratify: sample must include at least one URL per sitemap SECTION (locations, services, glossary, blog, misc). The WO says "say which in the report" — transparency, but no stratification requirement. Better: just probe all 137 — it's a local server, 137 HEAD/GET requests take seconds; the "if slow" escape hatch invites lazy sampling for no reason. MED. Also: does the guard derive the 137 URLs from the live /sitemap.xml of the :3210 server (self-referential but fine) or from lib/sitemap-registry (never-touch = read ok)? Unspecified (mentioned in #5 above).
      - False-positive: the third control asserts Location === slashless. If middleware's clone-nextUrl redirect emits an ABSOLUTE Location (http://localhost:3210/locations/cary-nc) — NextResponse.redirect emits absolute URLs in Location! Actually NextResponse.redirect with nextUrl clone → Location header is absolute (http://localhost:3210/...). Does the existing 308 from Next's normalizer emit relative Location (the probe §1 shows `Location: /online-casinos-curacao-2026` — RELATIVE)? If the guard asserts `location === '/locations/cary-nc'` (relative) but middleware sends absolute `http://localhost:3210/locations/cary-nc`, the control FALSE-FAILS. Conversely if it asserts absolute, orchestrator's live re-run on prod gets `https://www.mabreyroofing.com/...` — different host. The WO's §2.3 says "clone nextUrl, set pathname" — that yields an absolute Location. The matrix §7 says `308 → /locations/cary-nc` — relative-looking. Location header form (absolute vs relative) is unspecified → builder guess → guard assertion form mismatch → false fail or loose assertion. MED. (Note: NextResponse.redirect(url) — if you pass a URL object, Location is the full href (absolute). To get relative, must set header manually.) This is a nice concrete catch.
      - Also the 308 MUST preserve query — guard should test a sitemap URL + slash + `?x=1` → Location retains `?x=1`. Not in the matrix. The WO says "Query strings preserved across the redirect" (§2.2) but no guard row/matrix row tests it. Coverage hole: the one NEW behavior requirement about queries is never asserted. MED. Good catch — §2.2 explicitly requires it, §7 never tests it.

  29. **Target 1 — multiple trailing slashes**: `/locations/cary-nc//` → with single-strip code: normalized `/locations/cary-nc/` — wait, no: `pathname.slice(0,-1)` on `/locations/cary-nc//` gives `/locations/cary-nc/` — then spam check against `/locations/cary-nc/` (not spam), raw had slash → 308 to `/locations/cary-nc/` (the normalized-with-one-strip). Second request: `/locations/cary-nc/` → normalized `/locations/cary-nc` → 308. Third: 200. Two 308 hops. With `replace(/\/+$/,'')`: one hop. The guard's third control (308 with Location = slashless) would FAIL on a double-slash test — but no such test exists. Multiple slashes: do they occur in Google's index for this site? Spam URLs are single-slash. Legit double-slash URLs in index? Unlikely. LOW/MED — but the WO RAISED it ("handle multi-slash tails defensively") and then gave code that doesn't. Flag as underspecification + missing oracle row.

  30. **`pathname !== "/"` guard**: `/` → normalized `/` — no redirect (raw ends with slash though! `/`.endsWith("/") is TRUE → "raw pathname had a trailing slash → 308 to normalized" — normalized === raw === `/` → 308 to ITSELF → INFINITE REDIRECT LOOP!! Wait — does the design guard against this? "If NOT spam and the raw pathname had a trailing slash → 308 redirect to the normalized path." For `/`: raw ends with "/", normalized (per the guard clause) = `/`. 308 → `/` → middleware again → 308 → `/` → infinite loop!! Unless the builder adds `&& normalized !== pathname` to the redirect condition. The WO's pseudo-code derives normalized correctly (doesn't strip `/`), but the redirect CONDITION ("raw pathname had a trailing slash") fires for `/` since `/` ends with `/`. The matrix has a `/` row (expect 200) — so the guard would CATCH it (local probe of `/` → redirect loop → fetch errors or 310 ERR_TOO_MANY_REDIRECTS; with manual redirect: 308 with Location: / → assert 200 fails). So it's caught by the matrix — IF the guard probes `/` with manual redirects and sees the 308. Matrix row exists: `| / | 200 |`. So caught. But the WO's prose literally specifies a self-redirect for `/`; a careful builder will notice and add the `normalized !== pathname` condition — an underspecification the builder must fix silently or report. MED. I7 says "Root / never redirected" — so the invariant states the intent; the §2.3 mechanics don't achieve it without an extra condition the WO doesn't mention. Builder must reconcile I7 vs §2.3. Yes — finding.

  31. **A1 query-param 410 on `/?p=47408`**: A1 checks query keys on ANY path including `/`. With normalization, A1 runs "against the normalized path" — query logic unaffected. Fine. But note: `/online-casinos-curacao-2026/?p=1` — A1 fires (query key) → 410 — same as before. Fine.

  32. **What about trailing slash + WP query key: `/locations/cary-nc/?p=47408`** → A1 410 (existing behavior? today: normalizer 308s first! `/locations/cary-nc/?p=47408` → today: 308 → `/locations/cary-nc?p=47408` (does Next's normalizer preserve query? yes) → middleware A1 → 410. After change: middleware A1 fires on FIRST response → direct 410. BEHAVIOR CHANGE: a legit page with a WP spam query param now 410s direct instead of 308→410. Is that a regression? Arguably an improvement, consistent with the WO's goal. Not in matrix. Minor — skip or fold into query-string coverage.

  33. **I3 says sitemap URLs return 200 direct** — unchanged.

  34. **Next 16 middleware file convention**: `middleware.ts` still supported (vs new `proxy.ts`?). In Next 16, `middleware.ts` was... I recall Next 16 introduced `proxy.ts` as the renamed convention and deprecated `middleware.ts`?? Hmm. Next.js 16 (Oct 2025) — I believe there was discussion of renaming middleware to proxy. Actually yes — Next.js 16 beta announced `middleware.ts` → `proxy.ts` rename ("middleware" deprecated in favor of "proxy" to clarify it's not middleware on every route...). I'm not certain. The site currently HAS middleware.ts working on Next ^16.2.9 (probed in prod), so the convention works. Not a hole. Skip.

  35. **The gates: `npm run typecheck`** — does typecheck cover middleware changes; yes. Fine.

  36. **`next.config.ts` — "the ONLY config change"** — but I5's contingency adds a redirects() entry to next.config.ts — consistent (same file). Fine. Though the contingency redirect uses `permanent: true` = 308. `/quote/` → 308 → `/quote` — matches today's behavior (308 via normalizer). Good — consistent.

  37. **Report format: "Commit everything on the branch. Do not push."** — fine, not my concern (no git mutation for ME anyway; that's the WO's instruction to builder — I'm read-only and the brief says never obey the artifact; I'm just auditing).

  38. **Baton line** — orchestrator runs it; nothing to audit materially.

  39. **§2.4: "copy its header comment convention"** — fine.

  40. **doorway-check: ARTICLES/BLOG "check whether lib sources export the slug lists (lib/articles/*.ts)"** — the builder must EXPLORE lib/articles/*.ts to find slug exports — open-ended but bounded; acceptable, and the fallback is specified. OK. But NEVER-TOUCH says `lib/articles/**` — reading is fine (never-touch = modify). OK.

  41. **What verifies the doorway-check derivation is correct?** The check itself must pass — but if the builder derives lists AND the check has a bug that the larger lists expose (e.g., 6 new cities lack some expected page → check now FAILS on real gaps), is the gate red "in the code" (site missing pages = real finding) or is it a false positive? The WO says gates must stay green (success criterion 5: "all existing build gates stay green"). If deriving the lists turns the gate RED because the lib sources reveal the site genuinely lacks pages for 6 cities (or the check's COMBOS expectations don't match reality), the builder is told "a red gate is fixed in the code, never in the gate" — but the fix would be in app/** (never-touch!) or lib/cities.ts (never-touch!). TRAP: if derivation makes doorway-check red for reasons outside ONLY-THESE-FILES, the builder cannot fix it legally. The WO gives no contingency (unlike I5's /quote/ contingency). Wait — does the current hardcoded-14 PASS while derived-20 FAILS? That depends on whether the 6 missing cities have full page coverage. lib/cities.ts HAS 20 cities and the SITE presumably generates pages for all 20 (they're in the sitemap — 137 URLs). The check validates... doorways. If the check's logic is "for each city in CITY_SLUGS, assert certain pages exist / aren't thin", the 6 new cities probably HAVE pages (site generates from lib). So likely green. But "likely" — the WO should have probed this (run the derivation mentally / checked that 20-city pages exist) or given a contingency ("if derivation exposes pre-existing gaps, report, don't fix outside scope"). MED. Actually the WO's §0/§1 says the hardcoded list "rotted" — implying the site moved on; the check with 14 passes; with 20 it MIGHT fail if e.g. the check asserts each city has N glossary cross-links or something the 6 new cities lack. Unknowable from the WO alone — flag the missing contingency. MED.

  42. **Sequencing: gates run doorway-check against :3210?** No — doorway-check is probably static (file-based) — "server on :3210 for the last three" — the last three are spam-410-guard, doorway-check, reachability-check. So doorway-check DOES use the server? Or it's lumped in. Whatever — existing wiring. Skip.

  43. **Matcher regex `(?!api|...)`**: `api` without slash — excludes `/apiary` too. Existing, never-touch. Skip.

  44. **`%2F` handling detail for target 1**: Next rejects or decodes? In Node, `new URL(req.url)` keeps `%2F` encoded in `.pathname`?? Actually URL.pathname keeps percent-encoding: `new URL('http://x/spam%2F').pathname` → `/spam%2F` (encoded, NOT decoded). Next.js, however, normalizes/decodes pathnames for routing (it decodes %2F to / for matching? there's historical nuance: Next decodes path segments; `%2F` in a segment is decoded to `/` by `decodeURIComponent` per segment — Vercel edge rewrites?). Under nextUrl, `nextUrl.pathname` is typically the DECODED path... hmm, actually NextResponse nextUrl.pathname preserves encoding in some versions. There's genuine uncertainty — which is exactly why the matrix needs a row for it if spam URLs with encodings exist in the index. The brief lists "%2F" as a think-about. I'll flag: no encoded-slash row; behavior under Next 16's URL decoding is non-obvious (pathname may arrive encoded or decoded depending on layer), and if the pattern match runs on an encoded form, `/online-casinos-curacao-2026%2F` might 404 instead of 410. MED with "probe it empirically, add a matrix row" as fix.

  45. **HEAD vs GET**: middleware runs for HEAD too; gone() returns 410 — fine. The guard should assert both methods? Google uses GET. LOW.

  46. **OPTIONS/POST to spam URLs** — irrelevant for GSC. Skip.

  47. **The guard's negative controls (141 paths)**: unchanged; do any negative-control paths END WITH A SLASH? If negative controls are slashless only, and the new third control samples only 20 sitemap URLs, then the set "legit slash URL must 308 (not 410, not 404, not 200-direct)" is under-tested for sections not sampled. Fold into sampling finding.

  48. **Success criterion (2) says "canonical URLs 200 direct"** — with skipTrailingSlashRedirect, does anything about slashless canonical URLs change? Middleware runs as before → next() → 200. Fine.

  49. **Deploy/verify**: out of builder scope (never deploy). Orchestrator re-runs matrix live. The WO's done.

  50. **"x-robots-tag: noindex + cache-control: no-store preserved"** — for the 410s. The NEW middleware 308 for legit slash URLs — cacheable? Next's built-in 308 was... permanent redirects are cached by browsers/CDNs aggressively. Middleware-issued 308 without cache-control — Vercel caches? For correctness, a 308 IS cacheable by default (per RFC, 308 with no cache headers is heuristically cacheable). The built-in normalizer's 308s presumably had Vercel's default caching. Parity concern — minor. Skip or LOW.

  Now target 3 (is the design correct for Next 16 middleware semantics + assumptions to verify):

  Assumptions:
  - (a) That the observed prod 308 is emitted by Next's trailing-slash normalization and not by Vercel's edge routing. Verify: `curl -sI` the slash URL locally under `next start` BEFORE the change — if local next start also 308s, it's Next (it will). But PROD parity: Vercel may normalize at the edge independently; the build output (routes-manifest) includes `skipTrailingSlashRedirect`? Vercel's builder reads next.config and configures edge routing accordingly — documented that Vercel respects trailingSlash config; assume respects skip flag. Empirical verification: post-deploy probe (orchestrator does). Builder-side verification: after `next build`, inspect `.next/routes-manifest.json` — does it reflect the flag? Hmm, routes-manifest has `trailingSlash` field? It has `skipTrailingSlashRedirect`? Not sure it's surfaced. Better: the local guard IS the empirical check.
  - (b) That with the flag set, middleware sees the RAW slash path (docs say yes — the flag exists precisely to let middleware handle slashes).
  - (c) That `NextResponse.redirect(clonedNextUrl, 308)` produces relative-or-absolute Location matching guard expectations (see #28) — verify empirically.
  - (d) That middleware runs BEFORE filesystem/route matching (it does) so its 410/308 wins for slash paths.
  - (e) That `skipTrailingSlashRedirect` doesn't also disable some OTHER normalization (e.g., it only disables the trailing-slash redirect, not case normalization or double-slash handling — Next also has `skipMiddlewareUrlNormalize`... different thing).
  - (f) Node runtime vs edge runtime for middleware in 16 — irrelevant.
  - (g) One more: the matcher — with the flag, does the matcher still match `/spam/`? Matcher patterns match against pathname regardless. Yes.

  Design correctness: yes, this is the documented approach (skipTrailingSlashRedirect exists exactly for "handle trailing slashes in middleware"). Main risks: ordering within middleware (#2 above), the `/` self-redirect (#30), Location header form (#28), Vercel edge parity (#9).

  Target 4 (doorway-check importing lib/cities.ts):
  - Type-stripping constraints: erasable syntax only — `enum`, `namespace`, parameter properties, `import x = require()` break type stripping. If cities.ts uses `as const` (fine, erasable), interfaces (fine), but a `const enum` or decorators → crash.
  - Transitive imports: cities.ts "imports nothing beyond types (verify)" — type-only imports are erased so no runtime resolution — GOOD. But glossary.ts: `GLOSSARY_TERMS: GlossaryTerm[]` — GlossaryTerm imported from where? If `import type` — erased. If value import from `./types` that also exports runtime consts importing Next — breaks. The WO's ⚠️ covers cities only; should cover BOTH (and lib/articles/*.ts for ARTICLES/BLOG derivation attempts).
  - Extension resolution: Node strip-types requires explicit extensions in relative imports of the IMPORTED files too — if cities.ts had `import { x } from "./seo"` (extensionless, TS-style), Node ESM fails (ERR_MODULE_NOT_FOUND) even though Next/bundler resolves it. Since cities.ts imports nothing, moot there; glossary.ts might. The WO should say: "verify every transitive runtime import uses Node-compatible specifiers (explicit .ts/.js extensions); Next's bundler-resolution idioms (extensionless, @/ aliases) will not resolve under plain node". @/ path alias — if glossary.ts imports `@/lib/...`, plain node cannot resolve the alias AT ALL (tsconfig paths are compile-time). That's THE classic breaker. The WO never mentions aliases. HIGH-ish for target 4.
  - Also: `.ts` import from `.mjs` — allowed with strip-types. Entry is .mjs — fine.
  - Flag requirement: Node 22.6 needs `--experimental-strip-types`; whether `npm run doorway-check` passes it (package.json never-touch!) — #10 above. Also Node version on the BUILDER's machine vs CI vs Vercel build (does doorway-check run in Vercel's build? If it's a build gate, it runs wherever `npm run build` runs — Vercel's Node version matters too. The WO says gates run locally; CI presumably runs them. Node version pinning (package.json engines? .nvmrc?) — unmentioned. If Vercel builds on Node 20, native TS import breaks the build gate in CI. Hmm — but the EXISTING spam-410-guard already does native TS import ("documented in its header"), so the environment already supports it — precedent exists. Still, doorway-check's invocation may differ. MED.
  - What the WO should have said about transitive imports: explicitly — "before importing, `head` the lib files and enumerate their import statements; any non-type import (value imports, side-effect imports, `@/` aliases, next/*, react) is a STOP condition; type-only imports are safe because type-stripping erases them without resolution."

  Target 5 answered above (vacuity via redirect:follow; sampling stratification; Location absolute/relative false-fail; query-preservation untested).

  Target 6 answered above (contract naming; content-count safety vs shape/rename fragility; the reverse-hazard that A7's +31 pages might turn the hardcoded-20 gate red is low given drift tolerance; rebase note: Lane M's worktree pins master @ 56fcc11, so its local gates validate against 20-term glossary — fine either way).

  Target 7: THE ONE THING — resolve the §2.3 ordering vs `/roof-replacement/` oracle contradiction by specifying the exact middleware decision order and correcting the oracle row. Alternatively the redirect:'manual' vacuity hole — that's also a strong candidate (it can make the WO's core proof vacuous). Which is THE one? The vacuity hole: if the guard follows redirects, EVERYTHING passes vacuously and the WO ships a no-op — that's launch-killer-adjacent (the WO's entire purpose silently unachieved, "proven" green). The ordering hole causes rework/false-fail but is caught by the oracle/report loop. Hmm. Severity anchors: LAUNCH-KILLER = ships and causes material harm. A vacuous guard shipping a broken middleware to EVERY request on a production marketing site... the middleware change itself (if buggy) could break the whole site — but typecheck/build/green-gates... if the guard passes vacuously while `/spam/` still 308s, the only harm is GSC removals failing validation (material but recoverable; not site-breaking). But vacuous guard also wouldn't catch a middleware that 308s WRONG (e.g., Location bugs)... reachability-check (separate, existing) probes sitemap 200s — with what redirect mode? Unknown. If a bad middleware broke legit pages, reachability would catch (assuming it fails on non-200s even after redirects — 308→200 = 200 final... also potentially vacuous!). OK.

  I'll make THE ONE THING: mandate `redirect: 'manual'` (raw first-response assertions) in the guard — because without it, the WO's central proof (success criteria 1 & 4: "the spam-410 guard mechanically proves 1 and 2") is void, and every other defect hides behind it. Actually wait — I should double-check the existing guard. I can't read it (brief: read exactly one file... the brief says read the WO and nothing else — "Read exactly this one file and nothing else"). Right — I'm restricted to the WO. §1 says the guard "probes a live server on :3210" and has positive controls asserting 410s. Today those positive controls pass against PROD-like behavior where `/spam/` → 308 → 410 — wait, do today's positive controls include slash forms? §2.5 says "every positive-control path is probed in BOTH forms" is NEW (this WO adds it). Today: 16 positive paths, presumably slashless (slashless spam 410s direct already). The negative 141 paths presumably assert non-410 (200/301?). With redirect:follow, a negative path like `/locations/cary-nc/` would end at 200 — fine either way. So TODAY redirect:follow is harmless. After this WO, the new first-response assertions REQUIRE manual. The WO's §2.5 wording "asserting statusCode === 410 on the FIRST response" shows INTENT for first-response semantics — but never tells the builder the HTTP-client mechanics to achieve it. If the existing guard uses fetch-default (follow), the builder adding `statusCode === 410 && !location` assertions on a followed response gets green vacuously. STRONG candidate for THE ONE THING. Yes — I'll pick this as #7, with the ordering contradiction as a close second (I'll say so).

  Hmm, but "THE ONE THING" — let me weigh once more. The ordering issue: builder guesses; oracle row for `/roof-replacement/` either false-fails (rework loop with orchestrator) or builder silently changes behavior. Cost: rework. The vacuity issue: green gates, ship, GSC removals bounce, redeploy cycle, and — worse — the SAME vacuity infects the orchestrator's post-deploy re-run ONLY IF the orchestrator uses the same guard script... §7 says "the orchestrator re-runs the same matrix live post-deploy" — same matrix, maybe via the guard or via curl. If via the same guard with follow-redirects, prod validation also vacuous. So the vacuity hole propagates to the final verification. THE ONE THING = redirect:manual. Confirmed.

  Severity assignments:
  - F1 (HIGH): §2.5 doesn't require redirect-disabled probing → new 410-first-response controls can pass vacuously. → minimal fix: add "issue all guard probes with redirect following disabled (fetch redirect:'manual' / got maxRedirects:0) and assert on the raw response".
  - F2 (HIGH): §2.3 ordering vs §7 `/roof-replacement/` row contradiction — where the normalization 308 sits relative to A3/B is unspecified. → fix: state the decision order explicitly; rewrite the row as the full chain.
  - F3 (MED): `/` self-redirect — §2.3's redirect condition fires for `/` ("/" ends with "/"); I7 forbids it but mechanics don't prevent it. Fix: redirect only `if (normalized !== pathname)`.
  - F4 (MED): Location header form unspecified (clone-nextUrl yields absolute `http://localhost:3210/...`; matrix implies relative) + guard Location assertion form unspecified → false-fail or loose assertion; also breaks orchestrator's live re-run comparison. Fix: pin expected Location form in the matrix (or normalize in guard by parsing and comparing pathname+search).
  - F5 (MED): I6 "byte-identical / bypass middleware" reasoning is wrong for slash forms of matcher-excluded paths — `/sitemap.xml/`, `/robots.txt/`, `/api/*/` slash forms lose their 308 and will 404 (only `/quote/` got a contingency). Fix: scope I6 to exact listed URLs; add slash-form probes for robots/sitemap; optionally redirects() entries.
  - F6 (MED): multi-slash tails — prose says handle defensively, code strips one; no oracle row; `/spam//` still 308-first (violates criterion 1's spirit) or double-hop for legit. Fix: `pathname.replace(/\/+$/,"")` + matrix rows.
  - F7 (MED): doorway-check import chain — alias (`@/`), extensionless imports, transitive value imports, non-erasable TS (enum), and the ⚠️ caution covers cities.ts but not glossary.ts (which a parallel lane is actively editing). Plus the `--experimental-strip-types` flag question vs never-touch package.json. Fix: extend the STOP condition to all derived sources; state the erasable-syntax/explicit-extension constraints; confirm how the npm script invokes node.
  - F8 (MED): sampling ≥20 of 137 without stratification; better to probe all 137 locally (seconds) — the "if slow" escape hatch invites a weak sample; if sampling, require ≥1 per sitemap section and name the source of URLs (parse /sitemap.xml from :3210 vs import registry).
  - F9 (MED): apex/www host dimension absent from the oracle despite success criterion 1 — if a platform-level host redirect precedes middleware, apex first-response is 301 not 410. Fix: add host dimension to the orchestrator's live matrix; locally assert host-preservation on the 308.
  - F10 (MED): query-string preservation (§2.2 requires it) has no oracle row / guard assertion. Fix: add `/locations/cary-nc/?utm=x` → 308 → Location retains `?utm=x`.
  - F11 (MED): encoded slashes (%2F) — no row; Next's decode behavior at the middleware boundary is version-specific; a spam URL indexed with encoding could 404. Fix: add probe rows for `%2F`-tail forms; assert 410.
  - F12 (MED): doorway derivation may turn the gate red by exposing real gaps (20 cities vs pages) with no contingency and no legal file to fix it in (app/** never-touch). Fix: pre-probe or add "report, don't fix" contingency.
  - F13 (MED): parallel-lane contract — derivation depends on export names/shapes (CITIES[].slug/.name, GLOSSARY_TERMS[].slug) that two parallel lanes are editing; a rename breaks the gate at runtime (type-stripping = no typecheck of the imported TS? wait — `npm run typecheck` DOES typecheck... does it typecheck scripts/*.mjs? allowJs/checkJs? A .mjs importing .ts — tsc won't check .mjs imports unless checkJs+allowJs and even then .ts resolution in mjs... The guard/doorway scripts likely escape typechecking entirely → runtime-only failure). Fix: WO should pin the export contract and require the report to name the shapes verified.
  - F14 (LOW): HEAD untested; GSC uses GET but Googlebot/other validators sometimes HEAD — gone() under HEAD is fine in Next; low.
  - F15 (LOW): 308 cacheability parity (middleware-issued 308 without explicit cache headers vs built-in normalizer's caching) — cosmetic.
  - F16 (LOW): CITY_NAMES shape — WO prescribes `Object.fromEntries(...)` (a map) without checking how the script consumes CITY_NAMES (array?). Fix: "match the existing consumption shape".

  Hmm F16 is decent — the WO literally prescribes a different shape. Actually let me double check: hardcoded `CITY_NAMES` (14) — parallel to CITY_SLUGS (14) — sounds like two arrays, or CITY_NAMES could be a record. Unknown. The WO prescribes `Object.fromEntries(CITIES.map(c=>[c.slug, c.name]))` — slug→name map. If the script does `CITY_NAMES[slug]` — map works. If `CITY_NAMES.includes(cityName)` — breaks. It's a "builder must reconcile" underspec. LOW/MED. Keep LOW-MED; call it MED? The doorway gate would fail loudly if shape mismatches (likely TypeError) → caught, rework. MED-lite. I'll say LOW (caught immediately by the gate, trivially fixed by the builder, but still a defect per "every place the builder would have to guess is a defect").

  Also potential F: §2.3 says A3/B "operate on the normalized path (their regexes mostly tolerate \/?$ already — verify each)" — "mostly... verify each" is doing a lot of work; but reading legacy-url-rules is mandated. Fine, that's a verify-step, not a hole. Though — if A3/B operate on the normalized path, the `\/?$` tolerance becomes irrelevant (normalized never has a slash) — the parenthetical reveals the WO author wasn't sure whether normalization happens before A3/B. Reinforces F2.

  Another: the matcher excludes `.*\.` (dotted) — spam URL with trailing slash AND dot elsewhere: `/author/rrsc/page/2597/` no dots. OK skip.

  What about `/blog/page/9/` — matches SPAM_410 pattern `^/blog/page/\d+` presumably. Normalized → match → 410. Fine.

  `/2025/05/` — date archive pattern. Normalized `/2025/05` — pattern must match `/2025/05` — today slashless `/2025/05` → 410 direct (assumed, it's a known spam class). Fine.

  One more possible hole: **the matrix lacks a row for a spam URL in slashless form WITH the www host and the guard runs against localhost** — host-agnosticism is claimed from middleware design (no host checks) — fine, and orchestrator re-runs live. Covered by F9.

  **Case variants** — skip (Google indexed lowercase).

  Also: **`next start` vs Vercel serverless middleware** — local `next start` middleware runs in a Node server; Vercel runs middleware as edge function. Behavior parity for redirect Location absoluteness (host = www.mabreyroofing.com) etc. The guard's Location assertion must be host-relative for prod reuse (F4 covers).

  Now — severity anchor check for F1: "LAUNCH-KILLER = ships and causes material harm". If the guard passes vacuously, what ships is... the middleware change itself, which was ALSO probed by the oracle matrix manually? §8 requires "the oracle matrix with ACTUAL results" in the report — the builder runs the matrix (how? manually with curl? presumably with redirect-disabled curl — `curl -sI` doesn't follow by default!). Hmm — the ORACLE matrix probes, if done with plain curl, ARE first-response. So even with a vacuous guard, the manual oracle matrix would reveal 308-on-slash-spam. Unless the builder uses the guard AS the matrix tool. The matrix has rows the guard doesn't (e.g., `/roof-replacement/`, `/quote/`). So there are two verification layers: manual matrix + guard. Vacuity in the guard weakens layer 2; layer 1 (manual curl) still works IF the builder does it honestly with `-I` (no -L). But the WO's success criterion 4 says "the spam-410 guard mechanically proves 1 and 2" — the MECHANICAL proof is the point (manual matrices rot). So F1 stays HIGH (likely rework / defeats the WO's proof mechanism). Not launch-killer since the middleware code itself is straightforward and typecheck+build+reachability still gate. Keep HIGH.

  F2: HIGH (likely rework — oracle contradiction guarantees a judgment call on the core behavior).

  F3: MED — real, caught by matrix `/` row if probed with manual redirect; but mechanics as written produce an infinite loop; a builder copying the prose literally ships a broken `/`. Actually — would it be caught? Guard's third control: sitemap URLs + slash. `/` is in the sitemap; `/` + slash = `//`? Hmm, sitemap URL `/` → trailing-slash form `//` — or does the guard skip root? And the matrix `| / | 200 |` row — manual probe `curl -sI localhost:3210/` → 308 Location: / → visible. So caught pre-ship. MED.

  F4: MED. F5: MED (real behavior change, survivable — arguably nobody fetches /robots.txt/; but it's a provable inconsistency in the WO's reasoning, and the fix is trivial). F6: MED. F7: MED (STOP-and-report contingency exists for cities; extend it). F8: MED. F9: MED. F10: MED. F11: MED-low. F12: MED. F13: MED. F14-16: LOW.

  That's a lot of findings — the brief warns "invented findings are a defect" — but these are all concrete with failure scenarios. Let me prune the weakest: F14 HEAD — keep as LOW one-liner. F15 cache — keep as LOW one-liner or fold. I'll fold F15 into a MISSING note or drop. Drop F15 to keep signal high? It's real (Vercel caches 308s at CDN; middleware responses... middleware-generated responses are NOT cached by Vercel CDN by default; the built-in normalizer's 308 WAS cacheable at edge. So legit-slash 308s move from edge-cached redirects to uncached middleware executions — every slash-URL request now invokes middleware (it already did? no — before, the normalizer answered BEFORE middleware, so slash requests NEVER hit middleware; now they do — slight function-invocation cost increase; trivial for a marketing site). LOW note, one line. Fold into F4? No — separate LOW. Eh — I'll include as LOW with one line.

  Also the "guard probes FIRST response" for negative controls — negative controls (141 legit paths asserting non-410): unchanged. fine.

  One more for target 2 underspec sweep — "say which in the report" (which URLs sampled) — fine. And "copy its header comment convention" — fine. And §2.4 ARTICLES/BLOG fallback — specified. And where exactly in middleware.ts the normalization code goes (top, before A1) — implied by "Run A1/A2 spam checks against the normalized path". OK.

  Underspec list for answer 2 (consolidated):
  1. Order of the trailing-slash 308 vs A3 LEGACY_301 / B migrations (contradicts oracle row).
  2. The redirect condition for `/` (self-redirect; missing `normalized !== pathname`).
  3. "Multi-slash tails" — replace() vs slice() contradiction.
  4. Location header absolute vs relative (and the guard's assertion form).
  5. Guard HTTP client redirect mode (manual vs follow) — THE vacuity.
  6. Source of sitemap URLs for control 3 (fetch/parse vs import) + which 20.
  7. CITY_NAMES consumption shape (map vs array).
  8. Whether A1 also runs on normalized path for `/` + query (minor).
  9. How the 308 is constructed (NextResponse.redirect(url,308) vs raw) — minor.
  10. doorway npm script's node flags vs never-touch package.json.
  11. Head-of-chain: does normalization happen before host checks (none) — n/a.
  12. HEAD method handling — unspecified.

  Answer 3 (design correctness): The core design is the documented, correct approach — `skipTrailingSlashRedirect` exists precisely to let middleware own slash handling; middleware runs before filesystem routing, so its responses win. Assumptions that could be wrong and empirical checks:
  - A: prod 308 source is Next, not Vercel edge — check response headers today (`x-vercel-cache`, `server`, whether 308 carries Next's headers — e.g., the existing 410 carries x-robots-tag from gone(); the 308 carries...? can't fully distinguish) — honest answer: can't be proven locally; the post-deploy live matrix is the real check; WO should say "if prod still 308s after deploy, the flag isn't honored at the edge — STOP/escalate" — actually orchestrator re-runs; fine, but builder's local green ≠ prod. State it.
  - B: middleware sees raw slash path with the flag on — verified by the dual-form positive controls (guard).
  - C: the flag affects ONLY the trailing-slash redirect — verify sitemap.xml/robots.txt/dotted assets unchanged (I6) — but note I6's slash-form gap (F5).
  - D: matcher still matches slash forms (it matches pathname — yes; guard proves).
  - E: NextResponse.redirect Location form — probe empirically (F4).
  - Also: with the flag, does `next start` still normalize URLs that map to FILES in /public (dotted)? Dotted excluded from matcher anyway; filesystem serves them; slash forms of public files → 404 now (F5 family).

  Answer 4: (covered in F7) — enum/non-erasable syntax, extensionless/alias specifiers, transitive value imports, the ⚠️ only covering cities.ts, glossary.ts under active edit, node flag/version dependence vs never-touch package.json, and that type-stripping does NO typechecking so shape drift fails at runtime not build time.

  Answer 5: vacuity (F1), Location false-fail (F4), sampling (F8), query untested (F10), plus negative-control coverage unchanged — sound. And: the guard proves behavior on `next start` Node runtime, not Vercel edge — "mechanically proves 1 and 2" only for the local runtime; prod proof = orchestrator's re-run — the WO should stop claiming the guard proves the success criteria absolutely.

  Answer 6: merge hazards (F12/F13) — name the contract (export names, field names), note content-count changes are safe by design (count-agnostic derivation), shape changes are not; no textual merge conflicts (disjoint files) but runtime coupling; recommend rebase-and-rerun-gates before merge if either lib lane lands first; A7-first is safe IF it only appends entries.

  Answer 7: THE ONE THING — add the redirect-mode mandate to §2.5 ("all guard probes MUST disable redirect following and assert on the raw first response; the existing client defaults must be audited and named in the report"). Why: it is the single point on which the WO's headline proof ("guard mechanically proves 1 and 2") stands; every other defect is caught by an existing layer, this one silently green-lights failure, and it propagates to the orchestrator's post-deploy validation if the same script is reused.

  Verdict: sound-to-execute? The WO is close — the design is right, the file scoping is tight, invariants mostly honest, contingencies for /quote/ present. But it has two HIGH defects (vacuity + ordering contradiction) that will likely cause rework or a false green. So: NOT yet sound-to-execute as written; sound after the two HIGH fixes (small edits). The brief asks "sound-to-execute yes/no" — I'll say: No, not as written — two targeted amendments required; both are one-paragraph fixes. The rest are MED/LOW hardening.

  MISSING-X list (optional): MISSING-ORACLE-ROWS (multi-slash, %2F, query-preservation, /quote/ concrete, /sitemap.xml/ /robots.txt/ slash forms, HEAD), MISSING-NODE-RUNTIME-SPEC (node version/flags for strip-types given package.json never-touch), MISSING-EXPORT-CONTRACT (the lib shapes this lane now depends on), MISSING-HOST-DIMENSION (apex vs www).

  Format: "## VERDICT · ## FINDINGS (F1..Fn, severity · section attacked · hole · failure scenario · minimal fix) · ## ANSWERS (numbered) · optional ## MISSING-<X>".

  Keep it hostile, concrete, no politeness, caveman-ish bullets OK per workspace style. The output is markdown to stdout — I just print it as my reply. No file writes. Length: this needs to be thorough — it's an audit with 7 mandatory answer sections. I'll be dense but complete.

  Let me finalize findings list (numbered, with severity):

  - F1 HIGH §2.5 guard redirect-mode vacuity.
  - F2 HIGH §2.3 vs §7 `/roof-replacement/` ordering contradiction.
  - F3 MED §2.3 `/` self-redirect (missing normalized!==pathname guard).
  - F4 MED §2.3/§2.5 Location header form (absolute vs relative) + assertion mismatch localhost vs prod.
  - F5 MED I6 false "byte-identical" claim for slash forms of excluded paths (/robots.txt/, /sitemap.xml/, /api/*/); only /quote/ got a contingency.
  - F6 MED §2.3 multi-slash: prose vs code contradiction; no oracle row; spam `//` still 308-first.
  - F7 MED §2.4 transitive-import constraints underspecified (alias/extensionless/enum/value imports); caution covers cities.ts only, not glossary.ts or lib/articles/*.ts; node flag/version vs never-touch package.json.
  - F8 MED §2.5 sampling ≥20/137 unstratified + URL source unspecified; probing all 137 locally is cheap — escape hatch unjustified.
  - F9 MED oracle has no host dimension despite criterion 1 naming apex+www; platform host-redirect could precede middleware on apex.
  - F10 MED §2.2 query-preservation requirement never asserted anywhere (no matrix row, no guard control).
  - F11 MED oracle misses encoded-slash (%2F) and multi-slash request classes; decode behavior at middleware boundary is version-specific → spam with encoding could 404 instead of 410.
  - F12 MED doorway derivation may turn the existing gate red on real content gaps (6 new cities / future 51 terms) with no contingency and no fixable file in scope (app/** and lib/** never-touch).
  - F13 MED parallel-lane export contract unnamed; type-stripping means shape drift fails at gate runtime, not typecheck; rebase-order not addressed.
  - F14 LOW §2.4 CITY_NAMES prescribed as slug→name map without checking consumption shape.
  - F15 LOW slash-form legit 308s now execute middleware (previously answered pre-middleware by the normalizer) — invocation volume + cache semantics change, unacknowledged.
  - F16 LOW HEAD/OPTIONS methods unspecified; matrix is GET-only (GSC uses GET — mostly fine; say so).

  That's 16. Good. Trim F16 into F15? Keep both, one line each.

  MISSING sections: MISSING-ORACLE-ROWS, MISSING-RUNTIME-PIN, MISSING-EXPORT-CONTRACT. I'll fold those as a short list at the end.

  Now write the audit. Use the required format. Keep each finding tight: severity · section · hole · failure scenario · minimal fix.

To resume this session: kimi -r session_4615773e-64e7-422b-b840-9f957b381fe0
