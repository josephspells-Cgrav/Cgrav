# DISPOSITION LEDGER — Kimi audit of PLAN_TAKEOFF_MVP_20260807 (OS48, 2026-08-07 ~5:10am ET)

Audit: `KIMI_OUT_TAKEOFF_MVP_20260807-0433.md` — VERDICT "not sound as written" · 25 findings
+ 12 MISSING-CONTRACTS. Companion probe receipt: `PROBE_TAKEOFF_PDF_20260807.md` (P1-P7),
which falsifies one of MY plan's premises Kimi inherited: the set is **17 pages, not 122**
(P1), and the text layer Kimi demanded probing (its ONE THING) was probed CONFIRMED
born-digital before the audit landed (P2/P3). Where a disposition says "mooted-in-part," the
mooting evidence is the probe, not a re-grade of Kimi's severity.

| F | Sev (as found) | Disposition | Reason |
|---|---|---|---|
| F1 | LAUNCH-KILLER | **ACCEPT** | Verified against `line-items/route.ts` — zod `lineSchema` has no `meta`, insert maps listed columns only; first autosave after apply wipes every flag. Fix exactly as prescribed: `meta` passthrough in schema + insert + client round-trip + regression test apply→PUT→meta-intact. Owner: WO-T1 (per F19). |
| F2 | HIGH | **ACCEPT** | The stale-tab interleaving is real and unguarded by my updatedAt check. Fix as prescribed: revision token — PUT body carries `baseUpdatedAt`; a single conditional `UPDATE estimate_projects SET updated_at=now() WHERE id=? AND updated_at=? RETURNING id` gates BOTH the PUT and apply (0 rows → 409 + refetch). Single-statement atomicity is the neon-http house idiom. |
| F3 | HIGH | **ACCEPT** | The guard self-defeats: autosave rewrites the compared timestamp, and re-apply zeroes typed prices — Sean's Home Depot walk destroyed by the intended flow. Fix: provenance guard (`meta.runId` + `meta.extractedQty` vs current qty detects hand-edits) + pinned merge rule: re-apply preserves `materialCents`/`laborCents`/`sort` on lines matched by `meta.assemblyKey`+label; `--force` re-applies qty but STILL preserves prices. Pinned in Appendix A5. |
| F4 | HIGH | **ACCEPT-MODIFIED** | The 2×2-vs-4×6 contradiction was real (my R8 admitted it). Modification: P1+P2 collapse the economics Kimi computed on 122 pixel-first pages — 17 pages, text-first, vision only for symbol/geometry sheets (~5 sheets on this set). Pinned grid (Appendix A10): 200 DPI native, 1568px tiles, 8% overlap (7×5=35 tiles max on symbol sheets; 150 DPI/12 tiles geometry; 0 tiles text-only sheets). Per-sheetType budgets pinned. |
| F5 | HIGH | **ACCEPT (already executed)** | Its ONE THING. Probed BEFORE the audit landed: born-digital, 0 zero-text pages, median 3,011 ch/pg; sheet map + area schedule + 224 dim strings + window tags extracted deterministically (P2-P5). Architecture re-ordered text-first; vision reserved for symbol counts + spatial association; per-page raster fallback when text yield ≈ 0, labeled in the report. Node implementation: `pdfjs-dist` legacy build `getTextContent()` with positions (python pypdf scripts remain as probe receipts only). |
| F6 | HIGH | **ACCEPT** | Formulas consumed params S3 never emits — the builder-guess class the WO exists to kill. `paramsJson` pinned verbatim (Appendix A1) incl. `shellSystem` first-class. Bonus receipt: pg 9 legend text ("HYBRID BARNDOMINIUM PERIMETER WALL… WOOD FRAMING BETWEEN THE GIRTS… ENGINEERED METAL BUILDING SHELL REQUIRED" + 8"/6"/4" wall legend) means `shellSystem` and wall classes are TEXT-extractable on this set, not just flaggable. |
| F7 | HIGH | **ACCEPT** | Real double-count: PT seal plate booked in #2 AND inside #4's "3×". Pinned split (Appendix §3 rev): #2 = PT seal 1×extLF · #4 = top plates 2×extLF + full 3×intLF. Sean's "3× perimeter" arithmetic survives as 1+2 across two lines. |
| F8 | HIGH | **ACCEPT** | Schedule-authority silently drops tag-only (revision-added) types — a missing-line generator inside the dedupe design. Union-with-provenance pinned. On THIS set the point is sharper: NO schedule exists (P5), tags ARE the source — the rule matters for every future set. |
| F9 | HIGH | **ACCEPT-MODIFIED** | Resume/abort semantics were absent. Modification: at 17 pages text-first a full run is minutes and low single-digit dollars, so `--resume` is cheap insurance not survival gear. Pinned anyway (Appendix A9): run row created at start (`status=running`), per-assembly incremental report writes, `aborted` status + partial report, S1/S3 outputs cached by (sha256, page, stage), concurrency 4, per-call timeout 90s. |
| F10 | MED | **ACCEPT** | Slab-only concrete under-counts perimeter-footing designs. Footing term added: perimeter × width × depth from C1-1/section text when present, else flagged default (12"×20"), always a separate line so the flat-slab number stays clean. |
| F11 | MED | **ACCEPT** | Zero waste factors = systematically-low vs Sean's hand method, misread as extraction error. `WASTE_DEFAULTS` pinned (studs 5% · sheathing/decking 10% · drywall 10% · roofing 10% · flooring 8% · else 0), applied in qty, "(incl N% waste)" in label, net qty preserved in `meta.extractedQty`, flagged as assumption. |
| F12 | MED | **ACCEPT** | Vaulted ceilings break `heatedSF` for insulation/drywall. `ceilingSurfaceSF` param added; vault detection from notes/section text; flagged default = heatedSF when unread. |
| F13 | MED | **ACCEPT** | Aggregate-ceil drops the +1 starter per segment. Per-segment `ceil(LF×12/16)+1` over the walls[] list; corners param extracted-or-flagged (default 4 + flag). |
| F14 | MED | **ACCEPT** | Unpinned rounding = golden tests that can't pin string forms. `QTY_SCALE` pinned: ea/lf/sf integer half-up · cy/sq 2dp half-up · ls 1. Stored via `toFixed(scale)`. |
| F15 | MED | **ACCEPT** | An all-flagged run passing SC1 is a usefulness hole. Report prints flag-share per assembly; SC1 amended: >6 assemblies at 100% flag-share = FAILED usefulness gate; counted vs derived accuracy NEVER blended into one number. |
| F16 | MED | **ACCEPT** | Within-class additivity (enlarged plans, multi-page schedules) is the dedupe trap my cross-class table missed. Canonical-sheet rule pinned + THIS set's partition (Appendix A13): tags ← A1-1b only · dims ← A1-1a + A1-3 merged by segment key (never additive) · rooms/areas ← pg 7 · symbols ← E1-1/P1-1. |
| F17 | MED | **ACCEPT** | `contactId` NOT NULL + script-has-no-session were unpinned. PROBED: house pattern is `DEFAULT_ORG` (src/lib/org.ts; org columns default to it; prod scope = NULL-or-DEFAULT_ORG). Pinned: script stamps DEFAULT_ORG explicitly; `--contact-id` or `--create-contact "<name>"` required; Maass is Sean's real client in Sean's real CRM — prod, not demo data. |
| F18 | MED | **ACCEPT** | Without `meta.runId` + source identity, F3's guard is unimplementable and runs across re-exports are indistinguishable. Added: `meta.runId`, `takeoff_runs.sourceFilename`, `sourceSha256`. |
| F19 | MED | **ACCEPT** | Apply-route ownership collided (T1 lib vs T3 §4 routes; F1 amendment orphaned). Pinned: T1 owns apply lib + takeoff-runs routes + the PUT meta/token amendment; T3 consumes only. |
| F20 | MED | **ACCEPT** | Three implied line-item writers. Pinned one writer: `applyTakeoffRun()` lib called by S6, the POST route, and the CLI; first run = extract → stage → apply (guard trivially green) → report. |
| F21 | LOW | **ACCEPT** | Free wins verified against the house loop's capabilities: `cache_control` on static system/schema blocks, per-call AbortSignal 90s, max 3 retries, dated `PRICES` table in one config. |
| F22 | LOW | **ACCEPT** | T2's reconcile tests had no fixture author — circular-test risk. I author T2's fixture JSONs + expected outputs verbatim into the WO, same as T1's golden fixture. |
| F23 | LOW | **ACCEPT** | Meta-serving GET shape unpinned + CSV import strips meta. Pinned: project GET line serializer includes `meta`; CSV import documented meta-less by design (import creates hand lines, not extraction lines). |
| F24 | LOW | **ACCEPT** | Rasterizer choice left to builder = two-lib risk. Pinned: `@hyzyla/pdfium` (BSD); gate 0 renders pg 15 (E1-1) at 200 DPI on Windows and HALTS on failure with mupdf as the named fallback — a reported decision, not a silent swap. |
| F25 | LOW | **ACCEPT** | E-sheet symbol counting unbudgeted + $0-vs-unpriced ambiguity. Tiles/sheet budget pinned per sheetType (A10); `meta.unpriced=true` on extraction-seeded lines renders as blank "—", never $0.00. |
| C1-C12 | (contracts) | **ACCEPT all 12** | Every missing contract becomes plan **Appendix A1-A12 verbatim** (paramsJson · reportJson · meta keys · PUT amendment · apply semantics · CLI · citation shape · truth-yaml · status enum `running\|complete\|aborted` + `appliedAt` · tile budgets · defaults↔flag-text keys · org/contact). A13 added by me: this set's canonical-sheet partition (F16). |

**Score: 25/25 findings + 12/12 contracts dispositioned · 23 ACCEPT · 2 ACCEPT-MODIFIED · 0 REJECT.**
The two modifications both cite the same class of evidence (the 17-page/text-first probe) applied
to different findings — F4's tile economics and F9's resume scale — with distinct pinned outcomes.

**Re-baton:** Kimi's verdict prescribed amend-then-re-baton. FIRED on the amended plan
(v2 appendix) as `KIMI_{BRIEF,OUT}_TAKEOFF_MVP_V2_*`; builders launch only after its
disposition. Carried deliberately: none — every finding landed in the plan or a WO contract.
