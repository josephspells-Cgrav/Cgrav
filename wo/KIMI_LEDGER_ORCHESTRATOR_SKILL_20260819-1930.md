# DISPOSITION LEDGER — orchestrator skill CORRECTED re-baton (KIMI_OUT_ORCHESTRATOR_SKILL_20260819-1915.md)

Context: the 1900 baton was fired at a STALE pre-edit draft (author error — brief copied
without re-pointing the artifact path). Its F1-F4/F6/F7/F10 were against bytes already fixed
in v2. NEW LAW captured from the incident: **a baton receipt certifies BYTES, not intent —
the brief's artifact path is part of the receipt; verify it points at the installed file
before firing.** The 1915 re-baton audited the real installed file (v3). All 11 findings
dispositioned below; v4 applied every one. Genuinely-new 1900 findings (F5 judge-injection,
F8 judge packet, F9 activation, F11 WO-outside-worktree, F13 frontmatter) were applied in v3.

| F | Sev | Disposition | v4 change |
|---|---|---|---|
| F1 | HIGH | ACCEPT | The old token list "(lean/judge/gambit/thorough)" sat one sentence above the new pinned-token rule — a literal adjacent-sentence contradiction, plus token-scan vs intent. v4: old list deleted; skip fires only when LEAN/PLUS-JUDGE is used AS the mode declaration (intent test, "keep the copy lean" is incidental); doubt = ASK. |
| F2 | HIGH | ACCEPT | "UNAVAILABLE per the deadman rule" referenced a rule this file never stated — the floor's only non-Joseph hatch, self-declarable. v4: deadman inlined (ERR silent >30min → one re-fire → dead again or 45min = UNAVAILABLE; each spawn logged; one transient error is not unavailability); marker format pinned to `(a|b) + grounds`. |
| F3 | HIGH | ACCEPT | A cleanly-rebased lane merged on OLD green gates = an untested tree wearing a receipt. v4: re-cut/rebase = NEW TREE — law 4 re-runs, step 6 re-fires on floor lanes, re-cut recorded. |
| F4 | HIGH | ACCEPT | Rollback restored code, never schema; a drop/rename migration + rollback = the rollback creates the outage. v4: migrations must be expand/contract backward-compatible with the live deployment; WO names the rollback plan; post-7a rollbacks state schema disposition. |
| F5 | MED | ACCEPT | "The diff" was never pinned; uncommitted-fix scenario = gates certify a tree the merge doesn't take. v4: diff = `git diff <deploy-tip>...<lane-branch>` committed state; `git status` clean before gating AND before merging. |
| F6 | MED | ACCEPT | Untrusted-content warning was judges-only while findings traveled verbatim into fix-round builder prompts. v4: warning goes in every fix-round prompt; orchestrator strips/neutralizes embedded imperatives; findings sanity-checked on merits, not only invariants. |
| F7 | MED | ACCEPT | Lens menu, "major", LEAN loop end, round cap all undefined. v4: extended menu (perf/cost, data-integrity, a11y, legal — or invent+record); MAJOR defined; LEAN ends when re-read+re-gates surface nothing new; cap 3 fix rounds then escalate to Joseph with history. |
| F8 | MED | ACCEPT | The floor keyed off an undefined, unrecorded classification — a motivated session guesses narrowly. v4: "Floor surfaces touched: <list|NONE>" is a required WO field, probed at draft, quoted in the report; writes-into-customer-pipelines count; unrecorded = FLOOR. |
| F9 | MED | ACCEPT | Two-file typo on a floor surface force-fired the whole regime. v4: ON scoped by risk (behavior-changing floor edits), trivial multi-file floor edits may run inline under announce-OFF, floor intact. |
| F10 | LOW | ACCEPT | Yolo-vs-ask and "you decide" deadlocks. v4: mode question binds under yolo (design question, not permission prompt); "you decide" = recorded waiver → LEAN-with-floor; unanswered overnight ask = wave doesn't start (delta-0). |
| F11 | LOW | ACCEPT | Pre-dispatch WO baton + pre-deploy paranoia lived only in COMPOSITION while "the numbered steps ARE the law". v4: both folded into steps 2 and 8 as numbered duties. |

REJECTED: none. Cycle complete: draft → baton(v1,14 findings) → fix → Joseph mode-edit →
re-baton(stale-file error, new law) → corrected re-baton(11 findings) → v4. Next EDIT to the
skill re-batons; this disposition-application does not (the PLAN_FINAL pattern — a baton
cycle ends at the dispositioned artifact, no dialogue loops).
