• Reading the five permitted files in the worktree now.

• ## VERDICT

  **No — not sound to execute as written.** The WO's load-bearing premise ("location pages carry ZERO structured data — PROBED: no JsonLd import anywhere under `app/locations/`") was probed at the wrong layer: `app/locations/[city]/page.tsx` is a 35-line shell that delegates all rendering to `components/CityPage.tsx`, and `lib/schema.ts:236-238` documents in its own comment that `REPLACEMENT_PRICE_RANGE` is "used by the Service nodes on the service-detail pages **AND the city pages**" — strong evidence city pages already emit schema from inside the component tree the probe never looked at. If so, the builder adds a second graph on top of an existing one and every listed gate can still pass. On top of that, the two string inputs the graph needs (name, description) exist only inside `generateMetadata`, the WO doesn't say how to derive them, and the exemplar it points to actively misleads (its `webPageNode.name` is *not* its metadata title). Fix the premise and the string sourcing before dispatch; the compliance guardrails themselves (no aggregateRating, no second LocalBusiness, the locationNode trap) are well-handled.

  ## FINDINGS

  **F1 — HIGH — The "no JsonLd today" probe didn't cover where the rendering actually lives.**
  `page.tsx` renders only `<CityPage city={c} />` (line 34). Any existing JsonLd emission would be in `components/CityPage.tsx`, which the WO's probe ("no JsonLd import anywhere under app/locations/") cannot see — and `lib/schema.ts:237-238` says Service nodes are already used on "the city pages," implying `CityPage` (or something it renders) already calls `serviceNode`. Failure scenario: builder wires the instructed graph into `page.tsx`; pages now emit two ld+json blocks — pre-existing Service (+ possibly WebPage/FAQPage) nodes plus the new graph. I1 only asserts the page "gains exactly ONE new block," and the §7 oracle asserts *presence* of the three @types, never *uniqueness* — so duplicate WebPage nodes (two different `@id`s, possibly conflicting names) ship green. Duplicate WebPage/FAQPage on one URL is exactly the kind of entity-noise the WO's own LocalBusiness prohibition is designed to avoid. Minimal fix: before dispatch, read `components/CityPage.tsx` and grep `components/` for `JsonLd`/`lib/schema` imports; state in the WO what the rendered page emits today; extend I1/oracle to assert total block count and no duplicate @types across all blocks on the page.

  **F2 — HIGH — Title/description sourcing is underspecified, and the exemplar teaches the wrong lesson.**
  The strings live only inside `generateMetadata` (page.tsx:20-27): title = `` `${c.titleKeyword ?? "Roofing Contractor"} ${c.name}, NC | Mabrey Roofing` ``, description = `c.metaDescription ?? <fallback template with BUSINESS.phone>`. The WO says `name: <the page's existing title phrasing>` and `description: <the city metaDescription or its existing fallback>` but never says *how* the page body obtains them. The builder's options are all bad guesses: (a) fork the expressions into the default export — two copies drift apart on the next title tweak, and the graph's `name` silently stops matching the SERP title; (b) hoist a shared local function inside page.tsx — a refactor the WO neither sanctions nor forbids; (c) follow the exemplar literally — but the exemplar does NOT mirror its metadata title (`What Is ${t.term}? | Mabrey Roofing` in metadata vs `${t.term} — Roofing Glossary` in the graph, glossary page.tsx:31 vs 59), so "follow the exemplar" and "existing title phrasing" contradict each other. Failure scenario: three builders produce three different `name` values; the oracle can't catch any of them because it never asserts `name`. Minimal fix: WO pins the exact expressions (copy them from `generateMetadata`) or explicitly instructs hoisting one shared derivation inside page.tsx, and the oracle asserts graph `name === metadata title`.

  **F3 — MED — `<slug>` is ambiguous against the `-nc` URL scheme; the correct helper is sitting unused in the file.**
  page.tsx's own comment (lines 9-11): the canonical scheme is `/locations/[city]-nc`, the param carries the `-nc` suffix, and URLs are built via `locationPath(c.slug)`. The WO writes `url: /locations/<slug>` and breadcrumb `/locations/<slug>` without defining whether `<slug>` includes `-nc` or naming `locationPath()`. Failure scenario: builder template-strings `/locations/${c.slug}` → WebPage `@id`/`url` and breadcrumb item 3 point at `/locations/cary` while the page lives at `/locations/cary-nc`; the oracle GETs the same wrong URL and either 404s (caught, good) or follows a middleware redirect and validates a graph whose canonical URLs are all wrong (shipped, bad). Minimal fix: WO says "build all URLs with `locationPath(c.slug)` (already imported in the file)."

  **F4 — MED — The breadcrumb middle item is probably wrong, and there may be no visible trail to mirror.**
  The page.tsx comment records a "Phase-0 migration **from** /service-areas/[city]" — the WO's proposed middle crumb `Service Areas /service-areas` points at the scheme the site migrated away from. The WO hedges ("probe it"), which is good, but the deeper problem: page.tsx renders no `Breadcrumbs` component at all (unlike the exemplar, which renders the visible trail at lines 89-93). Whether location pages show any visible breadcrumb is unknowable from the audited files — it depends on `CityPage`. Google's breadcrumb guidance expects markup to reflect a visible trail; emitting `Home / Service Areas / Cary` when the page visibly shows something else (or nothing) is a mismatch. The oracle asserts only item 3's name, so a 404/redirecting item 2 ships green unless `reachability-check` happens to crawl schema URLs (unknown). Minimal fix: WO requires the crumb trail to match the trail `CityPage` actually renders; oracle asserts item 2 URL returns 200.

  **F5 — MED — The FAQ-dedup safety claim is asserted, not verified, and the obvious collision is sitting in lib/faqs.ts.**
  The dedup law (lib/faqs.ts:48-60) is about questions shared between the /faq hub and dedicated topic pages — a *global* namespace. The WO argues city FAQs are safe because they're "per-city unique (doorway gate enforces textual uniqueness)" — but per-city uniqueness says nothing about collision with the *site-wide* set, and lib/faqs.ts:13 contains the literal question "How much does a new roof cost in Durham?" A Durham (or any) city FAQ with near-identical text marked up in a second FAQPage violates the one-block-per-question law the file documents. Nothing in the gates or oracle diffs city questions against `TOP_FAQS`/topic-page questions. Failure scenario: one collision among 60-80 city questions → two FAQPage blocks site-wide carrying the same question, the exact condition `FAQPAGE_FAQS` exists to prevent. Minimal fix: add an oracle step asserting the union of all city `faqs[].q` is disjoint from lib/faqs.ts questions (and pairwise disjoint across cities).

  **F6 — LOW — I6 ("markup mirrors rendered text") is unverifiable by the stated oracle.**
  The oracle asserts `mainEntity` count equals `faqs.length` but never asserts each Q&A actually appears in the rendered HTML. If `CityPage` truncates, rewords, or renders only a subset of `city.faqs`, the markup violates Google's FAQ-content policy while every gate passes. Minimal fix: oracle greps the rendered HTML for each question string.

  **F7 — LOW — The WHY overstates the SERP payoff for FAQPage.**
  lib/schema.ts:320-323 says it in the codebase's own voice: "FAQPage + HowTo are DEAD in Google… faqNode is retained (zero harm, parsed by non-Google AI) but is NOT a Google rich-result lever." The WO's framing ("SERP presentation of the 20 highest-value local pages") is true for breadcrumbs, decorative for FAQ. Not a blocker — the node is harmless — but the orchestrator should know which third of the graph is actually earning its keep.

  **F8 — LOW — "ZERO structured data" is imprecise even if F1 comes back clean.**
  lib/schema.ts:314-318 documents that the org node is "emitted in the root layout" — every page on the site, locations included, already carries the canonical RoofingContractor/Organization node (and likely WebSite). The pages lack a *page-specific* graph, not structured data. Harmless wording, but it's the same file-level-vs-rendered confusion that produced F1.

  ## ANSWERS

  **1. Underspecification sweep.** Four places the builder must guess: (a) `webPageNode.name` — "the page's existing title phrasing" with no derivation given and an exemplar that contradicts it (F2); (b) `description` — "metaDescription or its existing fallback," where the fallback is a template embedding `BUSINESS.phone` that exists only inside `generateMetadata` (F2); (c) the breadcrumb middle item — the WO's own candidate (`/service-areas`) is likely stale post-migration, and "follow the existing convention" doesn't name where that convention lives (F4); (d) `<slug>` vs the `-nc` param scheme, with `locationPath()` available but unmentioned (F3). A fifth soft guess: whether `city.faqs` elements typecheck as `{q,a}` — the WO says so, but `lib/cities.ts` was outside my permitted reads, so I could not confirm `CityFaq`'s shape.

  **2. Collision read of page.tsx.** The file is a thin shell: `generateStaticParams` from `LOCATION_PARAMS`, `generateMetadata` via `pageMetadata`, default export rendering `<CityPage city={c} />`. Nothing in *this file* collides — no existing JsonLd, no schema import — so the WO's literal claim ("no JsonLd import under app/locations/") is true of the directory. But the claim is answered at the wrong layer: all rendering (including any existing schema, the `.seo-answer` block, and any visible breadcrumbs) lives in `components/CityPage.tsx`, which the probe excluded and which lib/schema.ts's own comments implicate as already emitting Service nodes on city pages (F1, F8). The `.seo-answer` existence claim ("PROBED" per §1) is likewise unverifiable from the page file — it can only have been probed in the component or in rendered HTML. The `[sub]` route is correctly fenced off; no collision there. `generateMetadata` is untouched by the plan, but its string logic is the unresolved dependency of the whole graph (F2).

  **3. Signature match.** The WO's node plan matches the actual contracts: `webPageNode({url, name, description, speakable?: string[]})` (schema.ts:198) — matches, speakable becomes `SpeakableSpecification.cssSelector`; `breadcrumbNode(items: {name, url}[])` (224) — matches the WO's three-item list; `faqNode(faqs: {q, a}[])` (281) — matches `city.faqs` as described. All four WO-cited line numbers (198/224/281/328) are accurate. Critically, `webPageNode` references the org and website by `@id` only (`about`, `isPartOf`) — it mints no entity, so the instructed graph cannot create a duplicate-business merge. The `locationNode` conditional instruction is sound *and its read resolves deterministically*: `locationNode` (328) mints `@type: ["RoofingContractor", "LocalBusiness"]` with its own `@id: <url>#business`, name, and telephone — precisely the LocalBusiness-type node the WO says to reject. So the correct decision is DO NOT USE, the graph is exactly three nodes, and I4's "pure Place node" escape hatch does not fire. One residual trap: the helper's doc comment ("A location-specific RoofingContractor sub-entity (WS-B location pages)") tells the builder it was *built for these pages*, directly opposing the WO's prohibition — the WO's text is explicit enough to win, but expect the builder to flag the contradiction rather than silently obey.

  **4. Compliance.** The instructed three-node graph is clean on the two hard laws: none of `webPageNode`/`breadcrumbNode`/`faqNode` can emit `aggregateRating`, and none mints a LocalBusiness/Organization/RoofingContractor entity (only `@id` references to the canonical org node from the root layout). The real compliance holes are around, not in, the node set: (a) FAQ-dedup is verified only per-city, not against the site-wide question namespace where at least one city-plausible question already lives in markup (F5); (b) markup-mirrors-rendered-text (I6, Google's FAQ policy) has no mechanical check (F6); (c) entity-merge risk re-enters through the back door if F1 is real and `CityPage` already emits nodes the WO doesn't know about. Note also the codebase's own stance that FAQPage is dead as a Google rich result (schema.ts:320-323) — zero harm, near-zero SERP value (F7).

  **5. THE ONE THING before dispatch.** Read `components/CityPage.tsx` and grep `components/` for `JsonLd` and `lib/schema` imports, then rewrite WO §1's "CURRENT BEHAVIOR (PROBED)" with what the *rendered* location page actually emits today. Everything else — the three-node plan, the prohibitions, the locationNode rejection — is executable as written once that premise is confirmed (or repaired), but if `CityPage` already emits Service/FAQ/WebPage nodes, the WO as written manufactures duplicate-entity markup on the site's 20 highest-value local pages while every gate stays green. While you're in there, fold F2's exact title/description expressions and F3's `locationPath(c.slug)` into §2 — they cost the drafter two lines and save the builder three guesses.

