• I need to read all the files fully: SKILL.md, doctrine.md, output-template.md, and the three .bak files. Plus possibly MODEL.md for context (mentioned but audit focuses on the three). Let me read them in parallel. Files could be long — I'll page through.

• Now read the three .bak files.

• I have all six files. Also MODEL.md was mentioned as governing client-facing numbers; the audit prompt says MODEL.md is assumed context. It might help to check whether MODEL.md mentions location math or audit/plan modes, but the audit is scoped to the three files. The prompt asks specifically about contradictions involving "audit/plan modes' location math" — audit-page.md and method.md aren't in scope for full read, but the audit is about whether new text conflicts with surviving old text in these files. I could peek at audit-page.md/method.md for location math, but the instruction says audit only these files. I'll work with what's here; the "radius-driven Inputs section" is in SKILL.md itself.

  Now let me actually do the audit. Key items:

  **Contradiction checks:**

  1. **§17 tiering vs §8 location content floor vs §16 page-count sanity.**
     - §8 says: "Location pages: unique local content (real projects in that town, local specifics)... If you can't write something real for a town, it's a county-hub mention, not a page." That's consistent with §17's fold/exclude. Actually §8 already has the seed of evidence-gating. No direct contradiction — §17 formalizes it. But subtle: §8's floor includes "real projects in that town" — Tier-2 evidence-woven pages have thin book; do they satisfy §8's floor? §17 Tier-2 weaves neighborhoods as completed-work mentions, so arguably yes. Not a major contradiction.
     - §16: "Only the location-page count is capped by the market's town count." §17 says roster is decided by book, not radius/town-count. §16's framing still says location count scales with town count; §17 says it's book-gated — towns in radius without book support get folded/excluded. These are compatible if read as "cap is town count, actual is book" — §16 says "capped by", which is an upper bound, not a mandate. So not a hard contradiction, but the surviving text in §16 could be read as "every town in the market gets a page" which §17 now forbids. Also the output template's old "Cities (M): <list>" is replaced with tiered list — good. But §16's line survives untouched: "Only the location-page count is capped by the market's town count." A future session could read §16 and list all towns in the radius. Addendum preamble says "where they conflict with older text, they win" — that resolves it legally, but the exploitable seam remains since nothing in §16 points to §17. MINOR/MAJOR? The addendum has a global supremacy clause ("they win"), so a strict reading resolves it. But audit target #1 explicitly asks. I'd call it a MINOR (covered by the supremacy clause, but no cross-reference).

  2. **"Book decides the roster" vs radius-driven Inputs.**
     - SKILL.md Inputs: "Market: anchor city + radius (default 30mi)". Stage-1 pull 5: "Geo triangulation ... for the radius". The audit/plan location math: audit-page.md/method.md not in scope, but SKILL.md Stage-1 #7 says "live SERPs on 2–3 money terms per hub city" — hub cities come from the radius. Now the book may exclude towns in the radius or include towns outside the radius (book contradicts claimed service area). Conflict between "radius defines the market" and "book defines the roster": which owns the location axis in the audit and plan artifacts? The new pull #9 is marked "(architecture mode...)" only — so audit/plan artifacts' location math remains radius-driven. The audit page (client-facing) presumably lists opportunity towns within radius; the architecture says the book decides. The audit could promise pages/towns the book can't support → the audit closes the deal on a roster the architecture then cuts. This is exactly audit target #5. MAJOR: addendum laws bind only architecture; audit/plan location math untouched, and pull #9 is architecture-mode-only, so in `all` mode (default) the audit runs before... wait, `all` mode = audit → plan → architecture sharing Stage 1. Stage 1 includes pull 9 only in architecture mode? The pull list is Stage-1 shared. Pull #9 is tagged "(architecture mode, when the client has any job history)" — like #6 "(audit + plan modes)" and #8 "(audit mode...)". In `all` mode presumably all pulls happen. But in `all` mode, audit is produced FIRST, and does it use the book? The audit reference (audit-page.md) wasn't edited; no instruction says the audit's town lists or projections incorporate book evidence. So in `all` mode, the audit page may name a roster that the architecture (built later in the same run, with the book) contradicts. That's a real internal-consistency break for a skill whose whole premise is "all internally consistent because every number comes from one model card and one data layer." MAJOR.

  3. **Pull #9 vs "pull ONCE" data-layer law.**
     - "Stage 1 — THE DATA LAYER (pull ONCE; every artifact reads it)". Pull #9 is tagged "(architecture mode...)". In architecture-standalone mode, fine — pull 9 happens in Stage 1. But wait: is pull #9 a Stage-1 pull? Yes, it's item 9 in the Stage-1 list. The "pull ONCE" concern: no contradiction — it's in the shared pull list. But there's a subtlety: pull #9 requires "the client's CRM/job list" — that's not an MCP pull, it's client input. It sits in a list of paid API pulls labeled with [M]/[MOD] tags; #9 has no [M]/[MOD] tag and no tool named. Minor inconsistency: every other pull names the tool; #9 names none and it's actually client-supplied data, not a "pull" in the data-layer sense. Also: "pull ONCE" — with architecture mode standalone, pulls 1-8 mostly don't happen (mode-tagged), but pull 9 does; is that "Stage 1" without the rest? Missing failure mode: architecture run standalone — does it still need pulls 3-7 for the Leads Projection section of the output template? The output template includes a "Leads Projection (two-tier)" section requiring addressable market ~X/mo — that requires the Stage-1 volume pulls. So architecture-standalone cannot fill its own template's projection section without Stage-1 pulls 2-5, yet pull #9 is the only one tagged for architecture mode. That's a genuine gap: mode tagging implies architecture mode runs only pull 9, but the template demands measured market numbers. MAJOR or MINOR? The user explicitly asks about "architecture mode run standalone without Stage-1 pull 9" — actually the reverse: architecture standalone WITH pull 9 but missing everything else the template needs. And also: what if architecture is run standalone and operator forgets pull 9 — the "No book access → default Tier-2" fallback covers no-book, but nothing says pull 9 is mandatory-attempted vs optional. Actually pull #9 says "(architecture mode, when the client has any job history)" — "any job history" — how do you know without asking? If the client has history but no CRM access granted, "No book access" fallback covers. OK.

     Also: pull ONCE + architecture-standalone: if audit was run last week (Stage 1 done, no book), and architecture is run today as a separate session, the "one data layer" doesn't contain the book — pull 9 never happened because it was tagged architecture-mode. Then architecture mode does its own Stage-1? The skill says modes share Stage 1; standalone architecture presumably runs Stage 1 but only its tagged pulls? Ambiguous. Exploitable: a session could say "Stage 1 already ran; pull ONCE; skipping book." The fallback line "No book access → location pages default to Tier-2 register and the deliverable says so" could be exploited to skip even asking for the book — declare "no book access" and default everything to Tier-2, skipping §17 entirely. MINOR-MAJOR. I'd call the exploitability of the no-book fallback a MAJOR ambiguity (undefined: what counts as "access", who asks, is asking mandatory).

  4. **Undefined thresholds / ambiguities:**
     - "roughly ≥10 jobs" for Tier 1 — explicitly fuzzy ("roughly"). What about 9 jobs? 12? Is 10 per town or per book? Presumably per town (count-led page leads with town's total). Not stated: is the ≥10 per-town? Tier assignment per town presumably by that town's job count. "strong book, roughly ≥10 jobs" — reads per-town but ambiguous. Exploitable: tier the whole roster one way. MINOR.
     - Tier 2: "the total appears NOWHERE (digit or word form)" — good and precise. But what counts as "total"? Only the completed-job count; what about years-in-business or dollars? It says "never per-neighborhood counts, years, or dollars" in Tier 1 context. For Tier 2, only "the total" is banned; are per-neighborhood counts allowed in Tier 2? Presumably the thin book has few; ambiguity. MINOR.
     - Fold-or-keep: "a 1-job town is a fold-or-keep decision for the operator" — operator ownership named, good. But "Weak towns get folded into a neighbor" — who decides weak for 2-9 job towns? Not stated. Partially covered.
     - "No book access → default Tier-2 register and the deliverable says so" — wait, Tier-2 is "evidence-woven" with real neighborhoods woven as completed-work mentions. With NO book, there are no real neighborhoods to weave. Tier-2 register without book = you can't print completed-work mentions at all (they'd be unverified claims under §18). So the no-book fallback as written instructs Tier-2 (which weaves evidence) with zero evidence — contradiction within the new text itself. Tier-2-with-no-book pages would violate §18 verify-or-cut if they mention completed work, and would violate §8 ("if you can't write something real, it's a county-hub mention, not a page"). The fallback should probably be "no location pages beyond anchor/county hubs," not "Tier-2." This is a genuine internal contradiction in the new material. MAJOR.
     - Actually, re-read: "No book access → location pages default to Tier-2 register and the deliverable says so." Tier-2 defined as "evidence-woven, never count-led (thin book): real neighborhoods are woven into prose as completed-work mentions." With no book, no real neighborhoods known → Tier-2 register collapses to zero evidence → pages become templated descriptors = the exact city-swap pattern §8 forbids. Yes, MAJOR.

  5. **Missing failure modes:**
     - Client with zero job history (brand-new business): pull #9 says "when the client has any job history" — zero history → no pull → fallback "No book access"?? Zero history is different from no access. The fallback conflates them. And with zero history, location roster has no evidence basis at all; "or genuine service capacity" in §17 roster line is the only out — undefined ("genuine service capacity" is exploitable: operator can claim capacity for any town and bypass the book law entirely). MAJOR ambiguity: "(or genuine service capacity)" is an undefined escape hatch that guts "the book decides the roster."
     - Book contradicts claimed service area: not addressed. If the book shows jobs only in 3 towns but client claims 30mi radius, which wins for the roster? §17 says book decides, but the audit's market sizing (radius-driven) still promises the whole radius. Tie-in with finding 2.
     - Architecture standalone: covered above.
     - Glossary on thin-vocabulary trade: §21 says "Cap it to real trade vocabulary" — good, and template says ~30–50. For a trade with 15 real terms, fine — cap line covers it. But no failure handling: does a thin-vocab trade skip the axis or shrink it? "Cap it" implies shrink; acceptable. MINOR at most. Also §21 claims "near-zero doorway risk" — fine. Template's "~30–50 terms" vs "cap to real vocabulary" — the range could push padding; the cap sentence mitigates. MINOR.
     - §22 rebuild hygiene: existing domain but NO spam legacy — "starts with a spam-legacy audit" — fine, audit could find nothing. Order locked. What if spam classes overlap legit URLs? "negative control list" covers. OK.
     - What happens when the book exists but client won't share (privacy)? Partially "no access."

  6. **BUILD GATES numbers: calibrate-don't-worship vs false precision.**
     - SKILL.md lock: "The architecture names its own gates — pairwise <40%, delete-the-city-name, reachability ≤2 hops, sitemap floor, scrub table (doctrine §19)." Stated as hard numbers with no calibration caveat. Compare Stage 0: "(Density thresholds are calibrate-don't-worship.)" — the base file models the pattern of flagging soft thresholds, but §19/BUILD GATES carry no such flag. §19 text: "Pairwise similarity <40%... plus delete-the-city-name ≥4 real local signals" — stated flatly, justified by "audited 5 clones..." provenance. Is <40% measured how? Similarity metric undefined (cosine on tf-idf? embeddings? shingle?). "Pairwise similarity" without a metric is unverifiable as a gate — a build can pick a lenient metric and pass, or a strict one and fail everything. That's false precision AND undefined measurement. MAJOR (the gate is unenforceable/unauditable as specified; also §19's "no clause of 8+ words repeated" — across how many pages? any two pages? common boilerplate like CTA text, footer, schema? Site-wide shared components (nav, footer, CTA blocks, FAQ schema) will trivially repeat 8+ word clauses. No exclusion for boilerplate/chrome. The gate as written fails every real build or gets silently scoped away. MAJOR/MINOR — I'd say MAJOR for enforceability, since it's the deliverable's closing block.
     - Also "≤2 hops from home (BFS the nav)" — defined enough. "Sitemap count is a floor gate" — a floor only catches drops, not bloat; fine as stated.

  7. **Audit/plan binding (target #5):**
     - §17 book-decides-roster → audit's upside narrative and plan's roadmap reference location coverage; not bound.
     - §20 pack distance-gating → this is client-facing promise material! The audit page is the close asset and makes projections; honesty rails say "no pack-position promises" (pre-existing). §20 says "Never promise pack presence in secondary towns." Does the audit-page reference get this? audit-page.md not edited; SKILL.md honesty rails line "Modeled-not-promised language; no ranking guarantees, no pack-position promises" already exists — but §20's specific distance-gating law (secondary-town pages are organic plays) affects the audit's projection story for corridor towns — remember Stage-1 #5's adjacent-market MEASURE law: the audit's upside thesis can live in adjacent markets; §20 says those towns' pages don't enter the pack. The audit's tier-1 (pack-heavy, 30-90d FAST lever) projection for corridor towns would contradict §20. New law should bind audit projections; it only appears in architecture context. MAJOR.
     - §22 rebuild hygiene → audit mode does a site crawl (pull 8) for existing sites; the audit's findings should flag spam legacy; currently pull #9's second half ("For an existing domain, also pull the spam-legacy picture") is tagged architecture-mode. The audit of an existing spam-dragged domain would miss the legacy story, and plan roadmap wouldn't schedule 410s. MINOR-MAJOR. Also note: pull #9's spam-legacy half is bundled under the job-book item gated on "when the client has any job history" — a client with an existing domain but NO job history skips pull 9 entirely and the spam-legacy audit never happens, even though §22 says an architecture for an existing domain "starts with" it. Structural bug: two unrelated concerns gated on one condition. MAJOR.
     - aggregateRating removal: SKILL.md locks list includes it; audit pages are client-facing sites — do audit pages use aggregateRating? audit-page.md not in scope. SKILL.md lock binds globally-ish. OK.
     - Verify-or-cut (§18) → audit pages print factual specifics about markets/towns (they do — competitor names, market facts). §18 says "printed on a money page" — scoped to the build. Audit pages are also client-facing factual claims; the verify-or-cut law doesn't bind them. The audit-page.md has its own "content-verify" (curl check) — different thing. MINOR.

  8. **§9 Trust bullet**: now "real reviews in visible copy (no self-serving rating schema — §10)". Fine. §10 supersedes old line explicitly — good, they handled the supersession in place. §14 fixed. Output template "Built Into Every Page" fixed. Consistent. Check for any surviving "AggregateRating" positive mentions: grep mentally — template line 62 "⛔ never self-serving AggregateRating" good; doctrine §9, §10, §14 consistent. SKILL.md lock good. Any leftover? Output-template Leads Projection Tier 1 "site + citations + reviews" — fine.

  9. **Template "Architecture principles" still says:** "A dedicated page on every axis — one per service, system, <type>, and city." "one per ... city" — conflicts with §17 fold/exclude; surviving old text inside the NEW-edited template. The Location Pages section was updated but the principles line at top wasn't. "complete coverage of every service, system, <building type>, and buyer question" + "one per city" reads as every-city coverage — contradicts evidence-gated roster. MAJOR or MINOR? It's the client-facing summary line of the deliverable; the addendum supremacy clause is in doctrine, not template. A build WO reader sees "one per city" + the tiered list — confusing at minimum. I'd call MAJOR (direct contradiction within the same edited file).

  10. **Template Total Scope / glossary:** Total scope line "~A company · B service · C system · D industry · E brand · F location · G resource · ~H project" — does NOT include glossary terms as a count! The Glossary section was added (~30-50 terms) but Total Scope omits it. Also sitemap-count floor gate "<TOTAL> routes" would then mismatch by 30-50. MINOR (arithmetic/consistency bug introduced by the edit).

  11. **"Mabrey" naming in client-facing template?** Template doesn't name Mabrey. Doctrine names Mabrey — doctrine is internal reference, fine. SKILL.md names Mabrey (Stage 0) — internal. Placement rules say client names never touch public repo — these skill files live in ~/.claude/skills, presumably private. OK.

  12. **Pull #9 vs "pull ONCE":** also, the data-layer law says every artifact reads the one pull; pull #9 tagged architecture-only means in `all` mode the audit (built first) technically COULD read it (Stage 1 shared) but no instruction wires it. Same as finding 2.

  13. **§17 "Maximalist sweeps are EVIDENCE-GATED"** vs SKILL.md Inputs radius + Stage-1 #5 union math over radii: the data layer measures the full radius market; the architecture builds only book-supported towns. Then the Leads Projection in the architecture template uses "<market>: ~X/mo" radius-wide demand against a roster covering a subset of towns — mismatch between measured market and addressed geography. No instruction to reconcile (project only what the roster covers?). MINOR-MAJOR. I'll fold into finding about audit/architecture geography mismatch. Keep as separate MINOR.

  14. **Glossary axis vs §16 page-count sanity:** §16 says "~100–160 pages"; glossary adds 30–50 — Mabrey sitemap 137→168 exceeds 160. §16's range survives untouched; §21's proof point (168) exceeds it. Contradiction-ish: a gatekeeper reading §16 might cap at 160 and cut glossary terms; or cite §21's 168. Supremacy clause resolves legally but the texts conflict numerically. MINOR.

  15. **"who owns fold-or-keep decisions":** "a 1-job town is a fold-or-keep decision for the operator" — operator named for 1-job towns only. For 2-9 (Tier-2 thin) towns, and for the general "weak towns get folded," owner unstated. Also fold-or-keep for towns with zero jobs but inside radius with high search volume (strategic target) — §17 roster line "(or genuine service capacity)" might intend to cover expansion towns but undefined. MINOR (partly covered) — merge with escape-hatch finding.

  16. **Tier-1 "roughly ≥10 jobs"** — per-town or per-book? If per-book (e.g., 200 jobs across 19 towns), every town with 2 jobs could go count-led with the town's 2-job total?? No — Tier 1 count-led leads with "the real completed-job total" — presumably the town's total. "strong book" reads as a per-town property in context (Mabrey: towns with many jobs). But text says "(strong book, roughly ≥10 jobs)" — ambiguous unit. Exploitable both ways. MINOR.

  17. **BUILD GATES "Evidence" gate:** "every new factual specific web-verified or cut" — §18 says "An independent web-verifying judge ... is part of the build pipeline." Gate block omits "independent" — minor weakening; fine. Also gates apply to the BUILD, but the architecture deliverable itself contains factual specifics? Template is a page inventory — fine.

  18. **Missing failure mode: book contradicts service area** — covered, make it explicit finding.

  19. **§20 vs plan mode:** plan's off-page ramp includes review velocity; §20 says pack war won near anchor — plans for multi-location/clients... method.md not edited; SKILL.md "multi-location rules" exist. §20 not wired into plan. Part of finding on audit/plan binding.

  20. Also check: SKILL.md pull #9 says "No book access → location pages default to Tier-2 register and the deliverable says so" — AND doctrine §17 says roster: town gets page because book supports one. No book → no towns supported → zero location pages by §17, but SKILL.md says default Tier-2 (implies pages still built for the radius roster!). Direct contradiction: SKILL.md fallback implies location pages still get built (in Tier-2 register) for towns the book can't support, violating §17's roster law. This sharpens finding 4 — the no-book fallback creates exactly the unsupported roster §17 exists to kill. MAJOR.

  Now verdict: AMEND-NEEDED clearly.

  Organize findings numbered, MAJOR/MINOR, exact quotes, then verdict. Keep quotes short. Maybe ~10-14 findings. Let me draft.

  MAJOR:
  1. No-book fallback contradicts §17 roster law + §18/§8 (SKILL.md pull 9 fallback builds Tier-2 pages with zero evidence; §17 says town gets page only if book/capacity supports; Tier-2 register defined as evidence-woven which requires a book).
  2. "(or genuine service capacity)" undefined escape hatch guts book-decides-roster; zero-history client unaddressed; fold-or-keep ownership only defined for 1-job towns.
  3. New laws bind only architecture while audit runs first in `all` mode and is radius-driven: Inputs radius, Stage-1 #5/#7 hub-city math, audit upside thesis in adjacent markets vs §20 distance-gated pack — audit can promise what §17/§20 forbid. Pull 9 tagged architecture-only.
  4. Template "Architecture principles" line "one per ... city" / "complete coverage" contradicts §17 tiered roster in the same file.
  5. BUILD GATES false precision + unmeasurable: "<40%" pairwise similarity with no metric defined, no calibrate-don't-worship caveat (contrast Stage 0's own caveat); "no clause of 8+ words repeated across pages" has no boilerplate exclusion (nav/footer/CTA/schema fail it trivially).
  6. Architecture standalone mode: template requires Leads Projection (~X/mo measured) but only pull 9 is tagged for architecture mode; "pull ONCE / every artifact reads it" + mode-tagged pulls leave standalone architecture with no market numbers, or invite re-pull (violating pull-once) or fabricating.
  7. Pull #9 bundles spam-legacy audit under "when the client has any job history" — existing-domain client with no job history skips §22's mandated first step ("An architecture for an EXISTING domain starts with a spam-legacy audit").

  MINOR:
  8. "roughly ≥10 jobs" unit undefined (per-town vs per-book); Tier-2 ban covers "the total" but per-neighborhood counts/years/dollars ban stated only in Tier-1 bullet.
  9. §16 "Only the location-page count is capped by the market's town count" survives — implies radius/town-count-driven roster; relies solely on addendum supremacy clause; no cross-ref to §17.
  10. §16's ~100–160 page sanity vs §21's Mabrey 137→168 — numeric conflict; supremacy clause resolves but no edit.
  11. Total Scope line omits glossary terms (~30–50) → TOTAL and the sitemap-floor gate undercount by that amount.
  12. Book contradicts claimed service area — unaddressed; also radius-wide market sizing (~X/mo) vs book-gated subset roster unreconciled in the template's Leads Projection.
  13. §18 verify-or-cut scoped to "money page"/build; audit pages print factual specifics and aren't bound (audit content-verify is a curl deploy check, not fact verification).
  14. §22/§17 ownership: scrub-table reviewer — "the reviewer can audit against the book" — reviewer undefined (operator? judge?). Maybe merge into 2. Also glossary thin-vocab trade: cap line mostly covers; note template still prints "~30–50" which invites padding; MINOR or skip. I'll include briefly.

  Also check question 4 explicitly: BUILD GATES numbers stated as false precision — yes, finding 5. Also note SKILL.md lock repeats the numbers flatly.

  One more check: §19 "gate lists DERIVED from the site config" — but architecture deliverable is pre-build; "site config" is build-layer vocabulary; architecture can't derive; fine, gates bind the build. Not a contradiction.

  Also: output-template Build gates "Sitemap count floor: <TOTAL> routes" — includes glossary? covered in 11.

  Also SKILL.md Stage 2 ARCHITECTURE line says "apply doctrine.md IN FULL — the base axes (§1-16) AND the Mabrey-proven addendum (§17-22)" — calls §1-16 "the base axes" while §1-16 aren't all axes; trivial, skip.

  Also: "The book decides the location roster" lock in SKILL.md — but audit mode never reads doctrine; locks are global so fine.

  Verdict: AMEND-NEEDED.

  Format: numbered findings with severity and exact quotes. Keep tight.

To resume this session: kimi -r session_ace1f04f-8f3f-40de-8273-3c8d0bed9331
