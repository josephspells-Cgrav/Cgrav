• Let me read the brief file first.Read the artifact: section "## Z." plus §B step 3 and §G of WO_SEO_LANE_M_410_DIRECT_DOORWAY_DERIVE.md. The brief says "Read nothing else." I should respect that — only read those sections. Let me find the sections. First grep for section headers to locate line ranges.I need to read: §Z (line 217 to end presumably), §B (lines 133-146), §G (lines 182-211). The brief says read ONLY those sections. Let me read them.Now I produce the adversarial audit. Let me analyze carefully.

  The artifact sections:
  - §B: middleware decision order: normalize path (strip trailing slashes; "" → "/"), then A1 WP query-param check (on normalized === "/"), then A2 spam checks against normalized (all rules incl. §Z) → gone(), then A3 LEGACY_301 → 301 direct, then B-section legacy route migrations, then trailing-slash 308, then next().
  - §G: oracle matrix.
  - §Z: two new rule classes.

  Audit targets:

  1. False-positive risk of ≥4-hyphen single-segment rule. Future legitimate root-level slugs with 4+ hyphens. Examples: campaign landing pages like /roof-replacement-financing-options-charlotte-nc (5 hyphens), /free-roof-inspection-storm-damage-claim-help, /2026-spring-roof-tune-up-special, seasonal promos. Marketing/SEO long-tail pages at root level with 4+ hyphens are extremely plausible. The negative control only validates against the CURRENT 137 sitemap paths + current app/ routes. It does NOT protect future pages. The WO claims "no real page — current or plausibly-future — can ever match either rule" per the brief's success criteria. The guard can't prove future-safety. The rule shape relies on an undocumented convention ("we will never create a root-level slug with ≥4 hyphens") that exists only in this WO; a future builder won't know it. This is a real HIGH finding: the success criterion "no plausibly-future page can ever match" is NOT mechanically provable by the negative control as designed — it's a social convention, not a mechanical guard. Minimal fixes: (a) invert to an allowlist shape: 410 only single-segment dotless paths that are ≥N hyphens AND not in a KNOWN_ROOT_ROUTES set — actually better: 410 if hyphen count ≥ 4 AND path not in the enumerated app route set at runtime (matcher can check the route manifest). Actually in Next.js middleware, you can't easily enumerate routes at runtime. Alternative: make the rule data-driven: a build-time generated ROOT_ROUTES constant the rule consults as an allowlist exception; guard regenerates it from app/ at build time so future routes auto-exempt. That's the minimal robust shape: rule = (hyphens ≥ 4) AND slug NOT IN ROOT_ROUTE_ALLOWLIST, where the allowlist is generated from app/ at build time and the guard fails the build if a new root route isn't in it. Or simpler: accept the risk honestly and downgrade the success criterion — state "future root-level slugs must keep ≤3 hyphens; enforced by adding a guard that scans app/ for root-level routes with ≥4 hyphens and fails the build." Actually the guard DOES enumerate every real root-level app/ route directory as negative control — but that's a one-time check at build time of THIS change. If the guard is kept in the test suite (a persistent CI check), then any future route with ≥4 hyphens would trip the guard... wait — would it? The guard validates every rule against the enumeration of root-level app/ routes. If a future dev adds /spring-roof-tune-up-special-offer (5 hyphens) as a root route, the guard (if run in CI) would fail because the rule now matches a real route. That's actually decent protection IF the guard is a persistent test, not a one-time probe. The WO says "The guard's negative control (all 137 sitemap paths + a NEW control enumerating every real root-level app/ route directory) mechanically proves no real page matches." If that guard is codified as a test that runs on every build, then future violations get caught at build time — the dev adds the route, guard fails, dev realizes. That's actually the right shape. But: does §Z say the guard is persistent/CI, or a one-time gate? It says "The guard's negative control ... mechanically proves no real page matches." Present tense, and §6 GATES says "run all, verbatim" — gates are typically run per-change. If the guard script lives in the repo and runs in CI or as a pre-deploy gate, future routes are protected (caught at build). If it's a one-shot validation during this WO only, future routes silently 410. The section doesn't pin persistence. That's a MED-HIGH ambiguity: the WO must state the guard is a permanent test wired into CI/build, and that a future root route with ≥4 hyphens will fail loudly (not silently 410 in prod).

  Also: catch-all edge — what about a future root-level route added as a dynamic segment? E.g., app/[slug]/ root-level dynamic route for blog? Then ANY single-segment path could be real and the ≥4-hyphen rule would 410 real content dynamically. The negative control enumerates route DIRECTORIES — a dynamic [slug] directory has no hyphens so it passes the control while the rule still 410s legitimate CMS slugs served by it. That's a concrete hole: the rule is structurally incompatible with any future root-level dynamic route (/[slug] for blog posts, landing pages from a CMS). For a marketing site this is plausible — actually very plausible for "blog/marketing pages added later." The brief's success criterion says "blog/marketing pages added later under namespaced paths are structurally unaffected" — note "under namespaced paths," so the WO scopes future content to namespaced paths. OK, so the WO does anticipate this: rule applies ONLY to single-segment root paths and future content goes under /blog/ etc. But then the invariant must be stated as a permanent constraint: "no future root-level dynamic route, no future root slug with ≥4 hyphens." If stated as a guard-enforced invariant, fine; the WO doesn't say the guard persists. Finding: MED — persistence of the guard unspecified; rule shape is convention-based, future safety rests on guard running forever. Minimal fix: pin "guard script is committed and wired into CI/predeploy; adding a root route that trips any rule fails the build," plus a code comment at the rule: "before adding a root-level route with ≥4 hyphens or a root [slug] dynamic segment, see §Z."

  Another false-positive vector for rule 1: the hyphen count. "dotless, root-level, single-segment path whose slug contains 4 or more hyphens." Deepest real route: /roof-cost-calculator (2 hyphens). Margin = 3 hyphens. Future plausible root slugs with 4+ hyphens: /roof-cost-calculator-charlotte-nc? Well they might namespace. Campaign pages: /free-estimate, /financing, fine. But: /storm-damage-roof-repair-insurance-claim (5 hyphens), /2026-spring-special, /roof-replacement-cost-guide-2026... marketers love long-tail. So yes, real risk; mitigate via persistent guard + allowlist.

  Also typo-squatting/decay: URL-encoded characters. Dotless check — how is "dotless" evaluated, on normalized path: percent-encoded dots (%2E) or encoded slashes (%2F — see oracle row for /online-casinos-curacao-2026%2F — "record actual"). If pathname arrives with %2F decoded... that's the oracle's open question. For rule 1, a slug like /abc-def-ghi-jkl-mno%2Fxyz — if middleware pathname decodes %2F, it becomes 2 segments and evades the single-segment rule; fine, it 404s. Not a false positive though.

  Case sensitivity: Next.js pathname is case-sensitive; spam may come in mixed case. Do the rules lowercase before counting/splitting? §Z doesn't say. If a spam slug is /ONLINE-CASINOS-CURACAO-2026 or token "Casino", whole-token match on raw case misses it. Whether Google indexes uppercase variants — possibly. This is a miss (under-coverage), MED-LOW. Minimal fix: lowercase the slug before both rules (and negative control must also test an uppercased variant of each real route — real routes are lowercase anyway, so lowercasing can't create false positives on real routes; it's safe).

  Leading hyphen, double hyphens, empty tokens: split on "-" produces empty tokens — harmless for whole-token match. Hyphen counting: "contains 4 or more hyphens" — count of "-" characters. A slug like "-roof-repair--" (4 hyphens) would 410; it's not a real route, fine.

  What about root path "/" — normalized "/". Split: no hyphens. Safe. "/quote" 0 hyphens safe.

  Non-root single-segment that Next.js serves at root with dot: e.g. /favicon.ico is dotted → excluded by dotless. /robots.txt dotted. /sitemap.xml dotted. Good — dotless protects static files. But what about /manifest.webmanifest, /icon.svg? Dotted, excluded. OK.

  But wait — dotless also excludes dotted spam, which the WO states as KNOWN CEILING. Honest. Good.

  2. Keyword-token false positives. Check each token as a whole hyphen-token in a legitimate roofing/home-services slug:
  - casino/casinos/gambling/gamble/gambler/jackpot(s)/roulette/blackjack/poker/bookmaker/chumba/melbet/pinco/aviator/curacao/spielautomaten/spieleliste/kasyno — no legit roofing use.
  - "slots" — hmm: "vent-slots"? Roof ventilation: ridge vent slots. A page /roof-vent-types-ridge-slots-soffit — "slots" as whole token is conceivable: /ridge-vent-slots-explained. Plausible-ish, LOW-MED. Minimal: keep "slots" but note the constraint or drop to "slot"? No — spam uses "slots". Keep, but guard + comment.
  - "spins" — roofing: no. "spins" in home services? No.
  - "betting" — no.
  - "wager"/"wagering" — no.
  - "bally" — Bally is a place name? Bally, PA exists (Berks County, PA — Bally is a borough in Pennsylvania!). A roofing site in Charlotte NC with /locations/ namespaced — but root-level location pages? Locations are namespaced per the WO. Still, "bally" as a town name — for THIS site (Charlotte NC per context) implausible. LOW. Also "Bally" is a brand. Fine.
  - "igt" — no.
  - "storm" explicitly excluded — good, that's the load-bearing warning; tokens like "hail" also not in list — good.
  - Missing check: token "games"/"game"? Not in list — spam positive control #3 contains "games" as a token but the slug has way more than 4 hyphens so rule 1 catches it. Fine.
  - What about legit "casino" near Charlotte? Harrah's Cherokee casino — a roofing company wouldn't make a page. LOW.
  The list is fine; the main residual risks: "slots" (roof vent slots) and "bally" (town name, irrelevant here). Real answer: each token checked, one plausible ("slots", and marginally "spins"? no). Also "aviator" — Aviator is a casino game; also a sunglasses style; no roofing use. OK.

  But there's a subtlety: whole-token matching requires splitting on hyphens — spam with underscores or URL-encoded chars: /online_casino — tokens split on hyphens only, "online_casino" is one token, no match; hyphen count 0 → evades. Under-coverage; acceptable? The WO's ceiling statement doesn't mention underscore variants. Hmm — do underscore spams exist? WP spam is typically hyphenated. The WO's known-ceiling covers dotted and ≤3-hyphen no-keyword. Underscore spam falls in "no family keyword, ≤3 hyphens" class mostly (each underscore-joined slug has 0 hyphens). So it's covered by the stated ceiling implicitly. Fine.

  Another: tokens in namespaced paths? §Z says keyword rule — does it apply to ALL paths or only single-segment root? §Z says the flat-slug rule "applies ONLY to single-segment root paths — namespaced paths (/locations/x, /resources/x) are untouched." For the keyword-token family, it says "split the slug on hyphens; 410 if any WHOLE TOKEN matches" — "the slug" — which slug? Ambiguous scope: if keyword rule applies to every segment of every path, then a legit namespaced page like /resources/ridge-vent-slots would 410. If it applies only to root single-segment, then spam like /wp-content/...casino... hmm those are other rule classes (A2 existing). The §Z text: "Two new SPAM_410 rule classes... 2. Keyword-token family: split the slug on hyphens" — "the slug" most naturally refers back to the flat-slug context (root-level single segment) but it's not pinned. A builder could read it as applying to the last segment of any path. Concrete failure: future legit page /blog/why-ridge-vent-slots-matter → 410 if keyword rule applies to all path segments; or current real page — need to check whether any of the 137 sitemap paths contain a token. The negative control (137 sitemap paths) would catch a current collision mechanically, so current state is proven; future namespaced content with "slots" would trip if rule applies to namespaced paths. The WO's success criterion claims namespaced paths structurally unaffected — that holds for rule 1 explicitly, but rule 2's scope is unstated. MED finding: pin rule 2's scope (recommend: single-segment root only, same as rule 1; spam is root-level per recon) and note "slots" caveat.

  Actually re-reading: "2. **Keyword-token family:** split the slug on hyphens; 410 if any WHOLE TOKEN matches". Given rule 1's explicit root-only sentence and rule 2 lacking one, ambiguity is real. Pin it.

  3. What spam sub-classes does §Z still miss, and does the WO state its ceiling honestly?
  Misses:
  - Dotted paths (.php/.html/.asp) — STATED as ceiling. Honest.
  - Root flat slugs with ≤3 hyphens and no keyword — STATED ("acceptable decay class"). Honest.
  - Underscore/percent-encoded/other-delimiter slugs — not stated explicitly, but subsumed by "≤3 hyphens no keyword" since underscores aren't hyphens. Borderline honest.
  - Case variants (uppercase spam slugs) — NOT stated. If rules are case-sensitive, /Chumba-Casino-Login evades rule 2 (token "Chumba" ≠ "chumba") and hyphen count still catches only if ≥4. /CHUMBA-CASINO (2 hyphens) evades both. Google does index case variants; WP sites served case-insensitively so spam links in any case got 200s on the old WP site. MED coverage gap, unstated.
  - Numeric/ID spam: /?p=... covered by A1 (existing). /author/, /category/, date archives covered (oracle rows show). Fine.
  - Multi-segment non-namespaced spam (e.g., /spam-dir/spam-page with keyword or ≥4 hyphens across segments)? Rule 1 is single-segment only; if rule 2 is root-only too, then /xyz/online-casinos evades. Old WP spam often had nested paths. Existing A2 rules cover /author/, /category/, /blog/page/, date patterns. But arbitrary nested spam slugs not matching those patterns: e.g., /2023/online-casino/ — date rule might catch /2025/05/ pattern. Nested arbitrary: unstated ceiling. LOW-MED honesty gap: the WO's ceiling names only dotted + short flat; nested non-pattern spam isn't mentioned. Whether it exists in the 5.5K — recon says "dominated by root-level flat word-salad" — dominated, not exclusively. So there IS a residual tail the ceiling doesn't enumerate. MED (honesty, not function).
  - Non-ASCII/Unicode slugs (German umlauts percent-encoded): e.g., /ausschüttung — percent-encoded as %C3%BC; token match on raw bytes won't match "ausschuttung" anyway (that slug is already ASCII-ified in the sample). Whole-token match operates on the raw path; Unicode spam with keyword "casino" still matches "casino". Fine.
  - The ceiling statement is mostly honest on the two named classes; gaps: case sensitivity, nested spam tail, delimiter variants. Also honesty check on "structurally unaffected" for namespaced — depends on rule-2 scope ambiguity (F above).

  Also: does the WO honestly state the positive controls? 9 slugs, both slash forms → 18 rows. Rule 1 catches all 9 (all have ≥4 hyphens — check the shortest: "consecutive-avalanches-incorporate-a-modern-secure-multiplier" — 6 hyphens, yes). Actually all 9 are long word-salad — all caught by rule 1 alone. So the positive controls DON'T independently exercise rule 2! Wait — rule 2 tokens appear in many (bally, casino, igt, spieleliste, gambling, jackpot, spins, chumba, gamble). But since rule 1 fires first (or both fire), the oracle can't distinguish which rule caught them — but for 410 it doesn't matter. However, for mechanical proof of rule 2, there's no positive control that ONLY rule 2 catches (a ≥4-hyphen-free slug with a keyword token, e.g. /online-casinos-curacao-2026 has 3 hyphens — "online","casinos","curacao","2026" — 3 hyphens! That's the §G row: /online-casinos-curacao-2026 → 410. Hyphens: online-casinos-curacao-2026 = 3 hyphens. Rule 1 (≥4) does NOT catch it. Rule 2 token "casinos" and "curacao" DO. So §G row 1-4 actually is the independent positive control for rule 2! Good — that row exists in §G already (from earlier A2 work? or was it pre-existing?). Either way, it's a rule-2-only positive control. OK so that's covered — nice, worth noting in answers that /online-casinos-curacao-2026 (3 hyphens) is the load-bearing rule-2-only oracle row. Wait but §G predates §Z? The §G row might have been expected 410 via an earlier keyword rule already (A2 existing spam checks). Hmm — §G is "Oracle matrix — replacement + additions" from the earlier audit round (F-references F2, F6...), before §Z. The row /online-casinos-curacao-2026 → 410 expected — under pre-§Z rules, what caught it? Possibly an existing keyword rule (A2 spam checks existed). §Z ADDS a keyword-token family — maybe extending an existing list. Can't read other sections (brief says read only §Z, §B step 3, §G). I should be careful not to speculate beyond. I'll note: ensure at least one positive control isolates rule 2 (keyword hit, ≤3 hyphens, no other rule firing) — /online-casinos-curacao-2026 appears to serve this role; confirm no earlier rule already 410s it, else §Z's rule-2 positive proof is entangled. Actually the brief context says "the guard ... against 9 observed real spam slugs in both trailing-slash forms (positive control)" — 9 slugs, all caught by rule 1 anyway. The guard validates "every rule" — presumably rule-specific. Fine; flag the entanglement as LOW: positive controls should include ≥1 slug that rule 2 alone catches and ≥1 that only rule 1 catches with no keyword token (e.g., "consecutive-avalanches..." has no listed token — check: consecutive, avalanches, incorporate, a, modern, secure, multiplier — correct, no token → rule-1-only control. Good, that exists). And jednym-ze-sposobow... (Polish, no token? "sposobow" no; no listed token — rule-1-only. Good.) And rule-2-only: /online-casinos-curacao-2026 (3 hyphens, tokens casinos+curacao) exists in §G. So control coverage is actually decent — say so.

  4. Interaction bugs with §B step 3 ordering/normalization:
  - Normalization strips trailing slashes BEFORE spam checks → §Z sees clean single segment. Good: "/spam-slug/" → normalized "/spam-slug" → 410 direct (no 308-then-410 chain). Oracle expects 410 direct for trailing-slash forms — consistent with step 2 < step 6. Good.
  - Multi-slash: "/slug//" → replace(/\/+$/,"") strips all trailing slashes → 410 direct. Oracle row says so. Good.
  - Root: "/" → normalized "/" — A1 check on "/" only; §Z on "/" — 0 hyphens, tokens [""] → no match. Safe.
  - Query string: normalized is pathname only (no search) → "/spam?junk=1" → 410 direct. Oracle row consistent.
  - Ordering: spam checks (step 3) run BEFORE LEGACY_301 (step 4) and before 308 normalization redirect (step 6). Potential conflict: a LEGACY_301 source path that ALSO matches a §Z rule → 410 instead of 301. Could a legacy source have ≥4 hyphens or a keyword token at root single-segment? Legacy 301s are old WP URLs like /roof-replacement/ → /services/roof-replacement (from oracle). If any legacy redirect source is a root single-segment with ≥4 hyphens (e.g. old page /roof-replacement-cost-guide-2024), step 3 kills it with 410 before the 301 fires — the negative control (137 sitemap paths) wouldn't include legacy 301 sources (they're not in the sitemap!). THE GUARD'S NEGATIVE CONTROL OMITS LEGACY_301 SOURCE PATHS. That's a concrete hole: the guard validates rules against sitemap paths + app/ route dirs, but the middleware chain has another class of "real" URLs — legacy redirect sources — that must NOT match §Z, and nothing validates them. If a legacy source like /hail-damage-roof-repair-claims (4 hyphens) exists in LEGACY_301, it gets 410 instead of 301 → SEO link equity lost, and worse the guard passes silently. MED-HIGH depending on actual legacy list; minimal fix: add LEGACY_301 + B-section migration source paths to the negative-control enumeration. This is a good catch — the guard proves "no real PAGE matches" but not "no real REDIRECT SOURCE matches," and §B ordering makes §Z fire before redirects.
  - Similarly B-section legacy route migrations (step 5) — same hole.
  - Keyword rule: a legacy source containing a token — e.g. /blackjack... no. But hypothetical legacy page /roof-vent-slots → redirected? If rule 2 scope is all paths and legacy sources not in control → hole. Same fix.
  - Another interaction: step 2 A1 runs on normalized === "/" before §Z — fine.
  - Encoded slash row: /online-casinos-curacao-2026%2F — "record actual; if pathname arrives decoded, expect 410." If decoded, pathname = "/online-casinos-curacao-2026/" → normalized strips → 410. Fine; if undecoded, slug contains "%2f" — dotless? "%2F" contains no dot → dotless ✓, single segment ✓, hyphens 3 → rule 1 NO; tokens: ["online","casinos","curacao","2026%2f"] — "casinos" matches → 410 anyway. OK.
  - Case: not handled (as above).
  - Does normalization strip only TRAILING slashes — leading double slashes "//spam-slug"? pathname "//spam-slug" — replace trailing only. "//spam-slug" segments: ["","" ,"spam-slug"] — is it "root-level single-segment"? Depends on matcher implementation; if matcher splits on "/" and filters empties vs not. Edge: Next.js normalizes "//" upstream usually. LOW.
  - trailing-slash 308 for real pages happens at step 6 AFTER §Z — so a real page with trailing slash never 410s unless it matches §Z — negative control covers. Fine.
  - HEAD requests → 410 expected (oracle row) — gone() must handle HEAD; presumably fine.
  - gone() should also be used by host dimension (www + apex) — §G note, fine.

  One more: §Z rule 1 "dotless" — evaluated on normalized path: "/spam" has no dot. But what about path "/sitemap.xml/" → normalized "/sitemap.xml" → dotted → excluded → falls through to... oracle row: "/robots.txt/ and /sitemap.xml/ → record actual; fix to 308 via redirects() if 404". Consistent: dotted excluded from §Z, so no accidental 410 of sitemap. Good — and that's exactly why dotless matters. Also protects /favicon.ico etc. Good design; note it.

  5. THE ONE THING for §Z: The negative control proves the present, not the future — and the success criterion demands "no real page — current or plausibly-future — can ever match." The gap between mechanical proof (current routes) and the marketing claim (future-proof) is THE thing: convert the guard into a permanent CI/build-time test that fails loudly when any future root route (static dir, dynamic [slug] segment, or legacy-301 source) trips a §Z rule, and add a code comment at the rule pointing at the invariant. Plus fold legacy redirect sources into the control. If forced to pick ONE: the guard's negative control must cover legacy-301/migration sources AND be permanent — i.e., "the guard as specified proves today only; make it perpetual and complete (redirect sources included) or the 'never matches a real page' claim is unproven."

  Severity assignment:
  - F1: Negative control omits LEGACY_301 / B-section migration source paths; §Z runs before redirects (§B step 3 < 4/5) → any legacy source matching ≥4 hyphens or keyword token gets 410 instead of 301, silently passing the guard. Severity: HIGH (likely rework — must fix guard before claiming proof; actual collision probability unknown but the proof gap is real). Concrete failure: old WP page /roof-replacement-cost-guide-charlotte (4 hyphens, root, single-segment) in LEGACY_301 → 410, link equity + crawl signal lost; guard green. Minimal fix: enumerate all redirect sources into negative control; run before deploy.
  - F2: Future-safety rests on a convention, not a mechanism: guard is specified as a one-time validation; WO doesn't pin it as a permanent test in CI nor state the invariant for future devs (no root routes with ≥4 hyphens; no root-level dynamic [slug] route). Failure: 2027 marketing adds /spring-storm-roof-tune-up-special (5 hyphens) → 410 in prod, silently delisted. Or a root [slug] CMS route → every ≥4-hyphen post 410s; the directory-enumeration control passes because "[slug]" has no hyphens. Severity: HIGH (likely rework when it bites; silent SEO harm = material). Minimal fix: commit guard as a permanent build/CI check that fails loudly + comment at rule + treat "root dynamic segment" as incompatible, stated in the WO.
  - F3: Rule 2 scope unpinned — "the slug" undefined; if applied to namespaced paths, future /resources/ridge-vent-slots-types 410s; "slots" is a real roofing term (vent slots). Severity: MED. Minimal fix: scope rule 2 to single-segment root like rule 1; keep "slots" but comment.
  - F4: Case sensitivity unaddressed — rules presumably match raw pathname; /Chumba-Casino (1 hyphen) or /ONLINE-CASINOS-CURACAO-2026 evade rule 2 if not lowercased; unstated in ceiling. Failure: Google indexes case-variant spam URLs (WP served them 200 case-insensitively); they stay indexed → class not fully killed, SUCCESS criteria "~5.5K flat-slug spam class returns direct 410" only mostly true. Severity: MED (survivable; cheap fix: lowercase before matching — provably safe since all real routes are lowercase, control can prove it). Minimal fix: slug.toLowerCase() before both rules; add oracle row /ONLINE-CASINOS-CURACAO-2026 → 410.
  - F5: Honesty gaps in ceiling: nested non-pattern spam tail and delimiter variants (underscores, %20, +) not mentioned; ceiling lists only dotted + ≤3-hyphen-no-keyword. Failure: report claims class kill; residual tail stays indexed; client surprised. Severity: LOW-MED (honesty/expectation). Minimal fix: one sentence in KNOWN CEILING.
  - F6? Entangled positive controls: the 9 quoted slugs are all caught by rule 1 alone (all ≥4 hyphens); rule 2's independent proof leans on §G row /online-casinos-curacao-2026 (3 hyphens) — confirm no earlier A2 rule already 410s it, else rule 2 has no isolated positive control. Also add a rule-2-only row in both slash forms? §G has it in both forms. Severity: LOW (verification hygiene). Minimal fix: state which rule catches each positive-control row, or add /online-casinos-curacao-2026 explicitly as rule-2-isolation row (it already exists — just annotate).

  Hmm also: "igt" token — "igt" could be substring issues? Whole-token only, fine. "bally" — Bally, PA; also "bally" no. Fine.

  Also check token list for tokens that are also English words that could appear in legit slugs: "spins" no; "poker" — "poker" no; "aviator" no; "curacao" no; "bally" place; "slots" YES (roof vents, also "time-slots" for scheduling! /schedule-your-inspection-time-slots? namespaced presumably). "wager" no. "spiele"/"spieleliste" German only. "kasyno" Polish. OK: answer Q2 with "slots" as the one real token risk, "bally" marginal, rest clean.

  Also Q1: enumerate plausible future legit root slugs with 4+ hyphens:
  - /roof-replacement-cost-charlotte-nc (5) — though locations namespaced; but a marketing one-off could be root.
  - /free-roof-inspection-storm-damage (4)
  - /2026-spring-roof-tune-up-special (5, leading digits fine)
  - /insurance-claim-roof-denial-help-guide (5)
  - /metal-vs-shingle-roof-cost-comparison (5)
  - /emergency-roof-tarp-service-after-storm (5)
  Guard's negative control as specified protects only the current 137 + current app dirs. Not sufficient alone; needs permanence + redirect sources + dynamic-route incompatibility statement. Alternative shapes: allowlist-based (410 only if NOT in generated ROOT_ROUTES manifest) is strictly better — rule becomes "single-segment, dotless, ≥4 hyphens, and not in ROOT_ROUTE_MANIFEST (build-generated)". That makes future routes automatically exempt and mechanically safe; the manifest regenerates each build. Actually even simpler and fully future-proof: since Next.js middleware can't enumerate routes at runtime, generate the manifest at build. That's the "different shape" the question asks about: allowlist exemption > pure heuristic. Recommend: keep heuristic + build-time-generated allowlist exemption; then "no future page can ever match" becomes mechanically true.

  THE ONE THING: I'll pick: the guard proves the present, not the future — add the build-time root-route allowlist exemption (or pin the guard as a permanent CI gate) so the "plausibly-future" success criterion is mechanically backed rather than conventional. And include redirect sources. I'll phrase ONE THING as: make the negative control perpetual and complete (redirect sources + future routes via build-generated allowlist/CI guard) — that is the single load-bearing gap.

  Also possible finding: §Z doesn't specify behavior when BOTH rules could apply — irrelevant, same action 410.

  Another: hyphen counting on slug containing encoded hyphen "%2D"? Pathname decoded? %2D in path — Next decodes %2D? Percent-encoded hyphen is unusual; skip. Keep findings tight.

  Another interaction: normalization strips trailing slashes but spam with trailing slash AND query — fine.

  What about the "/" → ""→ "/" — the A1 check on "/" — fine.

  Also: does the rule apply to paths with file extensions at root — excluded via dotless — but .well-known paths (/.well-known/...) dotted? "/.well-known/change-password" — segment ".well-known" contains dot → excluded. OK.

  VERDICT: sound-to-execute? The core rules are sound for the current corpus; but F1 (redirect sources not in control) and F2 (future safety) are HIGH-ish. Verdict: YES with conditions — execute only after adding redirect sources to the negative control and pinning the guard as permanent; otherwise the WO's own success criterion is unproven. I'd say "yes, with two mandatory amendments (F1, F2); everything else is cheap." LAUNCH-KILLER? Is anything launch-killer — shipping causes material harm? F1 collision could 410 a live legacy redirect — that's SEO harm but bounded and only if a collision exists (checkable in 5 min). Not launch-killer per anchor since unknown; HIGH. F2 is future harm, not immediate. So verdict: sound-to-execute: YES after F1/F2 amendments; neither requires redesign.

  Output format: ## VERDICT · ## FINDINGS (F1..Fn) · ## ANSWERS (numbered 1-5) · optional MISSING.

  Style: hostile reviewer, concrete hole → failure → minimal fix. No politeness. Markdown stdout. The brief says produce markdown analysis only, read-only — so output in reply, no files.

  Let me double-check a few facts:

  - "/roof-cost-calculator" = 2 hyphens ✓ (given).
  - Rule: 4+ hyphens. Margin: a real route at 3 hyphens would still pass; rule starts at 4.
  - Positive control slug 2: "diese-8-besten-online-casinos-via-schneller-ausschuttung-igt-spieleliste-im-kollation" — contains tokens casinos, igt, spieleliste ✓ and 11 hyphens ✓.
  - Slug 5 (Polish): "jednym-ze-sposobow-ktorymi-zajrzyj-do-probuje-wybor-nakladania-osobistych-limitow" — no listed token ("kasyno" absent) → rule-1-only control ✓.
  - Slug 6: "consecutive-avalanches-incorporate-a-modern-secure-multiplier" — no token → rule-1-only ✓.
  - /online-casinos-curacao-2026: 3 hyphens, tokens casinos + curacao → rule-2-only ✓ (assuming no earlier rule).

  Underscore note: spam with underscores — e.g. /online_casino_bonus — 0 hyphens, one token "online_casino_bonus" no match → 404 decay. Covered by stated ceiling? Ceiling says "Root flat slugs with 3 or fewer hyphens and no family keyword still 404" — yes, technically covered. Good, mention briefly.

  %2F row in §G — I referenced.

  Now the "MISSING-<X>" optional section: MISSING-EVIDENCE: I can't see the actual LEGACY_301 source list or lib/legacy-url-rules.ts (brief forbids reading beyond sections) — so F1's collision existence is unverified; the fix stands regardless. State that.

  Length: keep it tight but complete. Findings F1-F6. Answers numbered 1-5 referencing findings.

  Severity summary: F1 HIGH, F2 HIGH, F3 MED, F4 MED, F5 LOW, F6 LOW.

  Write the audit now. No files, stdout markdown in reply. Keep headers exactly: ## VERDICT, ## FINDINGS, ## ANSWERS, ## MISSING-...

To resume this session: kimi -r session_6b342bdc-6c66-4167-b501-7cd777afbb2d
