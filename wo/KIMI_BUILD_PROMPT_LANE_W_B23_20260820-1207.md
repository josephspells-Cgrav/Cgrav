You are the BUILDER for SEO Lane W, batches 2 AND 3, in this worktree (C:/Users/josep/Claude Gravity/.wt-seo-w, branch seo-w-city-sweep, currently at commit b87be3c — batch 1 + its fix round are DONE and judged; do not touch the Durham or Raleigh entries). Read wo/WO_SEO_LANE_W_CITY_SWEEP.md IN FULL including AMENDMENTS v2 AND AMENDMENT v3 at the bottom (later amendments OVERRIDE earlier sections; v3 makes the neighborhoods array a work-claim surface — array entries must be book-supported per wo/full-hoods.json, identity-anchor descriptors live in prose only, Clayton's array stays as-is per its carve-out), and wo/full-hoods.json (the complete adjudication source; a hood absent from a city's list has ZERO book jobs).

Execute BATCH 2 first: the APEX, WAKE FOREST, PITTSBORO, GARNER, CLAYTON, and MORRISVILLE entries in lib/cities.ts, per the WO — Tier-1 count-led treatment (honesty scrub with the mandatory scrub-table rows, evidence weave, v2.F opening spines each on its own bone, v2.C pre-adjudications: Salem Street, old-college/North Main, Creekside, Carpenter Village all 0-book), unique count-FAQ constructions (distinct from each other AND from Cary's "How many Cary roofs has Mabrey Roofing completed?", Durham's "How much roofing has Mabrey Roofing actually done in Durham?", Raleigh's "What is Mabrey Roofing's track record in Raleigh?"), metaDescriptions ≤155 with count. MORRISVILLE TRAP (v2.E): the book supports Carpenter PARK (1, Morrisville); "Carpenter Village" is a CARY neighborhood — scrub Morrisville's Carpenter Village claims per v2.C and rebuild those clauses on Kitts Creek (3) + Breckenridge (3); never substitute Carpenter Park into the old sentences and never cross-import from the Cary pack. GARNER: spread-not-depth phrasing (5 hoods × 1 job), name at most 3. PITTSBORO: Fearrington Village is the ONLY hood claim permitted. CLAYTON: count-led but ZERO hood work-claims; existing descriptors stay descriptors. Then run the full gate chain (npm run typecheck; npm run build; kill any prior :3221 listener BY PORT first; start a fresh server; prove /_next/static/<BUILD_ID>/_buildManifest.js == 200; PORT=3221 npm run spam-410-guard; PORT=3221 npm run doorway-check; PORT=3221 npm run reachability-check) and the §10 oracle for the six batch-2 pages plus cary-nc and burlington-nc frozen controls, plus durham-nc and raleigh-nc regression (exact totals still present). Run the v2.G forbidden-pattern greps and the git-diff scope assertions. Commit as "seo-lane-w batch 2: apex + wake forest + pittsboro + garner + clayton + morrisville".

Then execute BATCH 3: the six Tier-2 entries — KNIGHTDALE, HOLLY SPRINGS, HILLSBOROUGH, WENDELL, FUQUAY-VARINA, ZEBULON. Tier-2 rules bind hard: evidence-WOVEN, never count-led — the city total appears NOWHERE (digit or word form; run the v2.G Tier-2 total-leak grep, 0 hits required), no count-FAQ added (FAQ count unchanged), no per-hood counts, book hoods woven into intro/housingStock as completed-work mentions, arrays per v3 (book-supported only; may shrink to 3-4), Knightdale's Mingo Creek/Langston Ridge/Planters Walk stay as housing descriptors only per v2.C. Per-city oracle: ≥ min(2, full-list size) hoods present; Zebulon (1 hood) asserts exactly Rivercrest. Re-run the full gate chain + oracle (6 batch-3 pages + the 2 frozen controls + durham/raleigh/batch-2 regression) + greps + scope assertions. Commit as "seo-lane-w batch 3: six tier-2 cities".

APPEND per-batch sections to wo/BUILD_REPORT_SEO_LANE_W.md: gate exits, oracle tables, THE SCRUB TABLE per v2.C for every edited entry (every pre-existing neighborhood work-claim → KEEP/CONVERT/DELETE with its full-hoods.json count), all count-FAQ questions side by side (8 total including Cary/Durham/Raleigh, proving distinctness), forbidden-pattern grep outputs, git diff scope proof, every source URL added (or "none"). Kill the :3221 server when done. THEN STOP — the orchestrator runs the final sweep judge; do not merge, do not push.

HARD RULES: NEVER deploy, NEVER touch env files, NEVER read a DATABASE_URL, NEVER run migrations, NEVER git push, NEVER edit anything outside lib/cities.ts + your report; the 6 frozen entries (cary, chapel-hill, burlington, sanford, wilson, rolesville) stay byte-identical; the batch-1 entries (durham, raleigh) stay byte-identical in these batches; where the WO forces a guess, STOP and write the gap into the report — never improvise. Directional/geographic claims: use only WO-supplied placements or omit directions — batch 1's judge killed three invented directions; dropping a direction is always safe. Final output: compact summary with per-batch gate exits, oracle results, scrub-table row counts, and both commit SHAs.
