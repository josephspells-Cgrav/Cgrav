• Let me read the brief file first.The brief says: read exactly this one file and nothing else: `C:/Users/josep/.claude/skills/orchestrator/SKILL.md`. Then produce the markdown audit. Read-only, analysis only.Let me analyze this skill file carefully against the 9 audit targets. The brief says:

  - Read-only, markdown analysis only
  - Adversarial audit, hostile independent reviewer
  - The re-baton specifically asks: "Audit THE EDIT in context of the whole file: does the always-ask rule conflict with any other law, and does the skip clause (mode named in the activating message) reopen an inference hole?"
  - Output format: ## VERDICT · ## FINDINGS (F1..Fn, severity · section attacked · hole · failure scenario · minimal fix) · ## ANSWERS (numbered, mirroring the 9 targets) · optional ## MISSING-<X> list.

  Let me carefully audit this artifact, the installed SKILL.md (243 lines).

  Let me go through it systematically.

  ### Frontmatter description

  ```
  name: orchestrator
  description: WO-driven multi-agent build orchestration — the session becomes an ORCHESTRATOR that NEVER builds and OWNS TRUTH. ... Two modes, and the orchestrator ALWAYS ASKS which before drafting — LEAN ... and PLUS-JUDGE ... Triggers — "orchestrate this", "orchestrator mode", "run the full pipeline", "spin up builders", "WO this out to a builder", any multi-agent build wave — AND, regardless of vocabulary, any build touching a customer-facing/prod/money/credential surface that is larger than a one-file edit. NOT for single small edits (just do them), live taste iteration (edit-mode), continuous unpredictable inbound edits (dispatcher-mode), or research fan-outs where nothing ships.
  ```

  Note: the description says "the orchestrator ALWAYS ASKS which before drafting" but doesn't mention the skip clause ("mode named in the activating message"). That's in the body. The description says "Two modes, and the orchestrator ALWAYS ASKS which before drafting" — fine.

  ### Target 1: Coverage holes

  Real orchestration situations with NO instruction:

  a) **Builder crashes mid-run / dies silently** — Law 10 covers "A DEAD BUILD LANE resumes from disk" — so a dead builder is covered. But what about a builder that hangs (not dead, just stuck)? Or a builder whose report never arrives? There's no timeout instruction. A hung sub-agent: no instruction for how long to wait, when to declare it dead. Partial coverage — Law 10 covers dead lanes but "how do you detect dead vs. slow" is unspecified. MED.

  b) **Two builders' diffs conflict at merge** — Step 7: "Anything else: conflict resolution is a BUILDER task ... and the merged tree gets the full gate re-run." Covered. But what about: gate fails ONLY on the merged tree (semantic conflict, no textual conflict, fast-forward succeeded but combined tree fails)? Step 7 says "the merged tree gets the full gate re-run" only in the conflict branch. For a fast-forward merge... hmm. Wait — fast-forward means "the tested tree and the deploy tree are byte-identical", which is true for FF. But consider: lane A merges (FF). Lane B merges — if lane B's branch was cut from deploy tip before A merged, B's merge is NOT a fast-forward; it either merges with conflict resolution (covered) or merges cleanly with no textual conflicts — is that a fast-forward? No — if A has advanced the tip, merging B is not a FF unless B contains A. The skill says "Fast-forward preferred — a fast-forward means the tested tree and the deploy tree are byte-identical." So for non-FF clean merges... the "Anything else" branch says conflict resolution is a builder task and "the merged tree gets the full gate re-run". Does a clean non-FF merge count as "conflict resolution"? There's no conflict to resolve. So the orchestrator merges cleanly, and per the letter of the text, no full gate re-run is required ("the merged tree gets the full gate re-run" is attached to the conflict-resolution clause). But actually wait — after lane A merges, step 7 says "After EVERY merge, re-cut or rebase the remaining open lanes onto the new tip." So lane B gets rebased onto the new tip before its merge, which would make B's merge a FF again IF the rebase produces no changes. But rebase changes the tree B tested on — does the rebased lane B get its gates re-run? The skill doesn't say. A rebase can silently break the lane (semantic conflict). The law-4 re-run happened pre-merge on the OLD tip. Post-rebase, the lane's tree differs from what was gated. The text says "a lane building on a stale tip is gating a tree that will never ship" — but a rebased lane is gating a tree that was never tested. HOLE: after re-cut/rebase, is a fresh law-4 re-run required before that lane's merge? Not stated. This is a real coverage hole — "a gate fails only on the merged tree" scenario. HIGH? The baton gate at step 6 happens pre-merge too. So a floor-surface lane could be batoned and gated on tree T1, rebased to T2, merged, deployed — without any re-gate of T2. Hmm, but step 7's "Anything else: ... the merged tree gets the full gate re-run" — an adversarial reader could argue a rebase isn't a merge. And a clean merge of a rebased lane is a FF (byte-identical claim holds only if the rebased tree was tested). The failure scenario: lane B rebased onto new tip, no conflicts, tests would fail due to semantic interaction (e.g., lane A renamed a symbol lane B uses, but textual merge is clean). Orchestrator FF-merges, deploys, live-verify... live-verify would catch some things but not all (a broken non-critical path). Minimal fix: state that after any re-cut/rebase, the lane's gates re-run before merge (or equivalently, every merge requires a gate run on the exact tree being merged unless FF-of-tested-tree).

  c) **Joseph changes the WO mid-build** — Law 2 covers WO amendments (orchestrator-only, probe-first, written to disk; floor-touching amendments need Joseph's explicit approval + fresh baton). "A mid-lane WO change STOPS the lane first." Covered. But: who can amend? "amendments are orchestrator-only" — but if JOSEPH changes the WO mid-build (Joseph is the operator, he can do anything)... The skill handles "a mid-lane WO change" generically — stops the lane. OK mostly covered.

  d) **Judge and orchestrator disagree** — Law 6: "Disputes between a judge's reading and the orchestrator's runtime probes: probes win, and the dispute goes to Joseph if anything floor-adjacent turns on it." Covered for probes-vs-judge. But what about judge vs. orchestrator where the orchestrator has NO probe (pure judgment, e.g., the judge says the copy violates a banned-word law, the orchestrator's grep says it doesn't — that's probeable). What about judge A vs judge B contradicting? Not addressed. "Subjective findings that survive a round are LISTED as noise floor" — covers subjective. Two judges contradicting on contract matters: SHIP = "one full round that surfaces no NEW majors" — if Judge A says X is a major and orchestrator's probe says it isn't, probes win. OK, decent coverage. Judge vs judge: not explicitly covered. MED/LOW.

  e) **Builder crashes mid-run**: Law 10 dead-lane rule covers. Also "builder reports success but the worktree has uncommitted changes"? Not covered — hmm. A builder's diff: what does "the diff" mean — committed diff vs working tree? Step 3 says branch cut from deploy tip. The builder works in the worktree. The report comes. The orchestrator re-runs gates "with its own hands" — in the worktree. Then merges. If the builder left changes uncommitted, the merge won't include them; gates re-run on the worktree pass, but the merged tree differs. HOLE: no instruction that the orchestrator verifies the worktree is clean / everything committed before merging, or defines "the diff" as committed work. Actually, the harness-diff rule (law 4: "diffs the harness files against the deploy tip explicitly") shows diffs are computed — but against what base, and from committed or working state? Unspecified. MED — a session would have to guess. Actually this is a real exploit: builder leaves the fix in working tree uncommitted, report claims done, orchestrator re-runs gates in the worktree (working tree — passes), merges branch (missing the fix), deploys broken code. Live-verify catches only the WO-named probes. Minimal fix: require the orchestrator to verify `git status` clean (or diff the worktree against the merge result) before merge. I'd rank HIGH-ish... it's an underspecification more than a designed hole. MED-HIGH. Let me call it MED given live-verify is the backstop, but for floor lanes a missing uncommitted fix could be exactly the baton-flagged fix... Actually the baton reviews "WO + diff summary" — diff of what? Again unspecified. I'll fold this into one finding: "the diff" is never defined (committed vs working tree, base ref), which propagates through laws 4, 5, step 6, and the merge step. HIGH because multiple load-bearing steps rely on it.

  f) **The orchestrator session itself dies mid-run** — a dead ORCHESTRATOR: Law 10 covers dead build lanes and bank-locks, but the pipeline's resumption state (which step of which lane) — "Decisions bank to the vault at lock (km-ingest)". A fresh session resuming: does it re-probe everything? Law 2 says WO current behavior is "PROBED this session, never recalled" — a resumed session must re-probe. Probably fine. LOW.

  g) **Migrations fail halfway / migration applied but code deploy fails** — Step 7a: migrations BEFORE code. If the code deploy then fails and roll-back restores the previous deployment, the migration stays applied. Old code + new schema — no instruction that migrations must be backward-compatible or what to do about a migrated DB on rollback. HOLE: rollback (step 8's roll-back-FIRST) addresses the deployment, never the schema. A destructive migration (drops a column old code reads) + failed deploy + rollback = old code live against new schema = outage. Minimal fix: require migrations to be backward-compatible with the currently-live deploy, or name the migration-rollback plan in the WO. HIGH — this is the classic expand/contract problem and the skill governs real prod deploys. Actually, is this within scope? The skill DOES instruct migrations and rollback; the interaction is unaddressed. I'd say HIGH.

  h) **kimi-baton unavailable — the "deadman rule" is referenced but never defined in this file.** The floor says "(b) kimi is UNAVAILABLE per the deadman rule" — the deadman rule is not stated anywhere in the skill. An executing session has to guess what constitutes unavailability (one failed spawn? two? timeout?). The brief says "Assume they exist and work as one-line described in the artifact; do not audit them" — for kimi-baton. But the deadman rule is a rule OF THIS SKILL (referenced, not described even in one line). Hmm, the brief says related skills are "referenced by name: ... kimi-baton (blind external audit at commit points)". The deadman rule is not part of the one-line description. This is an underspecification: the session cannot know when skip (b) is admissible. And it's the kind of hole a cornered session exploits: "kimi didn't answer on the first try → UNAVAILABLE → skip baton, substitute a judge" — exactly the audit-floor cut the success criteria forbid. HIGH. Minimal fix: state the deadman rule verbatim in the floor section (e.g., "N failed spawns over M minutes = unavailable").

  i) **"one-file edit" boundary** — activation says larger than a one-file edit. What about a one-file edit that touches a floor surface and is high-risk (e.g., editing the send-closure file, an env template, a migration)? The skill explicitly says OFF for "a one-file edit you can make and verify faster than a WO takes to write" — but activation ALSO says "when the task is a BUILD touching a floor surface ... larger than a one-file edit". So a one-file floor edit stays un-orchestrated — but ACTIVATION bullet 2 says when the skill stays OFF on a build touching a floor category, say so in chat ("the floor still binds"). OK — so the floor (kimi-baton + live verification) still binds on a one-file floor edit. Wait, does it? Bullet 2: "the floor still binds" — yes. So covered-ish. But bullet 3: "If Joseph explicitly orders inline work on a floor surface, the ORCHESTRATION is waived but THE FLOOR IS NOT: kimi-baton (or its visible skip marker) and live verification still bind directly." Covered. Fine.

  j) **Two simultaneous orchestrations / nested orchestration**: a wave with ≥2 lanes is one activation. Fine.

  k) **What if the deploy branch's tip moved because someone else (Joseph, another session, dispatcher-mode) pushed between lane cut and merge?** The FF check handles it mechanically. The re-cut rule covers remaining lanes. OK.

  l) **The always-ask edit vs. other laws** (the delta note's specific question):

  The mode section: "On every activation the orchestrator's FIRST message asks: 'LEAN or PLUS-JUDGE?' — one question, before any WO is drafted. No inferred default. If Joseph already named a mode in the activating message (lean/judge/gambit/thorough), that answer stands and the question is skipped. 🔴 The skip fires ONLY on the explicit tokens LEAN or PLUS-JUDGE (or an unambiguous 'with judges'/'no judges'). Ecosystem slang — 'gambit', 'thorough', 'go fast', 'cheap' — does NOT count as naming a mode: ASK."

  INTERNAL CONTRADICTION: The first paragraph of the mode section says: "If Joseph already named a mode in the activating message (lean/judge/gambit/thorough), that answer stands and the question is skipped." The very next sentences say the skip fires ONLY on explicit tokens LEAN or PLUS-JUDGE (or "with judges"/"no judges"), and that "gambit", "thorough" do NOT count. So sentence 2 names "gambit" and "thorough" as valid mode-naming tokens that skip the question, and sentence 3–4 explicitly names them as NOT valid. Direct contradiction within one paragraph — leftover from the edit (the parenthetical "(lean/judge/gambit/thorough)" was the old inference rule; the re-baton F1 fix appended the stricter rule but didn't delete the old one). This is exactly what the delta note asks: "does the skip clause reopen an inference hole?" YES — sentence 2 is the inference hole, still present. A session reading "gambit" in the activating message can cite sentence 2 ("gambit" listed as naming a mode → answer stands → question skipped) and treat GAMBIT as a mode — but gambit isn't even one of the two modes! Which mode does "gambit" map to? LEAN (cheap) or PLUS-JUDGE (thorough)? The session must GUESS — the exact inference hole the edit was ordered to close. This is the headline finding. HIGH — it's a literal contradiction between adjacent sentences, both "🔴" load-bearing... actually sentence 2 isn't 🔴-marked but the paragraph is. The failure scenario: Joseph says "orchestrate this, gambit" → orchestrator skips the ask (citing sentence 2), guesses LEAN (gambit ≈ fast/cheap), runs LEAN on a floor lane — floor still applies (baton+live-verify), so material harm is bounded, but the operator-ordered "ALWAYS ASK, never default" is voided and the session picked a mode the human never chose. Also "judge" as a token: sentence 2 lists "judge" — does "judge" unambiguously mean PLUS-JUDGE? The 🔴 sentence says skip fires ONLY on explicit tokens LEAN or PLUS-JUDGE (or unambiguous "with judges"/"no judges"). "judge" ≈ "with judges" — probably fine. But sentence 2's list "(lean/judge/gambit/thorough)" vs the strict rule: contradiction stands regardless.

  Also case-sensitivity: "explicit tokens LEAN or PLUS-JUDGE" — is "lean" (lowercase) explicit? Sentence 2 says "(lean/judge/gambit/thorough)" lowercase. Ambiguity: does "go lean on this" count? "unambiguous" is undefined for the middle cases: "be thorough" (slang per sentence 4 — but "thorough" sounds like it means PLUS-JUDGE to a literalist). The fix deleted the inference but left the vocabulary list that enables it. Minimal fix: delete "(lean/judge/gambit/thorough)" from sentence 2, replacing with "(the explicit tokens LEAN or PLUS-JUDGE)". One-line fix.

  Another tension: always-ask vs. yolo-mode. "yolo-mode — INHERIT if Joseph granted it; the orchestrator never grants itself autonomy." And ACTIVATION bullet: "The orchestrator seat does NOT change permissions — confirm-first, yolo-mode grants..." If Joseph granted yolo-mode (no confirm prompts), does the orchestrator still ask LEAN-or-PLUS-JUDGE? The mode section says "On every activation the orchestrator's FIRST message asks" — unconditional. yolo-mode is a permission register (per the brief, "permission register"). Asking a mode question isn't a permission prompt, so arguably no conflict — the ask is a design question, not a permission. But a session in yolo-mode could read "never grants itself autonomy" + yolo's no-questions register and skip the ask to avoid interrupting. There's friction: always-ask says ALWAYS; yolo says don't pester. Not a hard contradiction but a genuine ambiguity the session must resolve. MED. Also: always-ask vs. the ACTIVATION "explicit orders" rule — if Joseph's activating message says "orchestrate this inline, don't ask me anything" — hmm, edge. Also what if Joseph is unreachable (overnight run — law 9 mentions "overnight shipping")? The orchestrator's FIRST message asks a blocking question... but the whole point of law 9 is forking to humans with delta-0 defaults. Does the mode question block the entire run until Joseph answers? "No inferred default" — so yes, the pipeline halts at step 0. For an overnight unattended run, that's a coverage hole: nothing ships because a question went unanswered — actually that's safe-by-default (delta-0), arguably intended. But worth noting: the skill codified "from the night arc (~30 agents...)" — overnight operation is the use case; a blocking ask with no default and no fallback (e.g., "if Joseph is unreachable, the run does not start" — which IS the safe reading) should be stated. Actually "No inferred default" does state it: you cannot proceed without an answer. It's fine but should be explicit that the run simply doesn't start. LOW/MED. Hmm, I'll fold this into the always-ask analysis.

  m) **Mode question vs. "the regime follows the work, not the wording"** — no conflict.

  n) **LEAN floor wording**: "any lane touching prod, money, credentials, or a customer-facing surface keeps its kimi-baton and its live verification UNCONDITIONALLY." Then: "Admissible baton skips are exactly two — (a) Joseph explicitly waives it in this conversation..." "UNCONDITIONALLY" + "skips are exactly two" — slight rhetorical tension but the two-skip clause is clearly meant as exhaustive; fine. Enforceability (target 4): "keeps its kimi-baton" — enforceable if the session knows how to run kimi-baton (composition section says RUN at step 6). The floor's wording: "touching ... a customer-facing surface" — who decides "customer-facing"? A lane editing an internal admin dashboard — customer-facing? A lane editing a README? The classification is a judgment call the session makes — a session motivated to cut the floor can argue "this endpoint isn't customer-facing, it's internal." "customer-facing surface" is never defined in the skill. The WO is supposed to name floor surfaces? Law 2 required fields don't include a floor-surface determination... Actually step 6: "before merge on any floor-surface lane" — the determination of "floor-surface lane" is the orchestrator's own, unrecorded anywhere required. Minimal fix: require the WO to carry an explicit "floor surfaces touched: <list/none>" field, probed at draft. MED — the classification hole is where the floor quietly disappears, the exact failure bullet 2 of ACTIVATION warns about. Actually bullet 2 mitigates (announce when floor binds while skill OFF). But in-mode, the floor-lane determination is implicit. MED.

  o) **"kimi-baton (or its visible skip marker)"** — skip marker format: `BATON: SKIPPED <which> ` — note the trailing space and `<which>` — "which" what? which skip reason (a or b)? which baton? Ambiguous but trivial. LOW. Also the marker has a literal trailing space inside the backticks — cosmetic.

  p) **The floor: "neither creates nor satisfies the other"** (baton vs judge) — good. But PLUS-JUDGE on a NON-floor lane: no baton required. A lane touching neither prod/money/credentials/customer-facing — e.g., internal tooling — ships with judges only. Fine.

  q) **SHIPPING to customer-facing with NO external check in LEAN** (target 4): LEAN on a floor lane keeps baton + live verification — those ARE external checks. LEAN on a NON-floor lane: orchestrator's own re-gate + diff read only. Can a non-floor LEAN lane ship something to a customer-facing surface? By definition if it touches customer-facing it's a floor lane. The risk is misclassification (above). So the floor's wording is enforceable PROVIDED the classification is made. The hole: nothing forces the classification to be explicit/recorded. Also: does LEAN require live verification on non-floor lanes? Step 8 (deploy + live-verify) applies to every lane — "After EVERY deploy" (law 8). So live-verify binds all lanes that deploy. OK.

  r) **Target 5 — Activation misfire:**

  - FALSE POSITIVE: "any build touching a customer-facing/prod/money/credential surface that is larger than a one-file edit." A two-file typo fix in customer-facing copy → activates the full orchestration regime? "larger than a one-file edit" — a 2-file copy fix activates. Is that intended? The NOT-for clause says "NOT for single small edits (just do them)". So a small-but-2-file edit on customer-facing copy: activates by the letter. The orchestrator must then ask LEAN-or-PLUS-JUDGE, write a full WO with ~14 required fields ("copy laws (banned words/punctuation)", oracle, negative probes...), spawn a builder for a 2-line change. Over-activation → either absurd overhead or the session rationalizes skipping the skill. The size test is "one-file" not "small" — mismatched with the OFF clause's "one-file edit you can make and verify faster than a WO takes to write". Actually the OFF clause and the trigger agree on one-file. So a trivial 2-file, 2-line fix → regime ON. Concrete example: fixing a two-character typo that appears in both the SMS template and the email template (two files, customer-facing, money-adjacent). Skill says orchestrate. Hmm — but per ACTIVATION bullet 2, if the session keeps it OFF it must announce. Actually no — bullet 2 covers "a build that DOES touch a floor category" when the skill stays OFF; the skill's ON condition is met here, so staying OFF contradicts the ON rule, and the announce rule is for when it legitimately stays OFF? Ambiguous. The honest reading: regime ON. Failure mode: overhead on trivia OR skill-decay (session learns to classify small things as "one-file-ish" to avoid the regime — the boundaries erode). MED. Minimal fix: size test in lines/files AND risk, or explicitly defer to "Joseph names it OR one-file OR trivial-diff-below-N-lines"... but that's taste. Report as MED ambiguity.

  - FALSE NEGATIVE: a build that touches a floor surface but is framed as "research": "prototype a new pricing page — don't ship" — nothing ships, research fan-out OFF-clause... but a prototype pricing page IS customer-facing-ish if later shipped. The OFF clause "research fan-outs where nothing ships" — fine, nothing ships, no floor. OK.
  - "the deploy pipeline failed — why?" — diagnostic, correctly excluded. But the follow-up "fix it" — is the fix a BUILD? The deploy pipeline (CI config) is... not in the floor list (prod, money, credentials, customer-facing). CI config is named in the SIX NEVERS as a gate. A broken deploy pipeline is prod-adjacent but not listed. "fix the Vercel config" — one-file edit, probably inline. Fine-ish.
  - Misfire: "any multi-agent build wave" in the description — a research wave (3 agents researching) — description says "any multi-agent build wave — AND ... any build touching..." but also "NOT for ... research fan-outs where nothing ships." Consistent.

  - Another misfire: description triggers include "WO this out to a builder" — fine.

  s) **Target 6 — pipeline steps ordered/receipts/state:**

  Step 6 (floor gate — baton) is before MERGE (step 7). Composition says "RUN at step 6 on floor lanes; also on any WO the floor covers before dispatch". So floor WOs get TWO batons (pre-dispatch and pre-merge)? Composition: "also on any WO the floor covers before dispatch". So yes two baton runs. That's fine (belt+suspenders) but step 3/4 of the pipeline don't mention the pre-dispatch baton — the numbered pipeline "ARE the law" and doesn't include it; the composition section adds it. Minor inconsistency: the pre-dispatch baton exists only in COMPOSITION, not in the numbered pipeline. A session following "the numbered steps ARE the law" could skip the pre-dispatch baton. LOW/MED.

  Step 7a placement: "7a. MIGRATIONS ... applied by the orchestrator to prod BEFORE the code deploy" — but 7a is listed AFTER step 7 (merge). Order: merge → migrate → deploy. Migration runs against prod AFTER merge but BEFORE deploy. Between migration and deploy, the OLD live code runs against the NEW schema — window of incompatibility (same backward-compat hole as (g)). Also: if the migration fails, the code is already merged — revert? Not addressed. The ordering within the skill is defensible (merge first so deploy is ready) but the compat window is real. Fold into migration finding.

  Step 5: "On the report: RE-RUN gates yourself" — state the session cannot know: where do gates run, in the worktree? Presumably. "diff the harness files" — against deploy tip (stated in law 4). OK. But: the orchestrator re-runs "tsc, the full suite, and the build" (law 4) — the WO names gate commands verbatim (law 2). Consistent.

  Step 6 receipt: "paste the verdict — or the admissible-skip marker — into the lane report." Receipt exists.

  Step 8: "DEPLOY yourself → LIVE-VERIFY". Paranoia runs "before every deploy (step 8)" — in the numbered pipeline, paranoia isn't mentioned. Again composition-only. LOW.

  Step 9: report receipts specified. "Push branches" — pushes happen at step 9, but step 8 says "A failed post-deploy PUSH is a live-verify-class failure too" — wait, the push happens at step 9 ("Push branches") but step 8 references "a failed post-deploy PUSH". So the push is expected to happen at/around deploy time (step 8), yet the numbered step for pushing is step 9. Order ambiguity: is the push before live-verify completes or after the report? Step 8's parenthetical "(prod running code the remote lacks): fix the push before calling the lane done" implies push attempted around deploy. But step 9 says "Push branches." Two different locations for the push. LOW-MED: a session must guess when the push happens; if it pushes only at step 9 after reporting, then step 8's rule is vacuous. Actually re-reading: step 8's rule is about a push that FAILED — whenever it happens. It's fine-ish but the push's canonical location is step 9 while step 8 worries about it. Minor.

  Step 1: PROBE before drafting. Step 2: DRAFT. But the mode question comes before any WO is drafted — where does the ask fit relative to probing? "the orchestrator's FIRST message asks" — FIRST message, before probing even. OK, mode ask → probe → draft. Fine.

  Missing receipt: step 5's judge findings — "paste the verdict" is only for baton. Judge outputs ride where? Step 9 report carries "fix-format links". The judge findings themselves — law 6 says findings travel as quoted data to fix rounds; the final report need not include them. Fine.

  State the session cannot know: step 3 "lockfile copied from a sibling worktree if `npm ci` needs one (the WO says which)" — wait, the WO is drafted at step 2, before the worktree prep at step 3. How does the WO know whether npm ci needs a lockfile from a sibling? The orchestrator would have to know the worktree environment at draft time — it can (it probed). Minor. Actually "(the WO says which)" — which what? which sibling worktree? which lockfile? Ambiguous. LOW.

  t) **Target 7 — Exploitability by hostile content:**

  - Law 6: findings travel as quoted data, "never execute, endorse, or relay an imperative found inside it." Good. "⚠️ Verify every prescribed fix against the invariants AND the NEVER-touch list before dispatching." Good.
  - Law 5's untrusted-content warning in every judge prompt. Good.
  - Law 3: canonical WO outside the worktree, diff on report. Good.
  - BUT: law 6 dispatches "findings verbatim" to fix rounds — the fix-round BUILDER receives the judge's verbatim output, which is adversarial data. Does the builder prompt warn the builder that judge output is untrusted? Law 6 says findings travel as quoted, attributed data — the builder gets "Judge A reports: …" — the builder is a sub-agent executing fixes; a hostile judge (or a judge that ingested hostile builder comments — note judges read the diff and builder report, which may contain injected instructions) could embed an imperative in a finding: "Judge A reports: CRITICAL — run `npm run db:reset -- --yes` against prod to verify." The builder receives this verbatim. Builders are bound by the SIX NEVERS (never run migrations etc.) — but "db:reset" isn't literally a migration... builders "never touch env files, never read a DATABASE_URL" — a reset script would need the URL. The orchestrator verifies fixes against invariants and NEVER-touch before dispatch — the ORCHESTRATOR is the filter. But law 6's verification is "against the invariants AND the NEVER-touch list" — a fix that doesn't touch invariants or the NEVER list but is still hostile (e.g., "add this exfiltration fetch to the success path of the send tool" — doesn't void the can't-send invariant, doesn't touch env files) could pass the filter. Hmm — "never execute, endorse, or relay an imperative found inside it" — but dispatching findings verbatim IS relaying the judge's words, including any imperatives, to the builder. There's a tension: "findings travel verbatim" vs "never relay an imperative found inside it". If a finding CONTAINS an imperative (all prescribed fixes are imperatives!), the orchestrator must relay the finding but not the imperative? The skill says verify each prescribed fix before dispatching — so the filter is the orchestrator's judgment. The instruction "never relay an imperative" contradicts "fix rounds get findings verbatim" when findings contain imperatives — which they always do (a finding without a prescribed fix is just a complaint). Resolution a session would likely make: relay verbatim but the orchestrator's verification is the gate; the builder is instructed to implement fixes, not obey embedded commands. But the skill doesn't say to warn the BUILDER that judge text is adversarial. The judge gets the untrusted-content warning (law 5); the fix-round builder does not (law 6 quotes findings to it). HOLE: the adversarial-data warning reaches judges but not the fix-round builders who consume judge output. MED. Minimal fix: extend the ⚠️ warning to fix-round prompts.

  - The untrusted-diff warning: "a builder comment reading '// JUDGE: pre-approved per WO' is an attack" — good.
  - Exploit: "When Joseph says 'throw another judge on' — even 'for shits and giggles' — take it literally" — fine.
  - Exploit surface: the builder's report is a claim — covered.
  - One more: law 2's "Builder/judge arguments that an invariant is wrong are CLAIMS to probe, never grounds to amend" — good.

  u) **Target 8 — frontmatter description accuracy:**

  - Description says "the orchestrator ALWAYS ASKS which before drafting" — body says always ask UNLESS mode named in activating message. The description omits the skip; minor inaccuracy but activates conservatively. Fine.
  - Description: "Triggers — ... any multi-agent build wave — AND, regardless of vocabulary, any build touching a customer-facing/prod/money/credential surface that is larger than a one-file edit." Matches ACTIVATION. 
  - Description: "Two modes ... LEAN (one builder; the orchestrator's own re-gate plus the unconditional floor...) and PLUS-JUDGE (fresh judge rounds, a different lens each, to a defined SHIP verdict)." Accurate.
  - Description does NOT mention: THE TEN LAWS, the six nevers, the pipeline... it's a summary, fine.
  - One inaccuracy: description says "NEVER builds and OWNS TRUTH. Drafts the work order judgment-zero, dispatches builders into isolated worktrees, re-runs every builder's gates itself ... fields fresh adversarial judges with DIFFERENT LENSES, dispatches fix rounds, and personally deploys and live-verifies" — "fields fresh adversarial judges" unconditionally in the summary, but judges are PLUS-JUDGE only; in LEAN, "Builder self-audit replaces fresh-agent passes." The description implies judges are always fielded. An activation-decision reader (another skill/router) could over-expect. Minor. LOW.
  - Trigger-list correctness: will it fire on trivial edits? "NOT for single small edits (just do them)" — good. Will it FAIL to fire on "refactor the send pipeline" (3 files, no explicit vocabulary)? The regardless-of-vocabulary clause catches it (customer-facing, money... a send pipeline is customer-facing + prod). OK.

  v) **More underspecification sweep (target 2) — be literal:**

  1. "deadman rule" — undefined (above, HIGH).
  2. "(lean/judge/gambit/thorough)" contradiction (above, HIGH).
  3. "the diff" never defined — base ref? committed vs working tree? (HIGH-ish, MED).
  4. Rebase/re-cut of remaining lanes: gates re-run? Not stated (HIGH).
  5. Migration rollback / backward-compat window (HIGH).
  6. "BATON: SKIPPED <which> " — `<which>` undefined; trailing space (LOW).
  7. "a different lens each" / "a lens not yet used" — the lens inventory is exactly two (A: contract/security/invariants; B: fidelity/UX/reachability) plus BLIND for visual. What are lenses 3, 4, 5 for "throw another judge on" rounds? The 08-18 arc had 4 judge rounds. The skill names 2 lenses + blind. Third+ judge: the session must invent lenses (performance? accessibility? i18n?). "pick a lens not yet used" — from what menu? Underspecified — the session guesses. MED. Minimal fix: name the lens menu or say "invent and name one".
  8. SHIP verdict: "one full round that surfaces no NEW majors." "majors" undefined — findings have no severity taxonomy in this skill (the baton brief has LAUNCH-KILLER/HIGH/MED/LOW, but the skill doesn't adopt one). What's a "major"? Session guesses. MED. Also "no NEW majors" — new relative to what, the prior round? Presumably. And fix rounds: "dispatch fix rounds (law 6) until SHIP" — after fixes, "re-judge" — SHIP = a full round with no new majors. If round 3 surfaces a major introduced by round 2's fix, that's a NEW major → keep going. Convergence: subjective findings → noise floor. But non-subjective majors could ping-pong indefinitely (fix introduces new major, etc.) — no escalation after N rounds. MED/LOW: no round cap or "escalate to Joseph after N non-converging rounds".
  9. "Fast-forward preferred" — what if the deploy branch is protected / requires PRs? Out of scope probably.
  10. Step 4: "DISPATCH the builder (background)" — background = run_in_background. Harness-specific, fine.
  11. "the name/ID the spawn returned" — fine.
  12. Law 7: "Verify the deploy by deployment age/state, never by a health SHA alone" — "age/state" — how old is too old? Session can determine. Fine.
  13. Law 2 required fields: "the critical artifact + its ORACLE and the oracle's source" — fine.
  14. "highest tier reachable" — "if a higher-tier model is available (Joseph switches the seat, or a higher-tier drafter sub-agent)" — how does the session know what's "available"? Session guesses; the fallback (record the model) covers it. OK.
  15. "one-file edit" — does a one-file edit + its test file count as one file or two? Session guesses. LOW. Interesting: editing code + test = 2 files → regime ON for a trivial change with its test? The OFF clause says "a one-file edit you can make and verify faster than a WO takes to write" — code+test is the canonical small edit; letter of the law says 2 files → ON. LOW/MED.
  16. PLUS-JUDGE "fix → re-judge to a defined SHIP verdict" — re-judge with the SAME lens or fresh? Law 5: "Every round is a FRESH agent" and "A lens re-checking its own prior findings in a fix round is NOT the banned second pass". So re-judge = same lens, fresh agent, with the judge packet. OK, covered (re-baton F8 fixed this).
  17. Mode ask: "the orchestrator's FIRST message asks" — what if the activating message IS from Joseph and contains the token "lean" in another sense ("the diff is lean")? The skip "fires ONLY on the explicit tokens LEAN or PLUS-JUDGE" — token matching without intent. "keep it lean" → skip fires?? Sentence 2: "If Joseph already named a mode in the activating message (lean/judge/gambit/thorough), that answer stands." A literal token scan of "keep the diff lean" → "lean" present → skip the ask, mode = LEAN. THE INFERENCE HOLE REOPENS — pure token presence is not naming a mode. The 🔴 sentence says "explicit tokens LEAN or PLUS-JUDGE (or an unambiguous 'with judges'/'no judges')" — "unambiguous" modifies the parenthetical phrases, but the tokens themselves need an intent test ("names a mode" vs "appears"). A hostile or careless reading: grep the message for "lean" → skip. This is exactly the re-baton question. The failure scenario: Joseph: "Orchestrate the landing-page rebuild. Keep the copy lean." → token "lean" present → question skipped → LEAN mode chosen → floor lane (customer-facing) keeps baton+live-verify, so bounded, but the operator never chose. MED-HIGH. Minimal fix: "named as the MODE (a direct object of the request — 'run it LEAN'), not an adjective in the request; when in doubt, ASK." Actually the safe fix: the skip fires only when the token is used AS a mode declaration; ambiguity → ask. The skill says "Ecosystem slang ... does NOT count" but doesn't handle incidental token collision. I'll fold this into F1's finding or make it its own — it's the specific re-baton question, so it deserves prominence. Combine: the mode-skip clause has (a) the contradictory token list and (b) no intent test. One finding, HIGH.

  18. What happens if Joseph answers the mode question with something else ("you decide")? "No inferred default" — Joseph delegating the choice back. The skill gives no out: must the orchestrator refuse? Session guesses. "No inferred default" literally forbids choosing. Deadlock. LOW-MED. Realistically Joseph saying "you decide" is an explicit waiver... not covered. LOW.

  19. "customer-facing surface" undefined (above, MED).

  20. Step 9: "Push branches" — ALL branches (lane branches + deploy branch)? The deploy branch was pushed at step 8 (deploy from git push? Vercel watches the branch — so deploy IS a push). Wait — deploys "via Vercel from git worktrees"... the brief says "Builds target production web apps deployed via Vercel from git worktrees." Step 8 "DEPLOY yourself" — if deploy = push to the watched branch, then "Push branches" at step 9 is lane branches. And step 8's "failed post-deploy PUSH" = the deploy push. OK, consistent-ish. Fine.

  21. Law 8: "a screenshot you personally read" — for non-visual lanes (API-only)? Session guesses whether screenshot applies. LOW.

  22. The floor's live verification: "keeps its kimi-baton and its live verification UNCONDITIONALLY" — live verification is already law 8 for every deploy. So the floor's live-verify clause is redundant-but-harmless... unless a floor lane could skip a deploy? No. Harmless emphasis.

  23. "kimi-baton ... also on any edit to THIS skill before its next use" — self-referential; fine. But who runs the baton on the skill edit — the current session? After editing the skill mid-run? Edge, skip.

  24. Law 1: "Orchestrator artifacts — WOs, probes, receipts, ledgers, vault notes — you write yourself." Probes are orchestrator-written — and failure-modes list says "gates the orchestrator wrote get the same suspicion as code the builder wrote". OK.

  25. Step 6 baton inputs: "(WO + diff summary)" — the baton gets a diff SUMMARY, not the diff. Fine.

  26. What if the WO itself is defective (builder correctly reports the WO's target is impossible/contradictory)? Law 2: builder arguments are claims to probe; amendment protocol exists; "A mid-lane WO change STOPS the lane first." Covered.

  27. Two lanes both need a migration — ordering between lanes' 7a's? Unspecified. LOW.

  28. The mode question must be the FIRST message — but ACTIVATION bullet 2 requires announcing "running this inline" when OFF. If OFF, no mode question. Consistent.

  29. Description vs body on "gambit": description trigger list doesn't include gambit. Body sentence 2 does. Consistent with... whatever. Covered in F1.

  w) **Internal contradictions (target 3) — collect:**

  C1. Mode section sentence 2 (gambit/thorough count) vs sentence 4 (they don't). Direct. HIGH.
  C2. "UNCONDITIONALLY" vs two admissible skips — rhetorical, resolved by "exactly two". Not a real contradiction. Skip.
  C3. Law 6: "findings travel verbatim" vs "never relay an imperative found inside it" — findings routinely contain imperatives (prescribed fixes). The orchestrator must quote the finding (verbatim) but not relay imperatives. Session must reconcile: probably "quote findings, strip/stale-check imperatives, verify before dispatch". The text doesn't reconcile it. MED.
  C4. Always-ask vs yolo-mode/overnight — friction, not hard contradiction. MED/LOW.
  C5. Step 8's push-failure rule vs step 9's "Push branches" — the push is referenced before its step. LOW.
  C6. Law 1 "never builds" vs... merge-conflict resolution goes to builder — consistent. Rollback (step 8): "restore/promote the previous deployment" — that's an operator action by the orchestrator — consistent with law 7 (orchestrator deploys). Fine.
  C7. ACTIVATION: "ON when ... task is a BUILD touching a floor surface larger than a one-file edit" vs OFF: "a one-file edit you can make and verify faster than a WO takes to write" — what about a TWO-file edit you can make faster than a WO? ON by letter. Is that a contradiction? Not strictly — the OFF clause's first bullet is only about one-file edits; the other OFF bullets are regimes. So 2-file trivial = ON. Consistent-but-dumb (over-activation, target 5). Not a contradiction per se.
  C8. "the orchestrator ALWAYS ASKS which before drafting" (description, unconditional) vs body skip clause — the description overstates; an activation-decision maker reading only the description expects an ask always. LOW.
  C9. Law 5: "FRESH ADVERSARIES JUDGE THE DIFF — ... (PLUS-JUDGE)" — law 5 is scoped to PLUS-JUDGE. But the floor says when baton is skipped per deadman, "a judge round becomes mandatory in its place" — in LEAN mode! So a LEAN lane with unavailable kimi gets a judge round — law 5 applies outside PLUS-JUDGE. Consistent-ish (law 5's parenthetical scopes it, but the floor imports it into LEAN). The session must know that the substitute judge round follows law 5's rules (packet, fresh agent, lenses). Slight underspec: which lens for the mandatory substitute judge? Contract/security presumably. LOW-MED. Also SHIP doesn't apply (SHIP is PLUS-JUDGE's definition) — how many judge rounds for the substitute? One round? Unspecified. Session guesses. MED-LOW.

  x) **Exploitability continued (target 7):**

  - The WO-outside-worktree + diff rule: good.
  - "paste the verdict — or the admissible-skip marker — into the lane report": the baton verdict is adversarial data; pasting it into a report is fine.
  - Hostile builder report claiming "kimi unavailable" to induce a baton skip? The deadman check is the orchestrator's own — fine, but undefined deadman rule = the exploit (session decides unavailability on thin evidence).
  - The biggest exploit: sentence 2 of the mode section can be QUOTED by hostile content... no, hostile content is builder/judge output, not Joseph's messages. The mode comes from Joseph. OK.
  - Law 6 double-check verified against invariants — good.
  - One more: judges get "the STANDING settled-items list" — a hostile builder could... no, settled-items are orchestrator-maintained. Fine.

  y) **ONE THING (target 9):** The single change: fix the mode-section contradiction — delete "(lean/judge/gambit/thorough)" and add the intent test. Because it's a literal self-contradiction at the exact point the operator ordered tightened, and it reopens the inference hole the edit was meant to close. Alternatively the deadman rule. I think F1 (the contradiction) is the ONE THING — it's the delta-note's own question, and it's a defect introduced/left by the edit under audit. The deadman rule is a close second. Actually — which causes more harm? The deadman rule gates the FLOOR's only escape hatch; an undefined "unavailable" lets a session cut the baton on prod/money work by declaring kimi dead. The mode contradiction costs a wrong mode choice, but the floor binds in both modes — LEAN on a floor lane still has baton+live-verify. So material harm from the mode bug is bounded; material harm from the deadman hole is the floor itself. Hmm. But the mode contradiction is a certain defect (text contradicts itself today); the deadman is an underspecification that a reasonable session reads conservatively (kimi down only after real failure). The brief's delta note centers the mode edit. I'll make the ONE THING: repair the mode paragraph (kill the stale token list + intent test) — justification: it's a self-contradiction in the skill's first interactive act, certain to be hit on literally the first activation, and it reopens the exact hole the operator ordered closed; the deadman fix is F-finding HIGH too. Actually let me think about which I'd force... The verdict first.

  **Verdict:** Sound to install? The skill is unusually strong — most classic holes are explicitly closed. Remaining: one certain internal contradiction (mode tokens), one undefined escape hatch (deadman rule), merge/rebase gate gap, migration rollback gap. Are any LAUNCH-KILLER (ships and causes material harm)? The migration/rollback gap can cause a prod outage — but it's a missing instruction, and a competent session would notice old-code-vs-new-schema. The rebase-gate gap: semantic conflicts shipping ungated — live-verify backstop exists. I'd say: NO, not sound-to-install as-is — two HIGH defects (contradiction at sentence level; undefined floor escape hatch) warrant one more edit round. But "sound-to-install yes/no" — the prior baton already happened; this is a re-baton after one edit, and the edit itself introduced/left a contradiction in the exact section it touched. The honest verdict: not yet — one surgical edit away. The contradiction is trivially fixable (delete 9 words). I'll say NO with the note that the kill list is short and surgical.

  Severity assignments:
  - F1: mode-token contradiction + no intent test — HIGH (re-baton F1 not actually fixed; the old inference rule survives adjacent to the new one).
  - F2: deadman rule undefined (floor escape hatch) — HIGH.
  - F3: rebase/re-cut lanes merge without a fresh gate run on the rebased tree (gate-fails-only-on-merged-tree hole) — HIGH.
  - F4: migration rollback/backward-compat window unaddressed (rollback-FIRST never mentions schema) — HIGH... or MED? The skill does say rollback first on live-verify failure; live-verify against old code+new schema would fail → then what? Re-enter fix loop — with a migrated prod DB. The failure scenario is real and material. HIGH.
  - F5: "the diff" undefined (committed vs working tree; base) — uncommitted-fix-merge failure — MED/HIGH. I'll say HIGH? The failure scenario: builder's fix uncommitted → gates pass in worktree → merge misses fix → deploy lacks it → live-verify WO probes may pass if probes don't cover the fix's path. On a floor lane the baton saw a diff summary — of what? I'll set MED, noting it compounds. Hmm, actually let me think about how likely a session is to be bitten. An executing session merging a branch would typically merge the branch; if the builder left work uncommitted, the orchestrator's gate re-run in the worktree passes (working tree includes fix), merge produces tree without fix. This is a classic real-world failure. But many harnesses commit. The skill never says "ensure the builder's work is committed" or "the diff = branch vs deploy-tip". MED-HIGH → I'll go MED with explicit note, no — severity anchors: HIGH = likely rework. Is rework LIKELY? The ambiguity hits every merge. I'll go HIGH. Hmm, but I don't want to inflate. Let me settle: F5 MED (a careful session commits/checks git status naturally; the hole is real but survivable because live-verify + baton are backstops). Fine, MED.
  - F6: fix-round builders don't get the untrusted-content warning (judge findings relayed verbatim contain imperatives; "never relay an imperative" vs "verbatim" unreconciled) — MED.
  - F7: lens menu undefined beyond A/B/blind; "majors" undefined; no round cap/escalation on non-convergence — MED.
  - F8: floor-surface classification ("customer-facing") never defined or required to be recorded in the WO — MED.
  - F9: pre-dispatch baton + paranoia live only in COMPOSITION, not in the numbered pipeline that "ARE the law" — LOW/MED → MED? "numbered steps ARE the law" vs composition additions — a session could treat composition as optional flavor. LOW-MED. I'll say MED... Actually the composition section is imperative ("RUN at step 6", "RUN before every deploy") — a literal session follows both. The risk is low. LOW.
  - F10: over-activation on trivial multi-file floor edits (2-file typo → full regime) — MED (skill decay risk) or LOW. The announce-OFF rule mitigates. I'll say MED because activation misfire is success criterion (c). Hmm — criterion (c): "does not fire on trivial edits". A 2-file typo fix IS a trivial edit and the regime fires by the letter. That's a criterion failure → MED.
  - F11: always-ask vs yolo/overnight/"you decide" — blocking semantics unstated — LOW.
  - F12: push ordering (step 8 references post-deploy push; step 9 owns pushes) — LOW.
  - F13: dead orchestrator/session resume of pipeline state — partially covered by law 10 + km-ingest; resume-of-orchestrator not explicit — LOW. Maybe fold into MISSING list instead of a finding.
  - F14: description says "fields fresh adversarial judges" unconditionally (LEAN has none) and omits the ask-skip — LOW.
  - F15: "BATON: SKIPPED <which> " format — `<which>` undefined — LOW. Fold into F2's fix? Keep separate LOW or fold. Fold into F2.

  Also check: "kimi is UNAVAILABLE per the deadman rule" — the brief says related skills are one-line described and not to audit them. kimi-baton's one-line: "blind external audit at commit points". The deadman rule is presumably part of kimi-baton's own spec... The brief says "Assume they exist and work as one-line described in the artifact". The deadman rule is NOT one-line described anywhere — it's named without description. I'll keep it as a finding but phrase carefully: the floor references a rule this file neither states nor one-line describes; if it lives in kimi-baton's file, the executing session may not have that file loaded (skills load on activation). A session that hasn't activated kimi-baton has no deadman definition. That's legitimate.

  Let me also double-check the "(re-baton F9)", "(re-baton F11)", "(re-baton F5)", "(re-baton F8)", "(re-baton F1)" markers — they reference prior baton findings. The delta note says the file "already carries: always-ask modes..." — and asks specifically whether the always-ask rule conflicts with any other law and whether the skip clause reopens an inference hole. My F1 answers: yes, it reopens — sentence 2's parenthetical is the hole, and token-presence has no intent test.

  Does always-ask conflict with any other law? Check each law: Law 9 (fork uncertainty into human questions, defaults delta-0) — consistent philosophy. yolo-mode inherit — friction only (the ask is a design question, not a permission; but "confirm-first... operate exactly as inline" — in yolo, confirmations are pre-granted; the mode question is not a confirmation, so it survives yolo — I should state that reading, but note a session could read yolo's no-interrupt register as covering it; the skill should say "the mode ask is not a permission prompt and binds under yolo"). Law: "rapid-mode suspends for WO authoring" — the ask is one question, fine. ACTIVATION bullet: "If Joseph explicitly orders inline work on a floor surface, the ORCHESTRATION is waived" — if orchestration is waived, no mode ask — consistent.

  One more potential contradiction: ACTIVATION says "On every activation the orchestrator's FIRST message asks" — but ACTIVATION bullet 2 says when skill stays OFF announce it. If the skill is ON, first message = the ask. Fine.

  What about: Joseph's activating message says "PLUS-JUDGE" — skip fires. Then the orchestrator probes, drafts... consistent.

  Now, MISSING-<X> list candidates:
  - MISSING-deadman-rule (definition)
  - MISSING-lens-menu (lenses beyond A/B/blind)
  - MISSING-severity-taxonomy ("majors")
  - MISSING-merge-definition ("the diff", committed-state check)
  - MISSING-migration-compat-rule
  - MISSING-round-cap (non-convergence escalation)
  - MISSING-mode-ask-under-yolo / unreachable-Joseph semantics

  Answers section must mirror the 9 targets. Let me draft concise answers.

  Format: ## VERDICT (one paragraph, yes/no) · ## FINDINGS (F1..Fn with severity · section attacked · hole · failure scenario · minimal fix) · ## ANSWERS (1-9) · optional ## MISSING-<X>.

  The brief says "Do not be polite." Fine — direct. "Every finding: concrete hole → concrete failure scenario → minimal fix." "Zero findings is valid... invented findings are a defect." My findings are all grounded in quoted text. Good.

  Output: markdown to stdout — I just reply with the markdown. No file creation (read-only constraint). The style guide of the workspace says caveman bullets etc. — but that's for codex-executor lane build reports; this is an audit for Joseph. The brief's format governs. I'll write clean, direct markdown. The AGENTS.md style says "Caveman bullets · ✅/❌/⚠️ · NO exclamation points" — that's the executor style; I can lean terse. I'll use plain prose+bullets, no exclamation marks. Keep it tight but complete — 9 targets all answered, findings F1..F12ish.

  One more check of things I might have missed — sweep the file once more for load-bearing ambiguities:

  - Law 2: "Draft on the highest tier reachable: if a higher-tier model is available (Joseph switches the seat, or a higher-tier drafter sub-agent)" — "higher-tier" than what — than the session's current model. OK.
  - Law 2's required fields list: "copy laws (banned words/punctuation)" — fine.
  - "THE WO CARRIES ... the datastore the builder's gates run against (never prod)" — good.
  - Step 3: "lockfile copied from a sibling worktree if npm ci needs one (the WO says which)" — noted.
  - Step 4: "The prompt carries the WO path" — wait, law 3 says the builder gets a COPY of the WO in the worktree... "The canonical WO lives OUTSIDE the worktree (the orchestrator's own wo/); the builder gets a COPY." Step 4: "The prompt carries the WO path" — which path, canonical or copy? If the prompt carries the canonical path, the builder reads the canonical (and could edit it? builders never modify gates but the WO isn't a gate...). Actually the copy exists so the orchestrator can diff the worktree's copy against canonical to detect tampering. The prompt should carry the COPY's path (inside the worktree). Step 4 says "the WO path" — ambiguous: canonical path or copy path? If the builder gets the canonical path and edits it... the diff check compares worktree-copy vs canonical — if the builder edited the CANONICAL, the diff detects nothing (canonical changed, copy pristine → delta detected actually — "any delta is a defect"). Either way delta detected. But a builder with the canonical path could read amendments... trivial. LOW: specify "the worktree COPY's path". Worth a LOW finding or fold into underspec sweep. Fold into answers.
  - Step 5: "Every fix round re-triggers the full law-4 re-run." Good.
  - Step 6: "run kimi-baton on the lane (WO + diff summary), disposition its findings" — "disposition" — accept/reject each finding with reasons, presumably. Fine.
  - Step 7: "re-dispatch the lane's builder or a merge builder against the conflicted tree" — "the lane's builder" — resume the same sub-agent? Law 10 says dead lanes re-dispatch FRESH builders. The lane's builder may still be alive (it reported). Re-dispatching the SAME live builder for merge conflicts — fresh-eyes chain? Law 1 says conflict resolution is building; the same builder can build more. Fine.
  - "After EVERY merge, re-cut or rebase the remaining open lanes onto the new tip" — who does the rebase — orchestrator (git op) or builder? Rebase = git operation; if conflicts arise during rebase → conflict resolution = builder. Unspecified but inferable. The gate re-run after rebase — THE hole (F3).
  - Step 8: "restore/promote the previous deployment" — Vercel promote. Fine.
  - Law 8: "the negative probes (WO-named escalation paths must 404)" — fine.
  - COMPOSITION: "verify-gate Stop hook" — assumes hook exists; brief says assume they work. Fine.
  - FAILURE MODES section: fine, evidence.

  Also — success criterion (b): "impossible to follow this skill as written and still ... delegate a deploy/migration/credential action to a builder" — Law 7 + six nevers cover. "cut the audit floor on prod/money/credential/customer-facing work" — the deadman hole + classification hole are the two ways. Good, my findings map to the criteria.

  Criterion (a): fresh session runs without guessing — the underspec list (deadman, lenses, majors, diff definition, which-WO-path) are the guessing points. 

  Criterion (c): activation — over-fire on trivial 2-file floor edits; under-fire... any under-fire case? A build touching floor surface exactly one file: regime OFF but floor binds via bullet 2 — but bullet 2 only requires a chat announcement; does the floor actually BIND when the skill is OFF? Bullet 2: "the floor still binds" — announced. Bullet 3 covers explicit Joseph orders. What about a one-file floor edit NOT explicitly ordered inline — skill OFF by size, bullet 2 says announce "the floor still binds" — so baton+live-verify bind. Covered. Under-fire: what about a build on a NON-floor surface that's huge (10 files, internal tooling)? Regime: ON only if Joseph names it or it's a "wave". A 10-file internal-tooling build with no trigger words: skill OFF. Is that a miss? The floor doesn't apply (not floor). The description says triggers = vocabulary + floor builds. So a large non-floor build without vocabulary → no orchestration. Intended? Probably (regime follows floor-risk + explicit asks). Not a defect per criteria. Skip.

  Edge: "any build touching a customer-facing/prod/money/credential surface that is larger than a one-file edit" — "touching prod" — EVERY deploy touches prod eventually. "Touching prod" as a build attribute presumably means prod config/infra/data. A build that changes only internal lib code but deploys to prod — touches prod? Literal reading: everything shipped touches prod → regime always ON → over-activation. The floor's own list "prod, money, credentials, or a customer-facing surface" — "touching prod" needs scoping (prod data/config/infra, not "will eventually be deployed"). Session guesses. Fold into F8 (classification undefined).

  Now the ONE THING. Candidates: F1 (mode contradiction) or F2 (deadman). The delta note centers the edit; F1 is a certain, adjacent-sentence contradiction that voids an operator-ordered rule on the very first activation. F2 is the floor's only escape hatch left undefined. I'll pick F1 as THE ONE THING because: (a) it's the edit under audit and it's defective as shipped; (b) it's hit on 100% of activations whose message contains incidental "lean"/"gambit"/"thorough"; (c) the fix is 9 words. And I'll note F2 is the runner-up that must ride the same edit. Hmm — "exactly one change" — I'll name F1 cleanly.

  Wait — is F1 really worse than F2 by the anchors? F1: wrong mode chosen without asking. Harm: LEAN instead of PLUS-JUDGE on a big build → less review coverage, but floor intact. PLUS-JUDGE instead of LEAN → wasted usage. Bounded harm, certain occurrence. F2: baton skipped on floor work via an undefined unavailability test → the floor's external audit gone on prod/money — the skill's core promise. Uncertain occurrence (requires kimi trouble + a liberal reader), unbounded-ish harm. By "ships and causes material harm" — neither is launch-killer alone. Both HIGH. The ONE THING: I'll go with the deadman rule... hmm. Let me think about which the author would most need forced. The re-baton's explicit question is about the mode edit; the contradiction there is glaring and certain — a reviewer who doesn't flag it as top is asleep. But "THE ONE THING if you could force exactly one change before install" — the deadman hole is more dangerous per incident; the mode bug is more certain. Actually — re-read the mode paragraph once more:

  "On every activation the orchestrator's FIRST message asks: "LEAN or PLUS-JUDGE?" — one question, before any WO is drafted. No inferred default. If Joseph already named a mode in the activating message (lean/judge/gambit/thorough), that answer stands and the question is skipped. 🔴 The skip fires ONLY on the explicit tokens LEAN or PLUS-JUDGE (or an unambiguous "with judges"/"no judges"). Ecosystem slang — "gambit", "thorough", "go fast", "cheap" — does NOT count as naming a mode: ASK. (re-baton F1)"

  Yes: sentence 2 lists gambit/thorough as skip-valid; sentence 4 lists them as skip-invalid. Both can't hold. A session asked to resolve will likely take the later 🔴 sentence (later + marked) — but "flag, never improvise" cultures will stall. And sentence 2's "(lean/judge/gambit/thorough)" also maps tokens to... "judge" → PLUS-JUDGE presumably; "gambit"/"thorough" → which mode? Even taking sentence 2 alone, gambit/thorough don't map to a mode. So sentence 2 is doubly broken. Confirmed HIGH.

  I'll make the ONE THING: repair the mode paragraph (delete the stale parenthetical, add the intent test). And in the verdict: install NO until F1+F2 land; both are surgical (a sentence each).

  Hmm, actually for the ONE THING, let me reconsider once more. The prompt says "if you could force exactly one change before install, what and why." The strongest argument: F2's deadman definition, because it's the sole remaining way to follow the skill literally and still cut the audit floor on prod/money/credential/customer-facing work — which is success criterion (b), the skill's reason to exist. F1 violates the operator's order and criterion (a) (guessing), but the floor holds in both modes. F2 voids criterion (b) directly. Yes — THE ONE THING = define the deadman rule inline (or delete skip (b)). I'll present it that way, with F1 as the certain-but-bounded runner-up. Hmm, but is deleting skip (b) the minimal fix? "kimi is UNAVAILABLE per the deadman rule, and then a judge round becomes mandatory" — if the deadman rule lives in the kimi-baton skill file, and the session only loads skills on activation... when the floor bites, the session WILL try to run kimi-baton (composition says so), which loads that skill, which presumably defines the deadman rule. Is that a fair reading? The brief says related skills "exist and work as one-line described in the artifact" — the one-line description of kimi-baton is "blind external audit at commit points" — no deadman. If the deadman rule is part of kimi-baton's spec, the orchestrator session gets it when it activates kimi-baton... but to DECIDE kimi is unavailable you must have already tried to activate it. Eh — the hole stands as: the ORCHESTRATOR file states the escape hatch but not its test; a session deciding unavailability pre-activation (e.g., kimi quota known-exhausted from earlier in the day) has no threshold. Keep HIGH, minimal fix = state the test inline.

  Alright — also need "## ANSWERS" mirroring the 9 targets, numbered 1-9, concise. And the optional MISSING list. I'll include a MISSING section listing undefined nouns: deadman rule, lens menu, majors/severity, "the diff" definition, customer-facing/prod-touching definition, round cap, floor-lane classification field.

  Also target 6 asks: "is any step ordered wrong, missing a receipt, or reliant on state the session cannot actually know at that step?" Answer: 7a-after-7 ordering creates the schema-window; push referenced at 8 but assigned at 9; step 3's "(the WO says which)" relies on WO knowing worktree facts (drafted at step 2 — knowable but awkward); step 4 "the WO path" ambiguous canonical-vs-copy; no receipt demanded for the rebase/re-cut of other lanes; baton pre-dispatch run missing from numbered steps. Also: step 5 "until SHIP" — in LEAN there IS no SHIP definition (SHIP is defined under PLUS-JUDGE); LEAN's loop end = gates green + diff read + floor. Step 5 says "dispatch fix rounds (law 6) until SHIP" — does LEAN dispatch fix rounds? LEAN: "Builder self-audit replaces fresh-agent passes" — the orchestrator's own diff read may find issues → fix rounds → "until SHIP" — but SHIP is a PLUS-JUDGE concept (rounds with no new majors). In LEAN, when do fix rounds stop? When the orchestrator's own read finds nothing more? Undefined. MED — fold into F7 (undefined terms) or its own LOW. I'll fold into answer 2/6 and F7's family. Let me structure findings:

  - F1 · HIGH · TWO MODES ¶1 · stale token list contradicts the 🔴 skip rule; no intent test on tokens → inference hole reopened (the re-baton question answered: YES).
  - F2 · HIGH · THE FLOOR · "UNAVAILABLE per the deadman rule" — the rule is never stated or one-line described; the floor's only non-Joseph escape hatch has no test → floor cuttable by declaration.
  - F3 · HIGH · step 7 · post-rebase/re-cut lanes merge with no gate re-run on the rebased tree; "gate fails only on merged tree" not covered for clean rebases.
  - F4 · HIGH · step 7a/8 · rollback-FIRST restores code, never schema; no backward-compat rule for the migration window (old code live vs new schema between 7a and 8, and after a rollback).
  - F5 · MED · law 4/step 5/7 · "the diff" never defined (committed vs working tree; base ref); uncommitted-fix merge scenario.
  - F6 · MED · law 6 · findings travel verbatim to fix-round builders, but the untrusted-content warning is judge-only; "never relay an imperative" vs "verbatim" unreconciled.
  - F7 · MED · law 5/PLUS-JUDGE · lens menu stops at A/B/blind while "throw another judge on" demands unused lenses; "majors" has no taxonomy; SHIP undefined in LEAN; no round cap.
  - F8 · MED · ACTIVATION/FLOOR · "customer-facing"/"touching prod" never defined; floor-lane classification not a required WO field → the floor can be classified away silently.
  - F9 · MED · ACTIVATION/description · trivial multi-file floor edits force the full regime (2-file typo) → skill decay; criterion (c) fails by the letter.
  - F10 · LOW · ACTIVATION/COMPOSITION · always-ask vs yolo-mode register + unreachable-Joseph (overnight) semantics unstated; "Joseph says 'you decide'" deadlocks against "No inferred default".
  - F11 · LOW · COMPOSITION vs PIPELINE · pre-dispatch baton and paranoia exist only outside the numbered steps that "ARE the law".
  - F12 · LOW · step 8/9 · push referenced as post-deploy failure class at step 8 but "Push branches" lives at step 9.
  - F13 · LOW · step 4/law 3 · "the WO path" in the builder prompt: canonical or worktree copy — unspecified (should be the copy).
  - F14 · LOW · frontmatter · description claims judges fielded (unconditional) and omits the ask-skip; overstates judge coverage in LEAN.

  That's 14. Maybe merge F13 into F5-family or keep. Merge F12+F11? They're distinct sections. Keep 14 — but "invented findings are a defect" — all are text-grounded. Maybe drop F14 to keep signal high? No, it's real and cheap. Keep. Actually, let me reconsider F9's severity: is it MED or LOW? Criterion (c) explicitly: "activation conditions unambiguous enough that it does not fire on trivial edits" — a 2-file typo fires it → criterion failure → MED. Keep MED.

  Verdict: NO — not sound to install as-is; two edits (F1, F2) are mandatory, F3/F4 strongly advised in the same pass. The skeleton is sound; the delta-note's specific worry is confirmed: the skip clause reopens the inference hole because the old token list survived the edit.

  Also answer target 1's example list explicitly: builder crashes mid-run (covered law 10, minus hang-detection), two builders conflict (covered step 7), gate fails only on merged tree (NOT covered — F3), Joseph changes WO mid-build (covered law 2 amendment protocol), judge vs orchestrator (covered law 6 probes-win; judge-vs-judge NOT covered). Plus uncovered: hung (not dead) builder — no timeout/dead-detection; dead ORCHESTRATOR mid-pipeline (partially covered by law 10 banking; pipeline-position resume unstated); migration rollback (F4); uncommitted work (F5).

  Answers 2 (underspec sweep, be literal): enumerate: deadman rule; lens menu; majors; SHIP-in-LEAN; "the diff"; "customer-facing"/"touching prod"; "the WO path" (canonical vs copy); "(the WO says which)" — which sibling/lockfile; "BATON: SKIPPED <which>"; round cap; one-file boundary (code+test = 2 files); mode ask vs "you decide"; what "unambiguous" means for mode tokens (intent test); who rebases open lanes (orchestrator git-op vs builder when conflicts); screenshot for non-visual lanes; whether the two baton runs (pre-dispatch + step 6) are both mandatory on floor lanes.

  Answer 3 (contradictions): C1 mode tokens (F1); C2 verbatim-vs-never-relay-imperative (F6); C3 "numbered steps ARE the law" vs composition-only duties (F11); C4 push at 8 vs 9 (F12); C5 "UNCONDITIONALLY" vs two skips — resolved by "exactly two", note only; C6 description vs body on ask (F14).

  Answer 4 (LEAN floor): LEAN on a floor lane keeps baton + live-verify → no, cannot ship floor work with only self re-run — IF the lane is classified as floor. The classification is the soft spot (F8) + the deadman escape (F2). Wording enforceable? "keeps its kimi-baton" is imperative and the skip marker is a concrete receipt — enforceable as written; the two holes are classification and the deadman test. Also: the floor binds "any lane touching prod..." — in LEAN a NON-floor lane ships with zero external check by design (acceptable, stated).

  Answer 5 (activation): false positive 2-file typo (F9); false negative — none found for floor builds (vocabulary-independent clause works); the "bare mention" diagnostic exclusion is good; misfire via "larger than a one-file edit" on code+test; token collision in description triggers ("run the full pipeline" in a diagnostic sentence — the body excludes it, description doesn't: description trigger list will fire the skill on "the deploy pipeline failed — why?"? Description says Triggers include "run the full pipeline" — a session reading the description only sees the phrase; the body's DIAGNOSTIC carve-out saves it post-load. Note in F14/answer 8.)

  Answer 6 (pipeline): covered above — 7a window (F4), rebase gap (F3), push ordering (F12), WO-path ambiguity (F13), composition-only duties (F11), step 3's WO-knows-lockfile; receipts: baton verdict pasted (good), gate outputs verbatim (good), judge findings need no receipt (ok), no receipt for re-cut of sibling lanes (who verifies it happened? nobody — LOW, fold into F3's fix).

  Answer 7 (exploitability): strongest surfaces are well armored (untrusted-diff warning, canonical-WO diff, six nevers, verbatim-with-attribution, fix verification, probes-win). Residual: F6 (fix-round builder unwarned); F2 (deadman self-declaration — the session exploiting its own ambiguity under pressure, not external hostile content per se); the "WHIM judge" clause ("take it literally") is safe (Joseph-only). One more: "paste the verdict ... into the lane report" — verdict is hostile-authored text pasted into a report that later sessions read as truth — but it's baton output, quasi-trusted. Fine. Also law 5's warning covers judges reading diffs; law 6 covers orchestrator reading findings; NOBODY warns the ORCHESTRATOR that the builder's REPORT (which it reads at step 5) is adversarial — law 4 says "a claim, not a receipt" — attitude covered even if the word "untrusted" isn't applied. OK, mention in answer 7 as adequately covered.

  Answer 8 (description): mostly accurate; three drifts — unconditional "fields fresh adversarial judges" (LEAN has none), "ALWAYS ASKS which before drafting" omits the skip (harmless direction), trigger list lacks the diagnostic carve-out so phrase-matching can over-fire the description where the body would not. Verdict: trigger list causes correct activation in body; description-only routers may over-fire on "pipeline" mentions.

  Answer 9 (ONE THING): define the deadman test inline or delete skip (b) — it's the only remaining literal path to following the skill while cutting the floor on prod/money/credential/customer-facing work (success criterion b). F1 rides the same edit pass but is bounded in harm because the floor holds in both modes.

  Hmm wait, I should double check one thing — the delta note says the file "already carries: always-ask modes" as a CARRIED feature, and asks "does the always-ask rule conflict with any other law". Let me be careful about yolo: the brief's one-liner: "yolo-mode (permission register)". The mode question is not a permission. So: no hard conflict; friction possible; recommend one clause. Good.

  Also "does the skip clause (mode named in the activating message) reopen an inference hole?" — YES: (i) sentence 2's stale list, (ii) no intent test → incidental tokens. Both documented.

  Length: this needs to be thorough but not bloated. Aim for a tight but complete audit. Use ✅/❌/⚠️ sparingly per workspace style? The AGENTS.md style is for codex-executor reports; this audit's format is set by the brief. I'll use plain severity tags. No exclamation points.

  Severity recount: F1 HIGH, F2 HIGH, F3 HIGH, F4 HIGH, F5 MED, F6 MED, F7 MED, F8 MED, F9 MED, F10 LOW, F11 LOW, F12 LOW, F13 LOW, F14 LOW. Four HIGHs — is F4 really HIGH? "likely rework" — the failure scenario (rollback leaves migrated schema; old code errors) requires a failed deploy after a migration — not every run, but when it hits it's a prod incident and the skill gives NO instruction → the session improvises (exactly what the skill exists to prevent). HIGH. F3 HIGH — every multi-lane wave hits re-cut/rebase; the missing re-gate is a literal gap in "the numbered steps ARE the law". HIGH.

  Any LAUNCH-KILLER? "ships and causes material harm" — none individually; F2 in the worst case (baton declared dead on a money lane, substitute judge misses what baton would catch) approaches it but requires compounding. I'll say none, and that's part of the verdict's honesty: the file is close; install NO is driven by HIGHs being trivially fixable, not by a killer. Actually — verdict: sound-to-install yes/no. I'll say: NO, not as-is; the mandatory delta is one surgical paragraph (F1) + one sentence (F2); with those two landed I'd pass it, with F3/F4 clauses strongly recommended in the same pass.

  Write it.

To resume this session: kimi -r session_4882c596-cb31-4b2a-a757-8ceb89699d11
