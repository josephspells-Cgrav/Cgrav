• Let me read the brief file.

• Now I need to produce the audit. The brief restricts me to those files, but I noticed the WO references things I can't verify (lib/cities.ts is not in my permitted read list — the WO says city pages render from lib/cities.ts; page.tsx imports getCity from @/lib/cities — I'm only allowed the 5 files). I should answer based on what I can see.

  Let me analyze:

  **1. Underspecification sweep:**

  - The WO says webPageNode `name: <the page's existing title phrasing>` and `description: <the city metaDescription or its existing fallback>`. But the actual title and description construction lives in `generateMetadata` (lines 20-27 of page.tsx): title = `${c.titleKeyword ?? "Roofing Contractor"} ${c.name}, NC | Mabrey Roofing` with the business name baked in, and description has a fallback template referencing BUSINESS.phone. The WO does not say whether to duplicate this logic inline (drift risk), extract a shared helper (but lib/ is read-only — NEVER-TOUCH lib/**, and the page can't import a new helper), or import pageMetadata. The exemplar builds the graph name independently from metadata (`${t.term} — Roofing Glossary` vs metadata title `What Is ${t.term}? | Mabrey Roofing`) — so the exemplar itself does NOT mirror the metadata title; the WO's "existing title phrasing" is ambiguous and the exemplar misleads. The builder must guess.
  - Duplication: page.tsx currently builds the title/description strings only inside generateMetadata. The WO says ONLY-THIS-FILE page.tsx and lib is read-only. So the builder must either duplicate the title/description string construction in the default export (drift risk if one changes) or refactor within the file (hoist a local function). Underspecified but solvable.
  - Breadcrumb middle item: WO says "Service Areas /service-areas — verify /service-areas is the real hub route (probe it...)". But wait — the page comment says "Canonical location scheme: /locations/[city]-nc (Phase-0 migration from /service-areas/[city])" — the site migrated AWAY from /service-areas. So /service-areas may not exist as a hub route at all; middleware 301s combos. The WO's own guess for the middle item is likely wrong — it hedges with "probe it" but the builder (K3, isolated, WO says gates include reachability) can probe. The underspecification is acknowledged in the WO, so it's partially mitigated, but the fallback instruction ("if the site's breadcrumb convention elsewhere differs, follow the existing convention and record it") is vague — which convention? The visible Breadcrumbs component? Location page's CityPage component (in components/, which is never-touch but readable?) — wait, the WO's ONLY-THESE-FILES is about writing; the builder can read components/CityPage.tsx. But I, the auditor, can't see CityPage — the visible breadcrumb rendering on location pages is unknown to me. Also does the location page even render visible breadcrumbs? The glossary exemplar renders Breadcrumbs; location page delegates to CityPage. If the location page has NO visible breadcrumb trail, emitting a BreadcrumbList that doesn't match a visible trail is against Google guidance (breadcrumb markup should reflect the page's visible breadcrumb). I can't verify. Flag as a guess point / unverifiable.
  - Also the WO breadcrumb plan says item name "Service Areas" with url /service-areas — if that route 404s or redirects, reachability-check gate might catch it, or not (does reachability check crawl schema URLs? unknown).
  - speakable: [".seo-answer"] — the WO claims the .seo-answer block exists on location pages (§1 "PROBED"). page.tsx doesn't show it; it's inside CityPage component. Can't verify from permitted files, but the WO asserts it was probed.

  **2. Collision with existing page.tsx:**

  - page.tsx is a thin wrapper: generateStaticParams, generateMetadata, default export rendering `<CityPage city={c} />`. No JsonLd import — the WO's "no JsonLd today" claim is true for this file (line: "NO JsonLd, NO schema import" confirmed by read; but whether CityPage component emits JsonLd is unknowable from permitted files — the WO probe claim was "no JsonLd import anywhere under app/locations/", which wouldn't cover components/CityPage.tsx importing JsonLd. That's a gap in the probe: if CityPage already emits a graph, adding one in page.tsx creates TWO ld+json blocks, violating I1 "exactly ONE new block" — well, I1 says gains exactly one new block, so two blocks total would still be "one new" but could create duplicate WebPage/FAQPage. Hmm, I1 says the page gains exactly one new script block; it doesn't forbid pre-existing ones. But success criteria in the brief says "all 20 pages emit one valid graph". If CityPage already emits schema (e.g. serviceNode per its comment in schema.ts line 237: "Used by the Service nodes on the service-detail pages AND the city pages"), then city pages DO already emit schema via CityPage! That's a big one: lib/schema.ts's REPLACEMENT_PRICE_RANGE comment says it's used by Service nodes "on the service-detail pages AND the city pages" — strong evidence city pages already emit Service nodes somewhere (likely components/CityPage.tsx). The WO's claim "ZERO structured data" on location pages is then false — the probe only checked app/locations/ for JsonLd imports, but the JsonLd would be in components/CityPage.tsx. This is a HIGH finding: the premise may be wrong, and adding a WebPage node would create a second WebPage @id collision or duplicate graphs. The oracle counts ld+json blocks per page, so it might catch duplicates — the oracle asserts presence of types but the brief's success criteria says "one valid graph". The WO's oracle doesn't assert exactly-one-block or no-duplicate-WebPage. Actually I1 does: "gains exactly ONE new <script>" — doesn't cap total. And the oracle doesn't check for duplicate WebPage nodes. So a pre-existing CityPage graph would slip through gates. HIGH.
  - The [sub] route: WO says DO NOT TOUCH — fine, and middleware 301s. No collision.
  - generateMetadata: no collision; the WO doesn't change metadata. But the builder needs the same strings in the page body — must duplicate logic (see #1).

  **3. Signature match:**

  - webPageNode({url, name, description, speakable}) — matches. speakable: string[] → cssSelector. OK.
  - breadcrumbNode(items: {name, url}[]) — WO passes [Home /, Service Areas /service-areas, City /locations/<slug>] — matches shape.
  - faqNode(faqs: {q,a}[]) — city.faqs is CityFaq[] with {q,a} per WO; type should match if CityFaq = {q,a}. Can't verify lib/cities.ts (not permitted). WO §1 says faqs: CityFaq[] = 3-4 {q,a} pairs. Assume ok but unverified by me.
  - locationNode: mints `@type: ["RoofingContractor", "LocalBusiness"]` — a LocalBusiness-type node! The WO's conditional instruction says: "If locationNode turns out to mint a LocalBusiness-type node, DO NOT use it." It does mint exactly that. So the answer is determined: DO NOT use locationNode. The conditional instruction is sound and the read resolves it — good WO design here. But note I4 says "unless the recorded locationNode decision adds a pure Place node" — locationNode is not a pure Place node, so no fourth node. Consistent. However there's a subtle trap: a lazy builder might use locationNode anyway because its doc comment says "A location-specific RoofingContractor sub-entity (WS-B location pages)" — i.e., lib/schema.ts's own comment says it was BUILT for these location pages. The WO's prohibition directly contradicts the helper's stated purpose. The WO anticipates this ("read it before deciding"). It's sound, but the builder sees conflicting signals: helper docs say "for WS-B location pages," WO says don't use if LocalBusiness. WO wins per its own text. MED at most — the WO is explicit enough. Actually the WO handles it well. Fine.
  - One more: webPageNode sets `about: { "@id": SITE.orgId }` and `isPartOf: { "@id": SITE.websiteId }` — @id references, not full nodes, so no duplicate entity. Good — complies with the no-duplicate-org law. The WO doesn't mention this; it's fine.

  **4. Compliance:**

  - FAQ-dedup law (lib/faqs.ts): the dedup law as actually implemented concerns TOPIC_OWNED_QUESTIONS — questions shared between /faq hub and dedicated topic pages. City faqs come from lib/cities.ts, a different source. The WO claims city faqs are "per-city unique (doorway gate enforces textual uniqueness) and none are marked up anywhere today." Risk: a city FAQ question might textually equal one of the site-wide FAQ questions already marked up on a topic page (e.g., "Do you offer financing?"). The WO's claim of uniqueness is based on the doorway gate enforcing per-city uniqueness — but does the doorway gate check city faqs against lib/faqs.ts site-wide questions? Unknown; the WO asserts uniqueness across cities, not against the global FAQ set. If a city faq duplicates "How much does a new roof cost in Durham?" (plausible for a Durham location page!), we'd get two FAQPage blocks site-wide with the same question → violates the dedup law. Notably the /faq hub's first question is literally "How much does a new roof cost in Durham?" — a Durham city page FAQ could easily collide. The oracle/gates don't check cross-page question uniqueness. MED-HIGH. The builder should diff all 60-80 city questions against lib/faqs.ts questions + each other; WO doesn't instruct that. WO asserts "none are marked up anywhere today" — probed? It says PROBED only for JsonLd under app/locations. The dedup safety claim is asserted, not shown. Finding: MED (likely rework if collision exists) — I'll say MED with instruction to add a cross-check to the oracle.
  - Google FAQ policy: markup must mirror rendered text. faqNode uses f.q/f.a verbatim — but does CityPage render faqs verbatim? Probably (FAQ accordion). I6 covers verbatim. But: does CityPage render ALL of city.faqs visibly? If it renders only some, markup includes non-rendered Q&A → policy violation. Can't verify from permitted files. The oracle checks count == faqs.length but not rendered-text mirror. MED/low. Also note schema.ts's own comment (line 320-323): FAQPage is DEAD in Google as a rich result; "Keep FAQ/how-to as answer-first BODY COPY... faqNode retained (zero harm...)". So adding FAQPage is not a Google lever — the WO's WHY (SERP presentation) partially rests on breadcrumbs (real) and FAQ (dead rich result). Not a defect per se — "zero harm, parsed by non-Google AI" — but the WO overstates the benefit. LOW.
  - Entity merge risk: the three instructed nodes contain no LocalBusiness; webPageNode references org by @id only. Safe. breadcrumbNode/faqNode mint no entities. Good.
  - aggregateRating: none of the three helpers emit it. Safe. I3 check is fine.
  - Also: FAQPage + WebPage on same page — fine.

  Wait, one more compliance item: Google deprecated FAQ rich results (Aug 2023) except for authoritative government/health sites — the schema.ts comment acknowledges. Fine.

  Another: the WO's breadcrumb item 2 "Service Areas /service-areas" — if that URL 301s (middleware redirects combos; and the scheme migrated from /service-areas to /locations), a breadcrumb item pointing at a redirecting/404 URL is bad. The WO says probe it — partial mitigation. But the oracle asserts only "breadcrumb item 3 name == city name"; nothing validates item 2 resolves. reachability-check gate may or may not cover schema URLs. MED.

  **5. THE ONE THING before dispatch:**

  The load-bearing uncertainty: the WO's core premise ("ZERO structured data on location pages, probed: no JsonLd import anywhere under app/locations/") was probed in the wrong place — the page delegates all rendering to components/CityPage.tsx, and lib/schema.ts:237-238 documents that Service nodes are already used on "the city pages." Before dispatch: read components/CityPage.tsx and grep components/ for JsonLd/schema imports to confirm what the location pages actually emit today, then amend the WO (node set, I1 "exactly one graph total," oracle duplicate checks). Also fold the generateMetadata title/description derivation into the WO explicitly so the builder doesn't fork it.

  Verdict: NO — not sound to execute as written. The premise is unverified/likely false (probe didn't cover CityPage), and key string derivations are underspecified with the exemplar actively misleading on title mirroring.

  Also a small one: the WO says "webPageNode (line 198, supports speakable)" — matches. "breadcrumbNode (224)" matches. "faqNode (281)" matches. "locationNode (328)" matches. Good.

  Another: the oracle requires printing cary-nc graph — cary-nc must be a real slug; locationPath(c.slug) — note URLs are /locations/<slug>-nc per the comment ("The param value carries the -nc suffix"). The WO's breadcrumb says /locations/<slug> — is <slug> including -nc? WO §2 says url: /locations/<slug> and oracle GETs /locations/<slug>. The page comment says the canonical scheme is /locations/[city]-nc. So "slug" in the WO is ambiguous: cities.ts slug is probably "cary" and locationPath adds "-nc". If the builder uses `/locations/${c.slug}` directly, they get /locations/cary instead of /locations/cary-nc — wrong URL in WebPage @id, url, and breadcrumb item — a real defect that gates might not catch (the pages would 404 → reachability check might catch it, actually the oracle itself GETs /locations/<slug> — if slug means cary-nc, consistent; but the WO never says to use locationPath()). The page.tsx has `locationPath(c.slug)` right there — the correct helper. The WO should have said "use locationPath(c.slug)". This is a concrete underspecification with a wrong-output failure mode. HIGH-ish/MED. Given the oracle uses the same ambiguous slug, the oracle and the page could agree on wrong URLs (both using bare slug), emitting graphs whose url fields 404 while the oracle passes since it tests the pages it can fetch... wait, if the oracle GETs /locations/cary (bare), that 404s → oracle fails, so builder would notice. Unless middleware redirects /locations/cary → /locations/cary-nc and GET follows redirects. Uncertain. Still a real underspecification: name the helper locationPath. MED-HIGH.

  Also `.seo-answer` on location pages: claimed probed in §1 ("The .seo-answer block exists on location pages (the answer field renders there)") — but page.tsx doesn't render it; CityPage does. "the answer field" — cities have an `answer` field? Plausible. Can't verify. If some city lacks the block, speakable points at nothing (harmless-ish). LOW.

  Also note the disclosure in the brief says client names etc. fine.

  Also I2: "FAQPage mainEntity count == that city's faqs.length" — consistent with faqNode.

  One more possible collision: does the root layout emit organizationNode on every page (per schema.ts comment: "the org node emitted in the root layout")? Yes — comment at line 316-318 says org node is emitted in the root layout. So every page already has a RoofingContractor/LocalBusiness/Organization node from the layout — that's the canonical single org node, fine, and explains the WO's prohibition. Also means location pages already DO have structured data (the org node from root layout + possibly WebSite node) — the WO's "ZERO structured data" is loosely worded but it means page-specific graph; fine, though technically the probe claim "no JsonLd import anywhere under app/locations/" doesn't establish "zero structured data on the rendered page" since layout emits one. Minor wording, but supports the "probe in the wrong layer" finding.

  Severity assignments:
  - F1 (HIGH): premise probe blind spot — CityPage may already emit schema (Service nodes per schema.ts:236-238 comment). Failure: duplicate WebPage/FAQPage/Service nodes, entity confusion, I1 passes while success criterion "one valid graph" fails. Fix: before dispatch, read components/CityPage.tsx; state what it emits; adjust node set + oracle to assert total graph count and no duplicate @types.
  - F2 (HIGH): title/description derivation underspecified + exemplar misleads (exemplar's webPageNode name ≠ its metadata title). Builder must fork generateMetadata's string logic into the page body; two copies drift. Fix: WO should specify exact expressions (copy from generateMetadata) or instruct hoisting a shared local function in page.tsx.
  - F3 (MED): slug ambiguity — /locations/<slug> vs locationPath() (-nc suffix). Wrong @id/url/breadcrumb item URLs. Fix: WO names locationPath(c.slug) explicitly.
  - F4 (MED): breadcrumb middle item /service-areas likely wrong post-migration; page comment says scheme migrated FROM /service-areas; also no visible Breadcrumbs import on the location page (page.tsx renders only CityPage) — if no visible trail exists, BreadcrumbList lacks an on-page counterpart. Fix: probe and require matching the visible trail in CityPage; oracle should assert item 2 URL resolves 200.
  - F5 (MED): FAQ-dedup cross-check absent — city faqs asserted unique per-city but not checked against lib/faqs.ts global questions (e.g., "How much does a new roof cost in Durham?" on a Durham page). Fix: add oracle step diffing city questions against FAQPAGE/topic-owned sets.
  - F6 (LOW): rendered-mirror for FAQ not verified — oracle checks count, not that the page visibly renders each q/a.
  - F7 (LOW): FAQPage is dead as a Google rich result per schema.ts's own comment — WHY overstates SERP benefit; breadcrumbs are the real lever. Cosmetic.
  - F8 (LOW): "ZERO structured data" wording — root layout already emits the org node site-wide; harmless but shows the probe conflated file-level imports with rendered output.

  Answer the five targets numbered.

  Output format: VERDICT, FINDINGS, ANSWERS, optional MISSING. Keep it sharp, hostile, concrete.

To resume this session: kimi -r session_8484ff68-e944-4b91-82e0-1c26c0b75e1b
