# WORK ORDER — KM VALUE-PROP MASTER RESEARCH (ultracode deep-research session)

> Not a build — a **max-rigor, fully-cited research deliverable**. Produce the definitive,
> defensible competitive case for the **King Maker enterprise authority site vs the $297/mo
> GoHighLevel template**, so every sales/reel/deck claim is backed by a source + a confidence
> rating. Run as an **ultracode** session with the **ultra-research** harness. Architect = WE12.
> Compounds on the ~35 sources already gathered this session (§Seed) — VERIFY + EXTEND, don't restart.

## §1 THE LENS — "Defensible, not optimistic. Every claim cited, every number honest."
The goal is a doc Joseph can pull any reel/pitch line from and survive a skeptic, a competitor, or
the FTC. Honest > impressive: flag **modeled vs measured**, give **ranges (conservative/realistic/
optimistic)**, and explicitly mark **what NOT to oversell**. The most persuasive thing in the room is
being the one operator who isn't promising the moon — protect that.

## §2 RESEARCH PILLARS (each: deep, cited, adversarially verified)
1. **Deep vs thin site — traffic + lead multiples.** Does ~130 pages out-perform ~15–20 by 3–5×+?
   Long-tail share of traffic; topical-authority case studies; the traffic→lead compression; the
   "quality gates it" caveat (thin pages get zero/negative). Land a defensible multiple for TRAFFIC
   and (separately) LEADS, with conditions.
2. **Dedicated location/service pages + service×city matrix.** Lift vs a single hub/list; ranking
   organically in suburbs you're not located in (vs the Map-Pack proximity ceiling); the "real-job /
   unique content" requirement + the doorway-penalty downside.
3. **Storm/insurance vertical.** Seasonal search surges (post-storm %), ticket size, close rates,
   share of roofer revenue; why a hub template captures ~none of it. (NC/Triangle specifics.)
4. **Off-page scaling + content×links synergy.** Does deep content amplify off-page ROI (linkable
   assets, more rankable pages, internal-link-mesh authority distribution)? What does **$500/mo**
   off-page realistically buy + its 6–12mo impact? Build the **"$5k own-it + $500/mo efficient ops
   vs $297/mo rent-a-brochure"** case.
5. **GoHighLevel platform SEO limitations.** The architectural ceilings (mobile PageSpeed ~60–70 cap,
   no SSR/edge, client-side render), schema gaps, CMS ceiling ~200 pages, no programmatic generation,
   duplicate-URL traps — cited to GHL's OWN feature boards/support where possible (hardest to dismiss).
   Plus the honest balance (GHL is fine for a 5-page local site + strong at CRM/conversion).
6. **Map-pack honesty.** GBP ≈32% + reviews drive the pack; the site wins ORGANIC + long-tail + storm,
   and *supports* the pack (behavioral/relevance/entity) but doesn't "win the pack with more pages."
   The precise, non-overstated framing.
7. **Conversion architecture.** The 2026 map-pack call-button cut → ranked-brochure call leak (20–40%);
   the 74%-unanswered-calls stat; instant-estimate / dual-intent / speed-to-lead close-rate data.
8. **AI search / future-proofing (honest).** AI ≈ rounding-error on lead VOLUME today (~0.3%), BUT
   agentic booking grounds on the site (schema/pricing/structured data) + the citation future. Sell
   future-proofing + "the site the AI recommends," NOT an AI lead flood.
9. **Platform-tier / scaling-headroom argument.** What do scaled/multi-office roofers actually run
   (custom/WordPress/enterprise CMS) vs the local-lead-gen tier (GHL templates)? "You can't build a
   multi-crew business on a $297 template" — defensible as the *scaled/content* case (don't name brands
   unverified).
10. **Revenue / ROI math.** $20k ticket × close-rate band (15–25%) → value-per-lead (~$3–5k) → annual
    revenue + the DELTA vs each GHL tier; the ramp (cold-start → maturity); conservative/realistic/
    optimistic.
11. **Honesty & defensibility guardrails.** A consolidated "what NOT to claim" list (no "win the pack
    with pages," no AI-lead-flood, no unsourced magnitudes, no SOC2/guarantee language, close-rate is
    on the client). FTC/screenshot-proof posture.

## §3 DELIVERABLES (write to `C:/Users/josep/Claude Gravity/`)
- **`KM_VALUEPROP_MASTER.md`** — the full sourced doc, one section per pillar, every claim cited inline.
- **A CLAIM LIBRARY** (table): each sales/reel claim → the exact stat → the source URL → confidence
  (High/Med/Low, measured vs modeled). This is the bulletproofing layer.
- **An OBJECTION-HANDLING appendix:** "my GBP already gets me leads," the proximity wall, "GHL has
  missed-call-text-back," "isn't this just more pages = doorway spam," "why one-time vs monthly,"
  close-rate-is-on-you — each with the honest, sourced rebuttal.
- **A CONSERVATIVE / REALISTIC / OPTIMISTIC model table** (traffic, leads, jobs/mo, revenue, the delta).
- **A REELS HOOK BANK:** ~10–15 one-line hooks, each tied to a citation + the honest framing.
- (Optional) drop a copy of `KM_VALUEPROP_MASTER.md` to `vault/inbox/` for vault ingestion.

## §4 METHOD (ultra-research harness, max rigor)
Ultrathink framing → parallel fan-out web search (structural citations) → loop-until-saturated gather
(stop when diverse searches surface nothing new; surface remaining gaps as provisional) → first
synthesis → **claim-level red-team/blue-team verification** (try to REFUTE each load-bearing claim;
keep only what survives) → final cited re-synthesis with an **evidence ledger + calibrated confidence**.
Prefer primary sources (Whitespark, Ahrefs, Google, GHL's own boards, BrightLocal, Sterling Sky) over
agency blogs; when a stat traces to a vendor with an incentive, flag it + find corroboration.

## §5 SEED EVIDENCE (already gathered this session — verify + extend, cite)
- Long-tail ≈ **70% of search traffic / ~91.8% of queries** (neilpatel/Backlinko, embryo).
- Ahrefs: **96.55% of pages get zero Google traffic** (quality-not-count caveat).
- Contractor case: **11 location pages → +220% traffic, +160% form fills, calls doubled, 6mo** (Sapphire SEO).
- Roofer cases: **Kodescape +188% traffic/+145% leads; LuccaAM +384% traffic/+294% leads**.
- Storm: **post-storm search +300–800% in 48h; 40–70% of hail-market revenue; $8.5–25k jobs; 45–65% close** (PinPoint).
- Local pack ≈ **32% GBP** weight (Whitespark/Hook Agency); roofing **60–90% of leads from the pack** (UpRankd).
- AI ≈ **~0.3% of contractor leads** today (McChesney + SearchLight); Perplexity ~13% vs ChatGPT ~0.59% citation.
- Map-pack call-button cut → **20–40% fewer calls**; **74% of contractor calls unanswered**.
- Off-page: **#1 pages avg 3.8× backlinks**; long-form **3.5× traffic**; high-quality **4× top-10 odds**;
  internal links distribute PageRank (0.85/hop); local SEO retainers **$500–2k/mo** (GHL build = one-time asset).
- GHL: mobile PageSpeed **~20–45 (cap ~60–70)** vs custom 90–100; no SSR/edge; **schema request 539 votes
  open since 2022**; CMS "rough past 200 pages"; no programmatic generation; duplicate-URL traps
  (PandaCodeGen, GHL ideas board, GHL's own technical-SEO post).
- Full source set (~35 URLs) is in this session's research-agent outputs — pull them all in.

## §6 GUARDRAILS (do NOT produce an over-claim)
Ranges not point-promises; modeled flagged as modeled; "search/organic-driven leads" not "we win the
pack"; AI = future-proofing not volume; no unsourced magnitudes; no guarantee/SOC2/"top-3 in 90 days"
language; concede where GHL is genuinely fine. The deliverable's credibility IS the product.

## §7 REPORT
On completion, report to `website-engineer` (WE12) on the blackboard with the deliverable paths + a
3-line confidence summary (what's High-confidence/measured vs what's modeled). WE12 reviews for the
honesty guardrails before it's used in market.
