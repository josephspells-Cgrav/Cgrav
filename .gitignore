# Dependencies
node_modules/

# Build output
dist/
build/

# Environment files
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Local Netlify folder
.netlify

# Obsidian knowledge vault (Karpathy memory architecture — local-only)
vault/
leads/
king_maker_outbound/
kingmaker/
blackboard/
kingmaker-v3/

# Codex CLI config — contains plaintext MCP API keys, never commit (public repo)
.codex/

# Vale — downloaded style packages (re-fetch with: vale sync)
.vale-styles/ai-tells/

# Alex prompt: master is annotated, VAPI paste file is GENERATED
king_maker_outbound/MABREY_ALEX_VAPI_PASTE.md

# Alarm-co dispatch demo — real customer names/phones/addresses, never public
alarm-dispatch/

# ─── OS63 hygiene pass, 2026-08-24 ────────────────────────────────────────────

# 🔑 CREDENTIALS — never commit. Was untracked but UN-IGNORED, i.e. one
# `git add -A` away from being pushed to a GitHub remote. Verified never
# committed (`git log --all -- king-maker-codes.txt` empty) before ignoring.
king-maker-codes.txt
*codes.txt
.env.production
*.pem
*.key

# Nested git repos — each has its OWN remote and is tracked there. Committing
# them here creates gitlink entries, the exact rot dropped in 3f0dabf
# ("two phantom gitlink entries, showing as permanent D since April").
mabrey-roofing/
groovy-lighting/
wildcat-cctv/
# Client-repo WORKTREES. The client repos above are ignored, but a worktree
# created beside them is a sibling directory that inherits none of that — 12
# `wildcat-wt-*` dirs sat untracked-but-unignored in this PUBLIC repo's working
# tree until 2026-08-30, one `git add -A` away from publishing a client site.
# The dirs are gone; these patterns stop the next ones from reopening it.
wildcat-wt-*/
mabrey-wt-*/
groovy-wt-*/
groovy-worktrees/
mabrey-crm-app/
mabrey-docs/
mabrey-construction/
summit-oak-roofing/

# Throwaway worktrees + WO scratch checkouts
mabrey-wt-*/
mabrey-meta-wt-*/
mabrey-fix-wt-*/
mabrey-crm-wo*/
so-ws-*/
*.bundle

# Capture / audit debris — screenshots and one-off probe harnesses from
# verification runs. Regenerable by definition; never inputs to anything.
.dq-*.png
.we10-*
.h10-*
.h11-*
.r3-*.png
.rate*.png
.rate-capture.cjs
.audit-*
.hero-reg*
.km-anim-frames*
.so-home-audit*
.so-seo-verify*
.so-reaudit/
.res-audit/
.rev-audit/
.rev-cap.cjs
.live-audit/
.live-verify/
.adqa/
.ugc-tmp/
.km-spec/
.v2-deploy.png
.backup-log.txt
HERO-1*-REVIEW/

# Large media + generated output (video-rig public assets are GBs)
video-rig/public/
video-rig/tools/
video-rig/assets/
video-rig/out/
video-rig/ingest/
out/

# Local backups (point-in-time copies, not source of truth)
backups/
_vault-backup-*/

# Stale gate-escape acks, kept for the record but not source
.skills-gate-ack.stale-*

# Media asset piles + vendored runtimes. Ignored HERE because they do not
# belong in this repo — NOT because they are disposable. mabrey-construction-
# assets/ (869 jpgs) and mabrey-ads/ (201 images) are client photography and ad
# creative; their durable home is the mabrey-construction / mabrey-docs repos,
# which have remotes. ⚠️ OPEN QUESTION for Joseph — see the OS63 hygiene report.
mabrey-construction-assets/
mabrey-ads/
mabrey-portfolio/
km-ugc-ads/
km-roofing-concept/
king_maker_brand/
knight-picker/
*.exe
*.dist-info/
*.jsonl

# Worktree seats (live: .wt-deploy-seo, .wt-seo-a5, .wt-seo-rating) — these are
# checkouts of mabrey-roofing, tracked by THAT repo. Never content here.
.wt-*/
# Dot-prefixed WE/WO capture debris (note: tracked `wo/` has no dot, unaffected)
.we[0-9]*
.wo[0-9]*

# Build artifacts inside the untracked-source app dirs tracked below
**/node_modules/
**/.next/
**/test-results/
**/playwright-report/
# ad-swipe: competitor reference recordings (~430MB of mp4/wav). Media, not source.
ad-swipe/**/*.mp4
ad-swipe/**/*.wav
ad-swipe/**/*.m4a
yt-research/**/*.mp4
yt-research/**/*.webm
# mabrey-blueprint-forge: a git WORKTREE of mabrey-roofing (branch
# forge/blueprint-motion-we23), in sync with its own remote. Its .git is a
# FILE (gitdir pointer), not a directory — a naive `[ -d .git ]` check reads it
# as a plain folder and would commit 14,723 files incl. .next build output.
mabrey-blueprint-forge/
memory_check_output.txt

# recovery — decrypted secrets never leave the machine
recovery/_unlocked/
vault/_ark/_unlocked/

# retired agent boot briefs — never publish (contains fenced-path map + prod flags)
AGENTS.md.bak-*

# Local agent permissions + SessionEnd hook. Untracked 2026-08-30: this repo is
# PUBLIC, and the file named the session-end script path plus a 28-entry
# allowlist of what agents may run unprompted. No secrets were in it.
.claude/settings.local.json

# 2026-08-30 (Phase 3): wo/ is 1.8GB of work-product incl. customer geocoords in a
# PUBLIC repo, untracked and UNIGNORED — one `git add -A` from publication and one
# `git clean -fd` from gone. Ignoring closes the add gun and makes `clean -fd` skip it.
# The 206 already-tracked files stay tracked until they are deliberately untracked.
wo/
